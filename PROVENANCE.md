# Provenance

Status: reference as of 2026-09-17. Describes what every part of this codebase is based on, and the licence that follows from it. Components that have since left the repository are named here with the package they went to; each of those packages keeps its own PROVENANCE.

Nothing here is a line-by-line translation of another program, but several components are derivative, with formulas, constants and bit-level behaviours taken from GPL sources, which is why this repository is GPL-3.0-or-later and must stay that way (see COPYING and CLAUDE.md § Licence rules).

Much of what this file was written about now ships as a package rather than as a folder here. Every dependency in `Directory.Packages.props` whose name begins `M0LTE.` was lifted out of this tree, and each one records in its own repository the provenance of the code it took. The rows below keep the lineage regardless, because it was true when the code was written and moving it changed none of it; each row names the package and the path the code had here. `M0LTE.FmChannel`, the FM link model that came out of the test project, is the same story, and `tools/Packet.SoundModem.Ota` is what references it now.

## The four kinds of source

1. **Published specifications, read directly**: the [IL2P specification draft v0.6](https://tarpn.net/t/il2p/il2p-specification_draft_v0-6.pdf) (Nino Carrillo KK4HEJ) including its worked example packets (provided by Jon Naylor G4KLX); the AX.25 v2.2 specification (HDLC rules, CSMA); the KISS specification; the FX.25 draft (Stensat, 2006, via archive.org).
2. **Dire Wolf source, read directly during development** (GPL-2.0-or-later, © John Langner WB2OSZ): `il2p_scramble.c`, `il2p_header.c`, `il2p_init.c`, `fx25_init.c`, `fx25_send.c`, `fx25_rec.c`, `gen_tone.c`, `hdlc_rec.c`, `hdlc_send.c`, `demod_9600.c` (in part).
3. **QtSoundModem** (GPLv3+, © Andrei Kopanchuk UZ7HO, ported by John Wiseman G8BPQ), read in full by the research phase that founded this project (see packet.net `docs/research/headless-soundmodem.md`): its findings supplied algorithms, formulas and constants used here. No QtSoundModem C was transcribed.
4. **Empirical determination**: several wire-format facts were established by experiment, not by any document - encoding against the IL2P spec's example packets byte-for-byte, decoding Dire Wolf-generated audio and having Dire Wolf's `atest` decode ours (bidirectional cross-validation, fixtures committed in `tests/Packet.SoundModem.Tests/Fixtures/`), and benchmarking against the WA8LMF TNC Test CD. The CRC-16/X-25 variant, the legacy max-FEC header bit behaviour, the 9600 IL2P baseband polarity, and the NRZI/scramble ordering are all in this category.

## Per component

| Component | Basis |
|---|---|
| IL2P codec, header, blocks and scrambler (**M0LTE.Il2p**, GPL-3.0-or-later; was `Il2p/*`) | IL2P spec v0.6 first: Hamming encode/decode tables **verbatim from the spec**, block-size computations, sync word, RS parameters. The scrambler's exact bit expressions and initial states (0x00F/0x1F0, 5-bit Galois delay, flush) and the header's bit-field placement are **from Dire Wolf's source** - the spec presents those only as figures. Byte-exact against the spec's three example packets. |
| Reed-Solomon (**M0LTE.Fec**, AGPL-3.0-or-later; was `Fec/ReedSolomon`) | Independent implementation of the textbook algorithms (systematic LFSR encoder; Berlekamp-Massey, Chien search, Forney - the Forney exponent derived from first principles during development). Field/root parameters from the specs and Dire Wolf's init tables. The least derivative DSP component. |
| CRC-16/X-25 and Hamming(7,4) (**M0LTE.Fec**; was `Fec/Crc16X25`, `Fec/Hamming74`) | CRC: the standard X-25 algorithm, variant pinned by the spec's S-frame example (0xF0DB). Hamming: both tables verbatim from the spec. |
| FreeDV datac modulator, demodulator, streaming and burst acquisition (**M0LTE.Ofdm**, AGPL-3.0-or-later; was `Ofdm/*`) | **codec2 lineage (David Rowe et al.), LGPL-2.1.** Ports of codec2 1.2.0 (git 310777b) `ofdm.c`/`ofdm_mode.c`: the TX chain (`ofdm_txframe`/`idft`, Hilbert clipper/BPF, `ofdm_generate_preamble`, burst assembly per `freedv_data_raw_tx`), the RX chain (`est_timing`, `est_freq_offset_pilot_corr`, `ofdm_demod_core`, packet disassembly/UW extraction), the burst acquisition (`est_timing_and_freq`, `burst_acquisition_detector`, `ofdm_sync_search_burst`) and the streaming/burst data sync state machines. Algorithms, constants and accumulation order preserved for interop; validated against codec2-generated vectors and codec2's own CLI tools; those vectors sat in `samples/freedv/` and left with their tests when the OFDM code moved to its package. Same LGPL-2.1 §3 relicensing basis (and pending review R-1) as `Fec/Ldpc/*` below. |
| LDPC, the FreeDV datac codes (**M0LTE.FecLdpc**, AGPL-3.0-or-later; was `Fec/Ldpc/*`, and `tools/gen-ldpc-tables/gen.py` is still here) | **codec2 lineage (David Rowe / Bill Cowley VK5DSP / Valenti CML), LGPL-2.1.** The five sparse parity-check matrices (`H_128_256_5`, `H_256_512_4`, `H_1024_2048_4f`, `H_4096_8192_3d`, `HRA_56_56`) and the built-in decode-oracle vectors are **transliterated verbatim** from codec2 1.2.0 (git 310777b) by `tools/gen-ldpc-tables/gen.py` (a port of the data, not a re-derivation). The sum-product decoder, `phi0` table and RA encoder are ports of codec2's `mpdecode_core.c`/`phi0.c`. Being a derivative of LGPL-2.1 code relicensed into GPL-3.0-or-later via LGPL-2.1 §3 - **awaiting a FOSS-licence review** (docs/dev/archive/ofdm-design.md open item R-1, [roadmap.md](docs/dev/roadmap.md) #10). That review has not happened, and some sixty releases have shipped without it; the roadmap records it as not a blocker rather than as a gate. `.g.cs` tables are generated output committed like the WAV fixtures. |
| `Hdlc/*` (framer, deframer, NRZI) | AX.25/HDLC specification; textbook. Unavoidably similar to every implementation because the protocol dictates the machine. |
| `Modems/AfskDemodulator` | **UZ7HO lineage via QtSoundModem**: chain architecture (BPF → complex mix → I/Q LPF → discriminator), the discriminator formula `(I₀−I₂)Q₁−(Q₀−Q₂)I₁`, and the 1400/650 Hz filter plan are QtSM's `Mux3`/`MODEM_1200`. The power normalisation, silence clamp and envelope min/max slicer are original to this project, developed against the WA8LMF corpus (QtSM solves those differently). |
| `Modems/Afsk1200MultiModem` (offset bank, emphasis variants) | UZ7HO's concepts (`RCVR` offset pairs, `emph_all`); implementations and the content-dedup scheme are original. |
| `Modems/BitDpll` | Dire Wolf's DPLL design and inertia constant (sample at wrap, transitions nudged toward zero, 0.74). The **sub-sample crossing interpolation is original to this project** (neither source has it). A searching/locked inertia switch was tried and regressed - see the comment. |
| `Modems/PacketDcd` | Dire Wolf 1.6's DCD algorithm and constants (±0.125 phase window, 32-transition history, assert ≥30/32, drop ≤6/32) from `fsk_demod_state.h`/`hdlc_rec.c`; fresh code. |
| `Modems/EnergyBusyDetector` | Concept prompted by QtSM/ARDOP's spectral busy detector; the actual design (block power vs min-tracking floor, 6/3 dB hysteresis, hold, warm-up seeding) is original. |
| `Modems/Bpsk*`, `Modems/Qpsk*`, `Modems/CostasLoop` | Symbol maps directly from the IL2P spec; per-mode filter numbers follow QtSM's tables; the fractional one-symbol delay is original. Two detectors: the default coherent path is a textbook decision-directed 2nd-order Costas loop (standard PLL gain equations, decision-directed M-PSK phase detectors) recovering the carrier and differentially decoding the recovered absolute symbols - the NinoTNC's method, implemented fresh; the differential detector is generic DSP. Per-mode loop bandwidths and the coherent-over-differential margin were tuned/verified by measurement in-project (issue #5), not copied. |
| `Modems/FskModem`, `Modems/G3ruhScrambler` | G3RUH scrambler expressions bit-exact from Dire Wolf (`gen_tone.c`/`hdlc_rec.c`), NRZI→scramble ordering confirmed from their source and by bidirectional audio validation. Receive chain outline is `demod_9600` lineage with this project's envelope slicer; the TX pulse shaping differs by design from Dire Wolf's phase-pinning synthesis (compatibility proven empirically). |
| `Fx25/*` | Correlation-tag table, block formats and the 8-bit match tolerance verbatim from Dire Wolf's `fx25_init.c` (protocol constants originating in the Stensat draft); wire behaviour (flag fill, LSB-first, NRZI placement, accumulator direction) from reading their send/receive paths; fresh implementation with an added miscorrection guard. |
| `Modems/C4fskModem` | Wire format from **MMDVM-TNC** (Jonathan Naylor G4KLX, GPL-2.0-or-later): the 0x77 preamble byte, the 4-byte outer-only sync 0x5D57DF7F, the dibit→level map and the "Mode 2 = IL2P bytes on 4-PAM" structure are its `Mode2Defines.h`/`Mode2TX.cpp`/`IL2PRX.cpp` constants and layout, which the NinoTNC C4FSK modes inherit. Established empirically first (symbol analysis of NinoTNC recordings, one error in 316 symbols against a known frame), then confirmed against the MMDVM-TNC source. The receive chain (adaptive outer-envelope 4-level slicer, sign-crossing-only symbol clock, energy-gated bit stream) is this project's own - MMDVM's matched-filter/fixed-clock receiver was read but not ported. TX pulse shaping is a windowed-sinc low-pass approximating their Gaussian BT 0.6 pulse, validated on hardware rather than copied. |
| `Modems/FreeDvDatacModem` | Original pdn glue over the codec2-lineage OFDM engine, now **M0LTE.Ofdm**: the `IModem` surface, the 48 kHz↔8 kHz rate bridge, and the payload convention - the project's own IL2P+CRC bit stream (see `Il2p/*` above) inside the fixed-size datac payloads. FreeDV defines no framing at the raw-data layer and nothing here is taken from FreeDATA. The opt-in end-of-burst extension to the burst sync state machine (`OfdmDemodulator.EndOfBurstUwDrop` + the CRC backstop in `DatacReceiver`) is likewise original, departing from codec2's fixed packets-per-burst to support variable-length bursts; it is off by default so the CLI-validated codec2 port behaviour is unchanged. |
| POCSAG codeword, encoder and decoder (**M0LTE.Pocsag**, AGPL-3.0-or-later; was `Pocsag/*`, of which the paging server `Pocsag/PagingTcpServer.cs` stayed here) | **Spec-first**: CCIR Radiopaging Code No. 1 (ITU-R M.584-2) structure - 576-bit reversal preamble, sync 0x7CD215D8 / idle 0x7A89C197, batches of 8 frames × 2 codewords, BCH(31,21) g(x)=x¹⁰+x⁹+x⁸+x⁶+x⁵+x³+1 + even parity, address split (18 high bits in the codeword, 3 low bits = frame), LSB-first numeric BCD and 7-bit ASCII. Layout confirmed by reproducing both published protocol constants from their own data bits. **Cross-checks, read during development**: multimon-ng (GPL, Sailer/Oenal) `pocsag.c`/`bch.c` for the decode conventions (numeric display charset, polarity - '0' = high frequency = positive baseband, batch state machine, drop-sync-on-uncorrectable policy) and MMDVMHost (GPL, G4KLX) `POCSAGControl.cpp` + DAPNET UniPager (GPL) `generator.rs` for the transmit conventions the spec leaves loose (18-codeword preamble, numeric 0xC space padding, alpha zero-bit padding, function-bit conventions). No code transcribed; the syndrome-keyed ≤2-bit error table and the audio chain (0.55·baud shaping, envelope slicer + shared DPLL) are this project's own. Validated live against multimon-ng 1.3.0 (samples/pocsag/). Note: UniPager's BCH division runs one step past the data bits and does not reproduce the sync word - the spec/multimon/MMDVMHost layout is implemented here. |
| ARDOP frame codec, 4FSK/PSK/16QAM modulator and demodulator, FEC mode (**M0LTE.Ardop**, AGPL-3.0-or-later; was `Ardop/*`, and the daemon keeps `ArdopChannelBridge.cs` and `ArdopBusyDetector.cs`) | **ardopcf lineage (Rick Muething KN6KB / John Wiseman G8BPQ / Peter LaRue AI7YN), MIT** - v1.0.4.1.3, git `a7c9228`, ports with per-file `file:line` citations (docs/dev/ardop-design.md §8.2 policy). The ARDOP Specification Rev 2.0 (2017-11-27, released to the public domain) is cited alongside, but the operative wire format is the code's: the nonstandard CRC-16 (0x8810 constant, LSB injection - spec *says* CCITT), the frame-type parity quirk (both parity symbols from the plain type byte), the frame geometry from `FrameInfo`, and the TX integer sample templates transcribed verbatim from `ardopSampleArrays.c` (`ArdopTxTemplates.g.cs`). The RX chain (leader search, envelope-correlator framing, phase-reversal sync, minimal-distance type decode, Goertzel 4FSK demod, differential 4PSK/8PSK and 16QAM demod on 1/2/4/8 parallel 100 Bd carriers - Hanning-windowed per-symbol phase probes, negated differential decisioning, the 16QAM rolling amplitude threshold, the QAM-only carrier-0 tuning-offset rotation as the reference ships it, phase/magnitude Memory-ARQ via `WeightedAngleAvg`, and the `UpdatePhaseConstellation` quality with its RS-corrections quality floor) and the TX side (comb-resonator filter, `Calc1CarPSKSymbols` differential symbol build with SymSet 2 for 4PSK, `PlayPSKSymbols` time-domain carrier summation with the empirical per-carrier-count scaling factors and `SoftClip`) are behavioural ports of `SoundInput.c`/`Modulate.c`. Reed-Solomon reuses **M0LTE.Fec** - same GF(2^8) 0x11D field and α¹… generator as ardopcf's `lib/rockliff/rrs.c` (Simon Rockliff, permissive) - through the Rockliff byte-reversed wire layout (`ArdopFrameCodec.RsAppend`), **established empirically** against rrs.c-generated vectors. Validated bidirectionally against ardopcf itself: its TXFRAME audio decodes here payload-exact and our audio decodes in its `--decodewav` (samples/ardop/). Noise-knee sweeps on identical audio, in tests that have since moved to the package, measured **trial-identical** decode counts between the two receivers at every swept (mode, SNR) point across the 4PSK/8PSK/16QAM classes - knee delta 0 dB. The ARQ session engine (`Ardop/Arq/*`) is a behavioural port of `ARQ.c` (state machine, `IRSNegotiateBW`, `Gearshift_9` with its threshold tables, `ComputeInterFrameInterval`, DISC/END teardown incl. the END-after-`InitializeConnection` session-ID quirk - ported, not fixed) plus the timer/poll scaffolding of `ARDOPC.c` (`CheckTimers`/`MainPoll`) and the IRStoISS shortcuts of `SoundInput.c`; validated by live ARQ sessions ours↔ardopcf over snd-aloop, both roles, FSKONLY and unrestricted mixed-mode with the gearshift climbing into the PSK rungs (tests/…/ArdopArdopcfLiveSessionTests.cs). The host interface (`Ardop/Host/*`) is a byte-compatible port of `HostInterface.c`/`TCPHostInterface.c` - the full command set with ardopcf's reply/fault formats reproduced byte for byte, including its quirks (the `not recoginized` fault spelling, NEWSTATE's trailing space, PROTOCOLMODE's accept-anything validation, the PING-in-RXO double reply), the CR-framed command socket + 2-byte-BE-length data socket with ARQ/FEC/ERR/IDF tags, per-mode frame routing (`ProcessNewSamples` SoundInput.c:1300-1420), `ProcessUnconnectedConReqFrame` monitor text, the `LostHost` failsafe, and the RXO monitor mode (`RxoComputeDecodeDistance`/`RxoDecodeSessionID`/`ProcessRXOFrame`, RXO.c) - validated by a 107-command transcript diff against a live ardopcf (byte-identical bar VERSION), full-stack daemon↔ardopcf ARQ sessions in both roles driven by Pat's exact scripted command sequences (wl2k-go `transport/ardop/tnc.go`), a real Pat 1.0.0 end-to-end message exchange through our modem, and a live RXO decode of a third-party ardopcf↔ardopcf session (tests/…/ArdopHostLiveTests.cs). |
| `Ms110d/*` (MIL-STD-188-110D App D 3 kHz serial-tone: FEC chain, autobaud preamble, probe-trained DFE, WN0 Walsh modem, `IModem`) | **Spec-first - no external implementation exists** (verified scoping; no open App D implementation or public off-air recording was found, docs/dev/ms110d/design.md §1.2). Every wire constant - PN sequences (fixedPN/cntPN/widPN), Walsh maps, transcoding/constellations, mini-probe bases, K7/K9 polynomials (133/171, 561/753), Table D-L puncture masks, interleaver sizes/increments, WID/downcount checksums, EOM/EOT - is transcribed from MIL-STD-188-110D (29 Dec 2017, Distribution A) via the dual-transcription discipline in `docs/dev/ms110d/tables/` (two independent transcribers, zero-conflict diff, machine self-checks; spec-internal golden vectors: the printed Trinomial(159,31) scrambler sequence + Walsh combine row, the D.5.3.3.2 interleaver worked example, puncture-mask/rate consistency). The DFE/equalizer and soft tail-biting Viterbi are textbook DSP designed from the spec's probe structure only (Proakis ch. 9-10 DFE; Haykin NLMS; wrap-extension tail-biting decoding); the Watterson channel simulator follows the standard's own Appendix E. No AGPL source (mars-suite or otherwise) was consulted at any stage (design § Licence provenance). Validation is hermetic: loopback + the D-LXIV/D-LXV performance masks in simulation - **spec-faithful + mask-passing, not interop-proven**; pdn↔pdn only until an off-air capture or live counterpart exists. |
| `Kiss/*`, `Channel/SoundModemChannel` (CSMA) | KISS specification (framing, commands, the BPQ ACKMODE extension) and AX.25 §6.4 classic p-persistence. Implemented from the specs, with no dependency on packet.net's `Packet.Kiss`. |
| `Audio/*` (ALSA, WAV) and the DSP primitives: FIR design, decimator, upsampler, FFT, spectrum (**M0LTE.Dsp**, AGPL-3.0-or-later; was `Dsp/*`, which now holds `WaterfallSource` and `ConstellationSource` only) | Standard APIs and textbook DSP (windowed-sinc design, polyphase-style rate change, radix-2 FFT). The runtime filter-design *choice* mirrors QtSM's approach. `WaterfallSource` and `ConstellationSource` (per-symbol PSK decision points, batched into auto-ranged scope frames) are original diagnostic side channels of this project, structurally alike; the constellation feed taps the differential product the PSK demodulators already compute (`IConstellationSource`). |
| `Channel/Cm108Ptt` | The de-facto CM108 HID report convention (5-byte output report, GPIO3) as documented and used by Dire Wolf and QtSoundModem. |
| `UberSdr/PcmBinaryDecoder` | **ka9q_ubersdr lineage** (madpsy, GPL-3.0 - compatible with this repo): a direct port of `clients/iq-recorder/pcm_decoder.go` (`DecodePCMBinary`). The hybrid `PC`/`PM` header layout, field offsets, the big-endian int16 payload and the pre-seeded rate/channels convention are that client's, cited in the file per this repo's porting rule. Cross-checked against it on a live simultaneous capture: bit-identical in steady state (docs/dev/archive/ms110d/ota-capture-client-plan.md § C1). |
| `UberSdr/UberSdrAudioInput`, `UberSdr/UberSdrDevice` | Original. The connection flow it re-implements (`POST /connection` → `/api/description` → `wss://…/ws?mode=iq48&format=pcm-zstd`) is documented from reading ka9q_ubersdr's `clients/iq-recorder/main.go`; the reconnecting receive loop, the ring buffer and the `IAudioInput` surface are this project's. |
| `Iq/DigitalDownconverter`, `Iq/MultiChannelReceiver` | Original. Textbook NCO + decimating FIR, mirroring **M0LTE.Dsp**'s `Decimator` in its output-instant evaluation. |
| SSB demodulation from complex baseband | Was `Iq/IqToAudioConverter` + `Iq/StreamingIqToAudioConverter` here; **lifted into `M0LTE.Dsp` 0.2.0** as `SsbDemodulator`/`StreamingSsbDemodulator` (2026-08-02), being general-purpose DSP rather than anything modem-specific. Original work either way - see that package's own PROVENANCE. |
| FlexRadio 6000-series client (**M0LTE.Flex**, AGPL-3.0-or-later; `FlexRadio/FlexDevice.cs` here is the daemon's adapter onto it) | Written here against FlexRadio's published SmartSDR TCP command and DAX interfaces, then lifted out so other programs could use it. That package records its own provenance. |
| Audio and PTT interfaces (**M0LTE.Radio.Audio**, AGPL-3.0-or-later) | The `IAudioInput`, `IAudioOutput` and `IPttControl` surfaces that the devices and modems here implement, shared so that out-of-tree modems compile against one definition. That package records its own provenance. |
| Daemon, KISS TCP server, config | Original. |

## What was not taken

QtSoundModem's program structure (global state, threading, buffer handling), its Memory-ARQ and dual-threshold recovery machinery, and Dire Wolf's multi-slicer are not ported. Where this project needed equivalents it either engineered its own (envelope slicer, dedup, busy detector) or recorded the lever as future work in [docs/dev/plan.md](docs/dev/plan.md).

## Licence consequence

Components above carry material derived from Dire Wolf (GPL-2.0-or-later, upgradeable), UZ7HO/QtSoundModem (GPLv3+), MMDVM-TNC (GPL-2.0-or-later, upgradeable) and ka9q_ubersdr (GPL-3.0). Beyond that, everything here was written with both GPL trees having been read, so the conservative licence for the whole is **GPL-3.0-or-later**, chosen at the project's founding (packet.net `docs/research/headless-soundmodem.md` §4 and §Decisions, 2026-07-14) rather than claiming a clean-room permissive implementation that could not be substantiated.

Moving a component into a package changed neither what it derives from nor what this repository is. `M0LTE.Il2p` and `M0LTE.FmChannel` are GPL-3.0-or-later; `M0LTE.Ardop`, `M0LTE.Dsp`, `M0LTE.Fec`, `M0LTE.FecLdpc`, `M0LTE.Flex`, `M0LTE.Ofdm`, `M0LTE.Pocsag` and `M0LTE.Radio.Audio` are AGPL-3.0-or-later, as are `Packet.Ax25` and `Packet.Core` from 0.26.0. GPLv3 §13 permits that combination, so this repository stays GPL-3.0-or-later, but AGPL §13's network-source requirement travels with those dependencies onto the combined work, which CLAUDE.md § Licence rules permits and records as approved for M0LTE.Flex. Nothing here may be copied into an MIT-licensed package, and no MIT-licensed package may depend on `pdn-soundmodem`.
