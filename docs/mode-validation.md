# pdn-soundmodem - mode validation report and provenance ledger

Living document. Tracks every mode string accepted by `ModemCatalog` (`src/Packet.SoundModem/Modems/ModemCatalog.cs`), how each has been validated (simulation and over-the-air), and - the point of this file - a dated, append-only ledger of *who/what proved a mode working*, with special attention to modes that were **broken and later fixed**, so a regression can always be traced back to the evidence that closed it the first time.

## Purpose and maintenance rule

This file exists because "does mode X work" is otherwise answered by grepping commit messages and hoping. Two things must stay true as the project moves:

1. **Every mode string in `ModemCatalog.KnownModes` appears in the matrix below**, with its current status. When a mode is added, removed, or renamed in the catalog, this file changes in the same PR (mirrors the `docs/plan.md` discipline).
2. **Every time a mode's validation status changes - especially broken → fixed, or working → regressed - append a dated entry to the [Validation ledger](#validation-ledger)** citing the exact commit hash, PR number, and/or issue number that did it, plus the evidence (mask figures, corpus decode counts, on-air BER). Do not edit or delete a prior ledger entry to "correct" it in place; append a new one and let the old one stand as the historical record. Where the provenance is genuinely not known, write **TBD** - never invent a commit, PR, or issue number.

Status legend used in the matrix:

- **working** - validated both in simulation/interop and confirmed over real RF (or, for corpus-based checks, against a real captured off-air/rig recording).
- **partial** - validated in simulation, but the on-air/interop result is mixed, marginal, or has a known open issue against it.
- **untested** - no meaningful validation recorded beyond the mode constructing (`ModemCatalog.Create` succeeding), or coverage is too thin to call it exercised.
- **not-yet-on-air** - solid simulation/interop validation; no real-RF attempt has been made yet.

## Mode matrix

> The reader-facing capability view of this matrix is [docs/modes.md](modes.md) - when a
> row's Status changes here, update that table's row in the same PR.

| Mode | Family | What it is | Sim validation (test/mask) | On-air validation | Status |
|---|---|---|---|---|---|
| `afsk1200` | AFSK | 1200 Bd Bell-202 AFSK, AX.25 HDLC framing (UZ7HO Mux3-style BPF→mix→I/Q-LPF→discriminator→DPLL chain) | `AfskLoopbackTests.cs` (clean/quiet/noisy/back-to-back/split-block); `DirewolfCrossValidationTests.cs` (4/4 vs Dire Wolf `atest` on independent `gen_packets` fixtures); `OccupiedBandwidthTests.cs` (spectral shape vs a checked-in real NinoTNC 48 kHz capture, `samples/ninotnc/afsk1200.wav`); `NinoTncParityTests.cs` (acquires at TXDELAY 0, matching real-NinoTNC bench figures); `QtsmInteropTests.cs` (qtsm→ours 10/10 UI frames) | **Real off-air corpus**: WA8LMF TNC Test CD Tracks 1+2 (APRS 1200 AFSK), multi+emphasis bank scores 972/970 (Track 2) and 959/999 (Track 1) vs Dire Wolf `atest` on the same corpus (`docs/plan.md` Phase 1) | ✅ working  **2026-08-21 (later6): seven timing phases + clock hold** - awgn +5/+6 dB 27/107 -> 49/129 of 200 |
| `afsk1200-fx25` | AFSK | AFSK1200 + FX.25 correlation-tag FEC, TX+RX | `Fx25ModemTests.cs`, `Fx25Tests.cs` (roundtrip + FEC error correction) | none recorded | 🚧 not-yet-on-air |
| `afsk1200-fx25rx` | AFSK | AFSK1200 + FX.25, receive-only | same as above | none recorded | 🚧 not-yet-on-air |
| `afsk1200-multi` | AFSK | AFSK1200 with a 3-pair (±offset) frequency-diversity decoder bank | `MultiDecoderTests.cs` (off-tuned TX decodes on an offset branch, on-frequency signal emitted exactly once); `CentreFrequencyTests.cs` | Same WA8LMF corpus as `afsk1200` - the 972/970 and 959/999 figures above are the multi+emphasis bank's numbers | ✅ working  **2026-08-21 (later6): seven timing phases per branch + clock hold** |
| `afsk1200-il2p` | AFSK | AFSK1200, IL2P+CRC framing | `Afsk1200Il2pLoopbackTests.cs` (crc/nocrc roundtrip); `DirewolfCrossValidationTests.cs` (IL2P-over-AFSK fixture, Dire Wolf's RESERVED-bit set-vs-clear tolerated by design); `OccupiedBandwidthTests.cs`, `NinoTncParityTests.cs`, `CentreFrequencyTests.cs` | none recorded (cross-validated against two independent implementations offline; no live off-air burst decoded yet) | 🚧 not-yet-on-air  **2026-08-21 (later6): seven timing phases + clock hold** - fm-data +7/+8 dB CNR 50/143 -> 60/151 of 200  **2026-08-22 (later2): a long run of one scrambled bit value no longer reads as silence and drops DCD mid-frame on a quiet channel (issue #339)** - see ledger |
| `afsk1200-il2p-nocrc` | AFSK | AFSK1200, IL2P framing without the CRC trailer | Construction only (`ModemCatalogTests.Create_Builds_Every_Known_Mode_At_Its_Native_Rate`) - no dedicated loopback/behavioural test found | none | ❔ untested  **2026-08-22 (later2): a long run of one scrambled bit value no longer reads as silence and drops DCD mid-frame on a quiet channel (issue #339)** - see ledger |
| `afsk300` | AFSK | 300 Bd AFSK, AX.25 HDLC framing | `OccupiedBandwidthTests.cs` (500 Hz published-limit check + never-wider-than-a-real-NinoTNC check against `samples/ninotnc/afsk300.wav`); `NinoTncParityTests.cs`; `DcdTests.cs`; `CentreFrequencyTests.cs` | none (spectral/acquisition cross-validated against a real NinoTNC capture; no full off-air frame decode recorded) | 🧪 sim-characterised 2026-07-28 (AWGN ~+8 dB; D.6.1 Poor fails - mode limit); on-air blocked by RSP1 CFO drift, **#116**  **2026-08-21 (later6): seven timing phases + clock hold** - awgn 0/+1 dB 70/161 -> 86/175 of 200 |
| `afsk300-il2p` | AFSK | 300 Bd AFSK, IL2P framing | `OccupiedBandwidthTests.cs` (published-limit only, no NinoTNC-capture comparison for this exact arm); `NinoTncParityTests.cs` | none | 🚧 not-yet-on-air |
| `afsk300-il2pc` | AFSK | 300 Bd AFSK, IL2P+CRC framing, ±250 Hz-branch frequency-diversity bank by default (`Afsk300MultiModem`) | `OccupiedBandwidthTests.cs` (published limit + never-wider-than vs `samples/ninotnc/afsk300-il2pc.wav`); `NinoTncParityTests.cs`; `Afsk300MultiModemTests.cs` (off-tune decode, dedupe, offset reporting, pairs:0 collapse) | **Real off-air, 2026-08-02**: first on-air decode (`GB7BEX-15`→`GB7IOW-1`, CRC valid, 0 RS corrections, 7.050300 MHz via `ubersdr:` on `m9psy-1`); **same day (later), the decode-rate fix** - on 30 min of captured slot traffic (13 CRC-valid frames, 3 stations, offsets −3/+35/−35 Hz) the wide-filter single modem copied 3/13, the new bank 13/13; a 10 s clip of the failure mode (neighbouring QSO inside the old ±400 Hz passband) is committed as `samples/offair/m9psy-40m-afsk300-il2pc-qrm.wav`, guarded three ways by `OffAirAfsk300Tests.cs` | ✅ working (bank default 2026-08-02 - see ledger)  **2026-08-21 (later6): seven timing phases + clock hold** - awgn -2/-1 dB 61/177 -> 92/187 of 200  **2026-08-22 (later2): a long run of one scrambled bit value no longer reads as silence and drops DCD mid-frame on a quiet channel (issue #339)** - see ledger |
| `bpsk300` | BPSK | 300 Bd BPSK, IL2P+CRC, differential detector + 4-pair frequency-diversity bank by default | `BpskLoopbackTests.cs`, `BpskMultiModemTests.cs`, `BpskCarrierOffsetEstimatorTests.cs`, `CoherentDetectionTests.cs`, `ConstellationTests.cs`, `CentreFrequencyTests.cs`; `OccupiedBandwidthTests.cs` (vs real NinoTNC capture); `NinoTncParityTests.cs` (differential + coherent acquisition variants); `QtsmInteropTests.cs` (qtsm→ours 10/10 under coherent) | **Real off-air**: `OffAirBpskTests.cs` decodes a real GB7RDG NinoTNC BPSK300 IL2P+CRC capture, verifies GB7RDG→EI0RSI addressing and an ~8 Hz carrier offset (issues #40/#42). **Plus an on-air BER-vs-SNR waterfall** over the Flex→RSP1 rig (2026-07-28, `docs/ms110d/evidence/2026-07-28-bpsk300-coverage/`): AWGN threshold ≈ −3 dB, D.6.1 Poor a mode limit; sim-confirmed. `NinoTncMissCorpusAspirationTests.cs` tracks 37 real off-air frames from a 24 h GB7RDG benchmark as a non-blocking scoreboard - 32 of 37 copy as of PR #236 (see the 2026-08-06 ledger entry), 5 still don't. **Also proven through a web receiver, 2026-08-02**: 7 live UK 40 m frames from 3 stations (`GB7OXF-2`→`GB7RDG-2`, `GB7WEM`, `GB7BWR`), all CRC-valid, decoded at 7.051600 MHz over the `ubersdr:` device (the −15 to −30 Hz "offsets" originally quoted with that run were branch labels, not measurements - see the 2026-08-04 ledger entry) | ✅ working (with an open residual-miss aspiration scoreboard, see ledger)  **2026-08-21 (later4): seven timing phases + clock hold** - -5/-4 dB 133/184 -> 174/197 of 200  **2026-08-22 (later2): a long run of one scrambled bit value no longer reads as silence and drops DCD mid-frame on a quiet channel (issue #339)** - see ledger  **2026-08-22 (later3): the factory aligned to the deployed bank's 0.35 roll-off; the 0.20 rested on a mis-measured NinoTNC reference (issue #340)** - see ledger |
| `bpsk300-multi` | BPSK | Same factory arm as `bpsk300` (`BpskMultiModem`, default bank) | identical to `bpsk300` | identical to `bpsk300` | ✅ working  **2026-08-22 (later2): a long run of one scrambled bit value no longer reads as silence and drops DCD mid-frame on a quiet channel (issue #339)** - see ledger |
| `bpsk300-nocrc` | BPSK | 300 Bd BPSK without the IL2P CRC trailer | Construction only - no dedicated loopback test found | none | ❔ untested  **2026-08-22 (later2): a long run of one scrambled bit value no longer reads as silence and drops DCD mid-frame on a quiet channel (issue #339)** - see ledger |
| `bpsk1200` | BPSK | 1200 Bd BPSK, IL2P+CRC, same differential/diversity-bank default | `BpskMultiModemTests.cs`, `CoherentDetectionTests.cs`, `ConstellationTests.cs`, `CentreFrequencyTests.cs`; `OccupiedBandwidthTests.cs` (2400 Hz published limit + vs real NinoTNC capture `bpsk1200.wav`); `NinoTncParityTests.cs`; `QtsmInteropTests.cs` (qtsm→ours 10/10) | **On-air AWGN validated** (2026-07-28, `docs/ms110d/evidence/2026-07-28-ssb-coverage/`): 11/18 decode over the Flex→RSP1 rig, threshold matches sim (~+3 dB corrected) | ✅ working (AWGN; D.6.1 Poor is a mode limit - sim-confirmed)  **2026-08-21 (later4): seven timing phases + clock hold** - +1/+2 dB 131/186 -> 173/198 of 200 |
| `bpsk1200-multi` | BPSK | Same factory arm as `bpsk1200` | identical to `bpsk1200` | via `bpsk1200` (AWGN on-air, 2026-07-28) | ✅ working (AWGN) |
| `qpsk600` | QPSK | 600 Bd (V.26A) QPSK, IL2P+CRC, coherent (Costas) default | `QpskLoopbackTests.cs` is 2400/3600-only, but `CentreFrequencyTests.cs`, `CoherentDetectionTests.cs`, `OccupiedBandwidthTests.cs` (500 Hz published limit + vs real NinoTNC capture `qpsk600.wav`), `NinoTncParityTests.cs`, `QtsmInteropTests.cs` (qtsm→ours 10/10) all cover it | none | ⚠️ partial - **issue #11 (open)**: interoperates with QtSoundModem only marginally (9/10 and 6/10 across two runs) even with the correct V.26A pairing; sim-characterised 2026-07-28 (AWGN ~+3 dB; D.6.1 Poor fails - mode limit), on-air blocked by RSP1 CFO drift **#116**  **2026-07-31: default reversed to differential** - coherent copied 0-1/3 real NinoTNC corpus frames (wired loop); differential copies 9/9 across all TXDELAY tiers and widens the CFO wall ±5 → ±10-40 Hz (+1.5 dB AWGN). The #11 mechanism addressed pending a live QtSM retest  **2026-08-21 (later3): nine-branch differential bank (`QpskMultiModem`) + the #236 chain port (#326)** - AWGN knee ~0.8 dB lower (0 dB 69 -> 82 %), CFO 30 Hz 3 -> 100 % at +4 dB; the real off-air burst of `samples/offair/2026-08-21/` still does not copy (10 wrong bytes against an 8-byte RS floor, truth recovered)  **2026-08-21 (later4): seven timing phases + clock hold** - -1/0 dB 100/166 -> 146/187 of 200, and **the 2026-08-21 off-air burst copies** (6 bytes corrected of 8) |
| `qpsk2400` | QPSK | 2400 Bd (V.26A/DW2400) QPSK, IL2P+CRC | `QpskLoopbackTests.cs`, `CentreFrequencyTests.cs`, `ConstellationTests.cs`, `CoherentDetectionTests.cs`; `OccupiedBandwidthTests.cs` (2400 Hz limit + vs real capture); `NinoTncParityTests.cs`; `QtsmInteropTests.cs` - pinned regression pair for **issue #6**: decodes QtSM's V26A/DW2400 (type 12) but not its legacy "QPSK AX.25 2400bd" (type 10) - a real protocol-variant difference, both directions asserted, not treated as a bug | none | ✅ working (interop nuance is documented/pinned by design, not an open defect); **on-air over the Flex→RSP1 rig blocked by CFO/phase-noise fragility (#116)** - 0/4 at dial 0, 0/6 at dial −10 - sim-characterised 2026-07-28 (AWGN ~+10 dB, D.6.1 Poor a mode limit)  **2026-07-31: default reversed to differential** (family flip) - corpus 3/3 either way; CFO half-width ±8 → ~±60 Hz (+0.4 dB AWGN) - the on-air #116 wall materially widened  **2026-08-21 (later3): nine-branch differential bank + #236 chain port + DPLL crossing interpolation (#326)** - AWGN knee ~1 dB lower (+5 dB 3 -> 35 %, +7 dB 72 -> 90 %), CFO 100 % across the +-120 Hz comb at +9 dB  **2026-08-21 (later4): seven timing phases + clock hold** - +6/+7 dB 155/180 -> 190/195 of 200 |
| `qpsk3600` | QPSK | 3600 Bd QPSK, IL2P+CRC | `QpskLoopbackTests.cs`, `CentreFrequencyTests.cs`, `ConstellationTests.cs`, `CoherentDetectionTests.cs`; `OccupiedBandwidthTests.cs` (3 kHz voice-channel fit + vs real capture `qpsk3600.wav`, corrected for issue #2's measurement bug); `NinoTncParityTests.cs`; `QtsmInteropTests.cs` (qtsm→ours 10/10) | none | 🚧 not-yet-on-air - sim-characterised 2026-07-28 (AWGN ~+14 dB); Flex-OTA-rig TX deferred, **#118**  **2026-07-31: default reversed to differential** - coherent copied 1-2/3 real NinoTNC corpus frames (#144); differential 9/9 across tiers; CFO half-width ~±60 Hz (+3.5 dB AWGN)  **2026-08-21 (later3): #326's timing fixes reach it (one-branch bank, detector unchanged)** - AWGN +8 dB 66 -> 81 %, fm-mic +8 dB CNR 62 -> 78 %, fm-data 54 -> 80 %; the decision-feedback reference re-measured and still loses its FM knee (78 vs 68 %), so it stays off  **2026-08-21 (later4): seven timing phases + clock hold** - AWGN +8 dB 81 -> 95 %, fm-mic +8 dB CNR 78 -> 85 % |
| `fsk9600` | FSK | 9600 bps GFSK, classic HDLC/AX.25 framing | `Fsk9600LoopbackTests.cs`; `NinoTncParityTests.cs` (`Fsk9600_Classic_Acquires_At_10ms_Like_A_NinoTNC`, matching the ~6-word/10 ms real-NinoTNC acquisition figure) | none | 🚧 not-yet-on-air - sim-characterised 2026-07-28 (AWGN ~+15-18 dB, FM discriminator-noise-limited); Flex-OTA-rig TX deferred, **#118**  **2026-08-21 (later5): seven timing phases per symbol** - AWGN +14/+15/+16 dB 63/119/159 -> 167/195/199 of 200, fm-data +12/+13/+14 dB CNR 27/83/135 -> 136/188/198 (TXDELAY 150; at TXDELAY 0 this leg still cannot acquire, unchanged) |
| `fsk9600-il2p` | FSK | 9600 bps GFSK, IL2P+CRC framing | `Fsk9600LoopbackTests.cs` (IL2P framing, back-to-back frames, scrambler mid-stream self-sync); `NinoTncParityTests.cs` | none | 🚧 not-yet-on-air  **2026-08-21 (later5): seven timing phases per symbol** - AWGN +9/+10/+11 dB 45/128/177 -> 135/183/197 of 200, fm-data +10/+11/+12 dB CNR 106/156/183 -> 171/188/199; still a hard 0 through a microphone path, which is the mode, not the receiver  **2026-08-22 (later2): a long run of one scrambled bit value no longer reads as silence and drops DCD mid-frame on a quiet channel (issue #339)** - see ledger |
| `fsk4800-il2p` | FSK | 4800 bps GFSK (NinoTNC RUH-4800-compatible), IL2P framing | `NinoTncParityTests.cs`; `QtsmInteropTests.cs` - pinned for **issue #10**: QtSM's RUH-4800 transmission decodes on our receiver (checked-in `QtSm_Ruh4800_Transmission_Decodes_On_Our_4800_Receiver`) | **ours→QtSM direction verified live on the rig** (documented in `QtsmInteropTests.cs` remarks, not a committed automated test - QtSM is not in-process) | ✅ working (one direction is a checked-in regression test, the other is a documented manual rig verification - see ledger)  **2026-08-21 (later5): seven timing phases per symbol** - AWGN +6/+7/+8 dB 16/74/133 -> 51/147/180 of 200, fm-data +9/+10/+11 dB CNR 78/138/185 -> 137/181/193, and its first frames ever through a microphone path (0/50 -> 4/50 at +27 dB CNR, 8/50 at +33)  **2026-08-22 (later2): a long run of one scrambled bit value no longer reads as silence and drops DCD mid-frame on a quiet channel (issue #339)** - see ledger |
| `c4fsk9600` | C4FSK | 4-level FSK, 9600 bps, NinoTNC-compatible | `NinoTncParityTests.cs` (TXDELAY-0 acquisition parity) | **Bench-proven 8/8 us→NinoTNC on first live attempt**, 2026-07-16, when `C4fskModem` landed (graduated aspiration, recorded in `NinoTncAspirationTests.cs` remarks and `docs/plan.md`) | ✅ working  **2026-08-21 (later5): seven timing phases + clock hold** - fm-data knee 16.6 -> 14.8 dB CNR (TXDELAY 0) and 18.9 -> 16.8 dB (150 ms); AWGN knee 18.0 -> 16.9 dB (TXDELAY 0)  **2026-08-22: decision-directed envelope (#336)** - fm-data knee 13.2 dB CNR at TXDELAY 0, 12.8 at 150 ms, 13.0 at 300 ms; AWGN 16.5 / 17.2 / 17.5 dB; the long run-in no longer costs the mode |
| `c4fsk19200` | C4FSK | 4-level FSK, 19200 bps, NinoTNC-compatible | `NinoTncParityTests.cs` | Same 2026-07-16 live bench session: **8/8 us→NinoTNC on both C4FSK modes at first attempt** | ✅ working  **2026-08-21 (later5): seven timing phases + clock hold** - fm-data knee 16.2 -> 14.4 dB CNR (TXDELAY 0) and 17.9 -> 16.3 dB (150 ms); AWGN knee 20.9 -> 19.9 dB (TXDELAY 0)  **2026-08-22: decision-directed envelope (#336)** - fm-data knee 12.6 dB CNR at TXDELAY 0, 12.4 at 150 ms, 12.4 at 300 ms; AWGN 19.4 / 19.7 / 19.8 dB |
| `freedv-datac0` | FreeDV DATAC (OFDM) | codec2 OFDM data mode, 291 bit/s payload, IL2P+CRC AX.25 over the `M0LTE.Ofdm` engine | `FreeDvDatacModemTests.cs` (IL2P+CRC frame roundtrip on the 48 kHz deployment path, in `main`); AWGN/Poor/Good statistical baseline vs published codec2 operating points - **PR #104, merged into branch `ms110d-ota-harness`, not yet forwarded to `main`** | **On-air 2026-07-28** (`docs/ms110d/evidence/2026-07-28-freedv-ofdm-oncar/`, DAX route): AWGN clean to −3.7 dB, Poor all rungs acquire+decode | ✅ working |
| `freedv-datac1` | FreeDV DATAC (OFDM) | 980 bit/s, wideband (1700 Hz) datac mode | `FreeDvDatacModemTests.cs`; PR #104 baseline: AWGN knee ~1.2 dB better than the 2020 codec2 figure; Poor (MPP) reads 12 points below FreeDV's published 92/100 | **On-air 2026-07-28**: AWGN clean to +1.8 dB - **exact match to the +1.8 dB sim baseline**; Poor to +4.6 dB (~0.7 dB of sim). **Real path 2026-08-15**: 11/14 (79 %) from the production GB7RDG daemon at 50 W to `ubersdr:wessex.zapto.org`, all `crc ok fec 0 +2 Hz` | ✅ working |
| `freedv-datac3` | FreeDV DATAC (OFDM) | 321 bit/s, narrow (500 Hz) datac mode | `FreeDvDatacModemTests.cs`; PR #104 baseline: AWGN 90 % (10 %-PER) knee −3.2 dB vs codec2's published −3 to −3.5 dB (essentially exact); Poor within ~1 dB of the published MPP point | **On-air 2026-07-28**: AWGN clean to −3.8 dB (sim −3.2, within 0.6); Poor to −0.7 dB (~sim −1.3). **Real path 2026-08-15**: 6/6 (100 %) from the production GB7RDG daemon at 50 W to `ubersdr:wessex.zapto.org`, all `crc ok fec 0 +2 Hz`; the robust choice where datac1 ran out of margin | ✅ working |
| `freedv-datac4` | FreeDV DATAC (OFDM) | 87 bit/s, narrowest (250 Hz) long datac mode | `FreeDvDatacModemTests.cs`; PR #104 baseline: Poor closest of all six modes to its published MPP point (84 vs 90/100) | **On-air 2026-07-28**: AWGN + Poor both acquire+decode (very robust narrow mode; 3 kHz-ref thresholds understate in-band SNR) | ✅ working |
| `freedv-datac13` | FreeDV DATAC (OFDM) | 64 bit/s, short signalling datac mode | `FreeDvDatacModemTests.cs`; PR #104 baseline: Poor reads well below the published point, attributed mainly to codec2's own documented long-PER-tail on short modes plus a burst-vs-continuous-stream methodology difference | **On-air 2026-07-28**: AWGN + Poor both acquire+decode (narrow signalling mode) | ✅ working |
| `freedv-datac14` | FreeDV DATAC (OFDM) | 58 bit/s, shortest signalling datac mode | `FreeDvDatacModemTests.cs`; PR #104 baseline: same short-mode caveat, furthest below its published MPP point of the six | **On-air 2026-07-28**: AWGN + Poor both acquire+decode (narrow signalling mode) | ✅ working |
| `ms110d-wn0` | MS110D | 75 bps Walsh-75 fallback waveform (App D) | Sim: hard-gated at mask, canonical **0 errs** / disjoint **3** errs (bound 2.92E-6) - PRs #87, #90 (DD-MRC Walsh RAKE + symmetric echo window) | **On-air 2026-07-27** (full campaign): AWGN clean to −3.5 dB; Poor clean to −0.5 dB, at/below the −1 dB mask  **On-air over a real path 2026-08-03** (§E4, 7.1285 MHz → wessex.zapto.org, 48 W): acquired, WID matched, **BER 0.00E+000 bit-exact**, EOM. | ✅ working |
| `ms110d-wn1` | MS110D | BPSK r1/8, U=48/K=48 | Sim: hard-gated, **0/0** both seed families - PR #75 (WID soft-vote fix) | **On-air 2026-07-27** (full campaign): AWGN clean to −2.5 dB, Poor clean to −1.6 dB (no separate Poor mask defined)  **On-air over a real path 2026-08-03** (§E4, 7.1285 MHz → wessex.zapto.org, 48 W): acquired, WID matched, **BER 0.00E+000 bit-exact**, EOM. | ✅ working |
| `ms110d-wn2` | MS110D | BPSK r1/4, U=48/K=48 | Sim: hard-gated at 6 M-bit budget, canonical 30/6.08 M = 4.93E-6, disjoint 29 (bound 6.84E-6) - PR #77 (B3.2 anchor-ridge) | **Was broken over real RF** (issue #101: all bursts `SignalLost`, DFE dead-init ~50× low gain at low absolute receive level). **Fixed** by an input-level AGC ahead of the DFE, mask-neutral by construction (PR #103, merged to `main`). On-air confirmation run, 2026-07-27: WN2 Poor decodes coded-BER-0 over the Flex→RSP1 rig, the AGC firing 19-32× on the low-level bursts (was all-`SignalLost` pre-fix). Confirmation evidence: `docs/ms110d/evidence/2026-07-27-ota-poor-validation/` (the post-fix re-run) and `docs/ms110d/evidence/2026-07-27-110d-full-campaign/`.  **On-air over a real path 2026-08-03** (§E4, 7.1285 MHz → wessex.zapto.org, 48 W): acquired, WID matched, **BER 0.00E+000 bit-exact**, EOM. | ✅ working (see ledger for the broken→fixed transition) |
| `ms110d-wn3` | MS110D | BPSK r1/3, U=96/K=32 | Sim: hard-gated, **0/0** - PR #71 (B0 weighted-RLS fix, closing issue #64's remainder) | **On-air 2026-07-27** (full campaign): AWGN clean to −0.6 dB, Poor clean to −1.4 dB (no separate Poor mask defined)  **On-air over a real path 2026-08-03** (§E4, 7.1285 MHz → wessex.zapto.org, 48 W): acquired, WID matched, **BER 0.00E+000 bit-exact**, EOM. | ✅ working |
| `ms110d-wn4` | MS110D | BPSK r2/3, U=96/K=32 | Sim: hard-gated, canonical **0**, disjoint **3** - PR #73 (Phase B2 exit gate) | **Real off-air lab campaign** (`docs/ms110d/evidence/2026-07-27-ota-lab-campaign/`, wired Flex→125 dB pad→RSP1 rig): AWGN knee below the +5 dB mask; Poor **8 of 9 bursts coded-clean**, the one MS110D Poor mode whose DFE both initialises healthy and recovers from real-RF fades  **On-air over a real path 2026-08-03** (§E4, 7.1285 MHz → wessex.zapto.org, 48 W): acquired, WID matched, **BER 0.00E+000 bit-exact**, EOM. | ✅ working - the strongest on-air-proven MS110D point |
| `ms110d-wn5` | MS110D | BPSK r3/4, U=256/K=32 | Sim: hard-gated at 6 M budget, canonical 23 (bound 5.32E-6), disjoint **0** - PR #88 (B4 gate, carrier-false-lock fix via `EstimateCarrierFit`) | **On-air 2026-07-27** (full campaign): AWGN clean to +3.7 dB; Poor clean to +5.1 dB, at/below the +11 dB mask  **On-air over a real path 2026-08-03** (§E4, 7.1285 MHz → wessex.zapto.org, 48 W): acquired, WID matched, **BER 0.00E+000 bit-exact**, EOM. | ✅ working |
| `ms110d-wn6` | MS110D | QPSK r3/4, U=256/K=32 | Sim: hard-gated at 6 M budget, canonical 35/6.49 M = 5.39E-6, disjoint 39 = 6.01E-6 - PR #89 (B4.1 SPIKE-UP pricing) | **Was broken over real RF** (issue #102: locks the waveform correctly, then the DFE collapses into the first Watterson fade and never recovers, all bursts `SignalLost`). Root cause isolated by hardware-free phase-noise injection to **receiver reference phase-noise** (1-3 Hz effective linewidth, inside the RSP1 rig's measured 0-8 Hz wander) - **not a modem defect**. Resolved by GPSDO-disciplining the Flex reference (rig fix, no code change). On-air confirmation, 2026-07-27: WN6 Poor decodes coded-BER-0 once the Flex locked to the external 10 MHz reference (residual offset collapsed from ~−202 Hz drifting to +36 Hz static). Issue #102 closed 2026-07-28; confirmation evidence in `docs/ms110d/evidence/2026-07-27-ota-poor-validation/`. The demodulator is not hardened against a few-Hz reference wander, so a field station with a lesser reference inherits this condition (the open, non-urgent question #102 records).  **On-air over a real path 2026-08-03** (§E4, 7.1285 MHz → wessex.zapto.org, 48 W): acquired, WID matched, **BER 0.00E+000 bit-exact**, EOM. | ✅ working, conditional on a disciplined RX reference (see ledger) |
| `ms110d-wn7` | MS110D | 8PSK r3/4, U=256/K=32 | **Poor sim hard-gated since 2026-08-20 (G1d, `docs/ms110d/evidence/2026-08-20-poorgate-g1d/`): 0 / 3,243,776 canonical and 0 / 3,243,776 disjoint at full §5.3 budgets**, through the 8PSK per-block ensemble - the DFE-chain path beside the MFB-form decoder, each receiver's evidence weighed in log-likelihood units, the larger winning. Was measured-only at 2.56E-5 / 1.48E-5 from Phase B (PRs #60, #79-#95, #97; closeout PR #98), whose B3.9 verdict ("the waveform's own floor, needs added information") held for the DFE-chain class alone and for the MFB class alone (G1: 238 vs 131) and fell to their ensemble: the two are never wrong on the same block. AWGN 0 at full budget, unchanged | **On-air AWGN 2026-07-27** (full campaign): clean to +8.8 dB. Poor not run - mask +19 dB is above the rig's ~15-16 dB ceiling at 3.7 W  **On-air over a real path 2026-08-03** (§E4, 7.1285 MHz → wessex.zapto.org, 48 W): acquired, WID matched, **BER 0.00E+000 bit-exact**, EOM. | ⚠️ partial (AWGN on-air-proven; **Poor sim hard-gated** since 2026-08-20 by the 8PSK ensemble, G1d; no on-air Poor confirmation is possible at +19 dB over either rig, so the row stays partial per the successor plan §6) |
| `ms110d-wn8` | MS110D | 16QAM r3/4, U=256/K=32 | AWGN hard-gated (0 errors, held through the W5 receiver swap); **Poor sim hard-gated since 2026-08-20 (G2f, `docs/ms110d/evidence/2026-08-20-poorgate-g2/battery-g2f/`): 12 / 4,325,120 canonical and 18 / 4,325,120 disjoint (97.5 % bounds 4.85E-6 / 6.58E-6)**, through the MFB decoder's MMSE cold rung (G2d) and its reworked QAM16 schedule (G2f: soft floor, hard-phase cycle members only, hard-first after a cycle with common-model pricing, plateau handover). Was 2.90E-4 / 1.75E-2 at the WN8 program's exit (ii) (2026-07-31, from coin-flip) and 7.17E-6 / 1.78E-5 after G2d; the "three first-block failures" of the W6 record were three mid-burst blocks the old cold rung never descended from. The Phase B walls are measured down: the waveform is not the floor (MFB 0/0), the basin is crossed label-free | **On-air AWGN 2026-07-27** (full campaign): clean to the ~+13.5 dB the rig delivers at 3.7 W (the +16 dB mask point is above the path ceiling). Poor not run (mask +23 dB above the rig ceiling - any Poor result stays sim-only)  **On-air over a real path 2026-08-03** (§E4): acquires with a correct WID, then BER 4.91E−001 / `SignalLost` - 16QAM is above what a 48 W NVIS path delivered, the ceiling of that session's ladder. | ⚠️ partial (AWGN on-air-proven at reachable SNR; **Poor sim hard-gated** since 2026-08-20 (G2f: 12 / 18 per 4,325,120 bits, bounds 4.85E-6 / 6.58E-6); no on-air Poor confirmation is possible at +23 dB over either rig, so the row stays partial per the successor plan §6) |
| `ms110d-wn13` | MS110D | QPSK r9/16, U=256/K=32 | Sim: hard-gated, canonical **0**/3.24 M, disjoint **0**/6.49 M (9× margin) - PR #80/#81 (SISO soft-feedback turbo) | Same issue #102 mechanism and fix as WN6 (reference phase-noise, GPSDO rig fix). On-air confirmation, 2026-07-27: WN13 Poor decodes coded-BER-0 under the disciplined reference. Same evidence as WN6 (`docs/ms110d/evidence/2026-07-27-ota-poor-validation/`)  **On-air over a real path 2026-08-03** (§E4, 7.1285 MHz → wessex.zapto.org, 48 W): acquired, WID matched, **BER 0.00E+000 bit-exact**, EOM. | ✅ working, conditional on a disciplined RX reference (see ledger) |

Notes: DSP rate is 12000 Hz for the AFSK/BPSK/QPSK tone-pair/carrier families and 48000 Hz for FSK/C4FSK, FreeDV DATAC, and MS110D (`ModemCatalog.DspRateFor`); only `afsk*`/`bpsk*`/`qpsk*` accept a settable audio-centre frequency (`AcceptsCentreFrequency`, issue #39/PR #51).

## Validation ledger

Dated, append-only. Newest entries at the bottom.

- **2026-07-15/16 - PSK detector default.** Coherent (Costas) detection made the default PSK detector for all `bpsk*`/`qpsk*` modes (issue #5, PR #13), validated against the QtSoundModem interop matrix (PR #14, resolving issues #6/#10/#11 on evidence - #6 and #10 pinned as characterized regressions, #11 left open as a genuine marginal-interop gap).
- **2026-07-16 - C4FSK bench validation.** `C4fskModem` (c4fsk9600/c4fsk19200) landed and was bench-proven 8/8 us→NinoTNC on both modes at first live attempt - graduated straight from aspiration to `NinoTncParityTests.cs` coverage (recorded in `docs/plan.md` and `NinoTncAspirationTests.cs`).
- **2026-07-17/18 - real off-air BPSK300 default reversal.** Coherent detection could not decode a real GB7RDG NinoTNC BPSK300 IL2P+CRC capture (differentially-encoded coherent BPSK) - issues #40/#42. Fixed by reverting the default to differential detection plus a 4-pair frequency-diversity bank (PR #56, PR #52), with the capture itself checked in as `OffAirBpskTests.cs` (PR #41). Coherent remains an opt-in detector; QPSK keeps its coherent default.
- **2026-07-18 - settable centre frequency.** `afsk*`/`bpsk*`/`qpsk*` gained a per-mode audio-centre override (issue #39, PR #51), validated by `CentreFrequencyTests.cs`.
- **2026-07-18 - `ModemCatalog` created.** PR #58 established the single mode→`IModem` factory this report is keyed against; `ModemCatalogTests.cs` guards every arm against being added to `Create()` but forgotten in `KnownModes` (or vice versa).
- **2026-07-19/20 - MS110D App D Phase A lands.** PR #34: Walsh-75/BPSK/QPSK, autobaud, DFE, mask-gated at first light. PR #60 adds WN7 8PSK + WN8 16QAM.
- **2026-07-20→23 - MS110D Poor-channel equalization found broken, then fixed.** An adversarial review of the equalizer campaign found the 3-pass bidirectional equalization degrading Poor-channel BER (averaging equalized symbols across passes with different fading states introduces noise instead of diversity) - **issue #63**, plus three measurement-instrument defects (RX-ring wrap under turbo re-reading overwritten samples on Long blocks; a mask harness that could report vacuous per-point passes; CI executing zero tests since an xUnit v3 migration). All four fixed on the Phase A closeout branch - bidirectional equalization gated on `IsFlatChannel()` (flat channel keeps 3-pass bidirectional, fading channel uses single-pass RLS) - **PR #68**, closing issue #63 and re-measuring every Phase A mask from scratch on the repaired instruments (`docs/ms110d/phase-a-closeout.md`).
- **2026-07-24→26 - MS110D Phase B: WN0-WN6+WN13 brought to simulated at-mask.** Entry program issues #64 (equalizer heuristics fitted to the Poor rig), #65 (turbo robustness gaps), #67 (test-coverage gaps), #69 (the Phase B program issue). Landed as PRs #70-#95 and #97 (B0 instruments → B1 autopsies → B2 science core → B3 family closure → B4 gate flip); formal closeout **PR #98** (`docs/ms110d/phase-b-closeout.md`): 8 of 10 Poor points hard-gated (WN0-6, WN13), WN7/WN8 close as written, measured-only verdicts (see the WN7/WN8 matrix rows above for the current figures).
- **2026-07-27 - WN2 (BPSK r1/4, K=48) found broken over real RF, then fixed.** The first off-air lab campaign (Flex→125 dB attenuator→RSP1 rig, `docs/ms110d/evidence/2026-07-27-ota-lab-campaign/`) found WN2 Poor failing every burst (`SignalLost`) despite clean acquisition - **issue #101**. Root cause: the K=48 DFE's cold-restart least-squares solve, already running near-collapse in clean sim, over-regularises at the real rig's ~20 dB-below-nominal absolute receive level (no AGC upstream) and initialises dead. Fixed by a slow, global input-level AGC ahead of the DFE - a dead-zone no-op at nominal level, so byte-identical on every simulated mask (WN2 canonical 30/6.08 M = 4.93E-6, disjoint 29, both `agc 0`) - **PR #103**, merged to `main`. On-air confirmation run, 2026-07-27: WN2 Poor now decodes coded-BER-0 over the same rig, the AGC firing 19-32× on the low-level bursts that were previously all-`SignalLost`. (The confirmation figures are recorded here as reported this session; the campaign's own dated evidence write-up for the post-fix re-run had not been committed to the repository as of this report - follow up by adding it under `docs/ms110d/evidence/`.)
- **2026-07-27 - WN6/WN13 (QPSK r3/4, r9/16) found broken over real RF, resolved by a rig fix.** The same lab campaign found WN6 and WN13 Poor locking the correct waveform, then the DFE collapsing into the first fade and never recovering (`SignalLost`) - **issue #102**. A hardware-free discriminator (Wiener-phase and level-scaling injection into the clean sim capture, `docs/ms110d/evidence/2026-07-27-ota-lab-campaign/phase-noise-experiment.md`) isolated the cause to **receiver reference phase-noise**: a 1-3 Hz effective linewidth (WN13 holds to ~2.5 Hz, WN6 to ~1-2 Hz, tracking coding strength) reproduces the exact collapse signature, and sits squarely inside the RSP1 rig's measured 0-8 Hz reference wander. **No modem code change** - the indicated and applied fix is disciplining the Flex reference to an external GPSDO. On-air confirmation, 2026-07-27: both WN6 and WN13 Poor decode coded-BER-0 once the Flex locked to the external 10 MHz reference, the residual offset collapsing from ~−202 Hz drifting to +36 Hz static. (Issue #102 remained open on GitHub as of this report - the fix is operational/rig, not a merged code change, and the confirmation run's dated write-up had not been committed. Follow up: close #102 with a pointer to the confirmation evidence once written up, and consider whether the demodulator should be hardened against a few-Hz reference wander for field deployments with less capable references - noted as an open, non-urgent question in issue #102 itself.)
- **2026-07-27 - FreeDV DATAC OFDM simulation baseline.** First AWGN/Poor/Good BER-vs-SNR characterization of all six `freedv-datac*` modes against codec2's own published operating points, on a simulation harness generalized off the MS110D ladder (`sm-ota sim`). AWGN faithful to codec2 (datac3's 90 % knee −3.2 dB vs published −3 to −3.5 dB). Poor (MPP) reads 6-28 points below the published N/100, attributed to a mix of methodology (independent-burst vs continuous-stream) and channel-model differences, not a demodulator defect; level-invariant to ≥60 dB, ruling out a WN2-style front-end bug. **PR #104**, merged into branch `ms110d-ota-harness` - **not yet forwarded to `main`** as of this report, so the evidence directory (`docs/ms110d/evidence/2026-07-27-freedv-datac-sim-baseline/`) is not yet present there. Not yet attempted on-air.
- **2026-07-27 - MS110D full on-air campaign: every masked waveform meets its mask on the real rig.** An 18-run AWGN+Poor sweep of all ten MS110D waveforms over the Flex→RSP1 rig under the GPS-locked reference (`docs/ms110d/evidence/2026-07-27-110d-full-campaign/`). **AWGN** clean coded-BER-0 thresholds, monotonic in robustness: WN0 −3.5, WN1 −2.5, WN2 −1.6, WN3 −0.6, WN4 +1.5, WN5 +3.7, WN6 +5.7, WN7 +8.8, WN13 +2.6 dB; WN8 clean to the ~+13.5 dB the rig delivers at 3.7 W (its +16 dB mask point is above the path/artefact ceiling). **Poor** (D.6.1) clean thresholds at or below every defined mask: WN0 −0.5 (mask −1), WN2 −1.1 (+5), WN4 +4.0 (+10), WN5 +5.1 (+11), WN6 +9.5 (+14), WN13 +7.0 (+11); WN1/WN3 clean (no Poor mask defined). WN7/WN8 Poor (masks +19/+23) were not run - above the rig ceiling at this power, deferred to the real-antenna phase. This forwards **WN0/WN1/WN3/WN5** from not-yet-on-air to working, and adds AWGN+Poor confirmation for WN2/WN4/WN6/WN13. This directory (and the sibling `2026-07-27-ota-poor-validation/`) is also the dated on-air write-up the #101/#102 caveats above pointed at - those are now committed.
- **2026-07-28 - FreeDV DATAC OFDM: first on-air campaign, all six modes validated on hardware.** A 12-run AWGN+Poor sweep of all six `freedv-datac*` modes over the Flex→DIGU→RSP1 rig via the DAX route (`docs/ms110d/evidence/2026-07-28-freedv-ofdm-oncar/`; harness PR #108). All six acquire and decode over the air. **AWGN matches the #104 sim baseline**: datac1 +1.8 dB exact, datac3 −3.8 vs sim −3.2 (within 0.6 dB) - the clean on-air validation of the managed OFDM engine. Poor: datac3/datac1 within ~0.7 dB of sim; the very robust narrow modes (datac4/13/14) read below sim, reflecting the **known sim-Poor pessimism** (#104) plus the 3 kHz-SNR reference vs their 250-500 Hz occupied bandwidth (a −13 dB 3 kHz-ref threshold is ~−2 to −3 dB in-band), not the rig being kinder than a fading channel. DAX/ALC costs ~1.2 dB at the top rung at 0.5 drive. Forwards freedv-datac0/1/3/4/13/14 from not-yet-on-air to **working**. Follow-ups: re-anchor the sim Poor against codec2's own `ch` (makes the Poor cross-check as clean as AWGN), and forward the OFDM harness (#108) + sim baseline (#104) to `main`.
- **2026-07-28 - FM transmit/receive path built; FM-native modes validated offline through the whole FM chain.** The FM-native modes reach the air as frequency modulation (per `docs/mode-modulation-reference.md`, from the NinoTNC firmware modulator table), so they need an FM chain the SSB/OFDM ladders do not: a software FM modulator (`FmModulator` - audio → complex FM baseband at a **settable peak deviation**), an IQ→FM discriminator RX seam (`IqToFmAudioConverter`, the FM mirror of `IqToAudioConverter`: instantaneous-frequency/phase-derivative demodulation feeding the mode's own demodulator), a peak-deviation measurement tool (`sm-ota fm-deviation`, and the per-burst read-back in the scorer), an FM §E2 ladder (`sm-ota ladder --mode <fm-mode>`: render → sim-baseline channel → FM-mod at the mode's target deviation → FM-demod → decode + delivered-SNR tracking), and a live DAX FM transmit route (`FlexTransmitterOptions.SliceMode` = `FM`, drive calibratable to the target deviation). Injecting the ladder's self-calibrating noise as audio *before* the modulator is faithful to the on-air ladder (which transmits its noise lead-in through the radio's FM modulator too) and keeps the mod/demod round trip an SNR-preserving identity, so the scorer recovers the delivered SNR. **Offline `--dry-run` result (no radio):** all six FM modes decode their frame bit-exact through the software FM round trip at their target peak deviation - `afsk1200` 3.0, `fsk9600` 2.4, `fsk4800-il2p` 1.2, `c4fsk9600` 2.5, `c4fsk19200` 5.0, `qpsk3600` 5.0 kHz - with the measured SNR tracking the injected SNR to <0.4 dB across the ladder and the scorer catching the cliff (a −15 dB rung is lost). Covered by `FmBasebandTests` (deviation modulate→measure round trip, discriminator recovery) and `FmLadderPassTests` (the end-to-end dry-run, mirroring `OfdmLadderPassTests`). A supporting fix: `SimModem.RenderBurst` rendered at TXDELAY 0 (2 opening flags), which the fast classic-HDLC baseband detectors (`fsk9600`/`c4fsk9600` at 9600 Bd) cannot lock onto - it gained an optional TXDELAY parameter (default 0, so the datac/sim baseline is unchanged) and the FM ladder renders with a realistic 150 ms run-in. **Not yet on-air:** the live per-mode deviation calibration is a radio step (transmit, measure the achieved peak deviation with `sm-ota fm-deviation`, adjust `--audio-amplitude` until it equals the target) run by the on-air session; the matrix on-air column is unchanged pending it. Draft PR off `main` (this change). Note the standing warning from the reference doc: "wide/narrow" (25/12.5 kHz) is **channel spacing**, not deviation - the drive is calibrated to the Tgt Dev column, verified by measurement.
- **2026-07-28 - FreeDV DATAC OFDM Poor/MPP sim re-anchored against codec2's own `ch`; the #104 shortfall is methodology, not a defect.** The #104 sim Poor read 6-28 points below codec2's published MPP N/100, confounded between the channel model and the measurement method. This pass separated them (`docs/ms110d/evidence/2026-07-28-freedv-poor-reanchor/`): built codec2's `ch` + `freedv_data_raw_tx/rx` from source, generated a faithful MPP fading file (a spectral-synthesis port of codec2's `doppler_spread.m`, `gen_fading.c`, validated by the codec2 tx→ch→rx anchor reproducing published within ±9), and added a continuous-stream measurement to the harness (`sm-ota sim-stream`, `tools/Packet.SoundModem.Ota/SimStreamCommand.cs`; modem untouched). **Finding: the gap was overwhelmingly the independent-burst-vs-continuous-stream methodology, not the channel.** On the identical `WattersonChannel` Poor, switching to codec2's continuous-stream method recovers +2 to +22 points (largest for the shortest bursts - the long-PER-tail codec2 documents), and the re-anchored sim now lands on the published points: **datac0 71 vs 70, datac3 75 vs 74** (exact), datac1 88 vs 92, datac13 86 vs 90, datac14 85 vs 90 (within 4-5), datac4 82 vs 90 (within 8). The channel-model axis (our modem through codec2's real `ch --mpp`) confirms our Watterson Poor is faithful - comparable-to-milder than `ch` for five of six modes (so it is not the optimist; the wideband datac1 is the one where `ch` runs harsher). The surviving few-point residuals on the heavily-coded long modes (datac4, datac1) are a small managed-modem coded gap under fading - our modem trails codec2's own modem by ~4 points on the *same* `ch` channel - plus single-realisation tail noise, characterised not fixed. **This closes the #104 Finding-1 re-anchor follow-up.** Evidence dir + `analyze.py` decomposition + `waterfalls.svg`; **PR #115** (draft).
- **2026-07-28 - SSB audio-carrier coverage: bpsk1200 AWGN validated on-air; afsk300/qpsk600 sim-only (CFO wall, #116); D.6.1 Poor is a mode limit for all three.** The three NinoTNC-lineage audio-carrier modes taken through the harness (`docs/ms110d/evidence/2026-07-28-ssb-coverage/`). **Sim** (CFO-free `sm-ota sim`, 100 bursts/pt, frame layer): AWGN 90%-frame thresholds qpsk600 ≈ +3, bpsk1200 ≈ +4, afsk300 ≈ +8 dB; **D.6.1 Poor fails for all three** (<15 % success even at +15 dB) - narrowband, uncoded/lightly-coded carriers have no equaliser/interleaver for 2-path fading, so Poor is a *mode limitation* (and is exactly why bpsk1200 returned 0 Poor decodes on the air), not a rig or harness fault. **On-air** (Flex GPS-locked → 125 dB → RSP1, IQ route, 11.8 W): **bpsk1200 AWGN validates** - 11/18 decode, clean to delivered ≈ +2 dB (≈ +3 corrected for the 2.3 dB self-cal offset), matching sim; acquired-burst CFO −120…+60 Hz shows it rides the RSP1 drift. This forwards **bpsk1200 / bpsk1200-multi** from not-yet-on-air to **working (AWGN)**. **afsk300 and qpsk600 do NOT acquire on-air** (0/12 each) - their acquisition has no carrier-offset search, and the RSP1's undisciplined LO drifted +35 → ~−90 Hz over the session; the sim dry-run reproduces it exactly (bpsk1200 tolerant to ±120 Hz injected CFO; afsk300 decodes only the burst at CFO ≈ 0). Per operator (2026-07-28): characterise the two narrow modes in **sim only** and defer their on-air validation to a disciplined receiver - **issue #116** (which also tracks a first-burst −250 dB self-cal artifact and bpsk1200's ~2.3 dB delivered-vs-asked offset). FM modes should be CFO-immune (the discriminator ignores carrier offset) - relevant to the #114 FM leg.
- **2026-07-28 - FM-native coverage: modem sim-validated; on-air deferred to FM-capable hardware (Flex TX wall, #118).** The six FM-native modes through the harness (`docs/ms110d/evidence/2026-07-28-fm-coverage/`). **The FM modem is sound** - all six decode through the software FM round trip (mod→discriminate→demod; #114 unit tests prove it bit-exact), and several are already ✅ in the matrix via bench NinoTNC / WA8LMF corpus (afsk1200, c4fsk9600, c4fsk19200, fsk4800-il2p). **Sim AWGN thresholds**: `sm-ota sim` (CFO-free baseband) afsk1200 ~+8, fsk4800-il2p ~+11, qpsk3600 ~+14 dB; the fast HDLC FSK modes (fsk9600, c4fsk9600, c4fsk19200) can't run through `sm-ota sim` (TXDELAY-0 defeats their detectors) and through the faithful FM round-trip ladder sit high - fsk9600 ~+15-18, c4fsk9600 ~+24-30, c4fsk19200 ~+30 dB - the **FM discriminator noise triangle** (wideband FM trades bandwidth for a threshold penalty, physically expected). **D.6.1 Poor fails for all six** (mode limitation, no fading equaliser). **On-air over the Flex→RSP1 rig is blocked by a transmitter hardware wall, not the modem**: the Flex 6500 waveform IQ path is single-sideband ≤10 kHz one-sided (too narrow for the two-sided FM Carson bandwidths - only `fsk4800-il2p` at ~7 kHz clearly fits), and the DAX→NFM route corrupts the data with voice DSP (deviation limiter + pre-emphasis + audio filter). FM is CFO-immune, so the RSP1 drift (#116) is not the blocker here - the transmitter is. Per operator (2026-07-28): sim-validate + defer on-air to FM-capable hardware - **issue #118**.
- **2026-07-28 - bpsk300 (PSK300, the primary NinoTNC live mode): on-air AWGN waterfall added; Poor a mode limit.** bpsk300 through the OTA harness for a proper BER-vs-SNR curve (`docs/ms110d/evidence/2026-07-28-bpsk300-coverage/`) - it already had a real GB7RDG off-air *decode* (#40/#42) but no rig characterisation. **On-air** (Flex GPS-locked → RSP1, IQ route, 11.8 W, dial 0; the RSP1 offset had thermally settled back to ~0): its differential detector + 4-pair frequency-diversity bank rode the residual cleanly (pilot 6/6, self-cal 0.10 dB). **AWGN threshold ≈ −3 to −4 dB** (3 kHz-ref) - clean decode to delivered ~−1.3 dB, a robust negative-SNR mode (IL2P Reed-Solomon FEC + narrow ~400 Hz 300 Bd occupancy). **D.6.1 Poor mostly fails** (2/36, only at delivered ~+7 dB): the diversity bank rides carrier offset, not multipath, and there is no equaliser for the 2 ms fade - a mode limitation like the other SSB audio modes. **Sim confirms both** (`sm-ota sim`: AWGN 88 % @ −3 dB, Poor 11 % best @ +3). bpsk300's status is now a full on-air AWGN waterfall on top of the GB7RDG decode.
- **2026-07-28 - qpsk2400 (2400 QPSK / DW2400, a primary NinoTNC live mode) folded into the CFO wall (#116).** On the Flex→RSP1 rig (offset settled to ~−10 Hz via a bpsk300 probe) qpsk2400 **acquired 0/4 at dial 0 and 0/6 at dial −10** - nulling the offset did not rescue it. The dry-run decodes it **only at CFO exactly 0** (CFO-fragile like afsk300/qpsk600 but tighter; as coherent QPSK also exposed to the RSP1's phase-noise wander - the RX-side of the closed #102). Sim baseline (`sm-ota sim`, CFO-free): AWGN ~+10 dB 90 %-frame (56 % @ +9), Poor fails. **Of the four primary NinoTNC live modes (300 BPSK / 2400 QPSK / 3600 QPSK / 9600 GFSK), only 300 BPSK (`bpsk300`) validates on this rig** - 2400 QPSK is #116; 3600 QPSK + 9600 GFSK are the Flex-FM-TX wall (#118). On-air deferred to the disciplined-UberSDR campaign; making these modes work on *undisciplined* radios is a further modem-side enhancement (carrier-offset tracking / AFC). Sim CSV in `docs/ms110d/evidence/2026-07-28-ssb-coverage/data/`.
- **2026-07-31 - WN8 (16QAM) Poor: coin-flip → decoding; the MFB-form receiver ships.** The WN8 redesign program (`docs/ms110d/wn8-program-plan.md`, issue #130) ran its measurement ladder and build in one day: W1b's matched-filter bound decoded every block of both specimens to zero on the exact channel (the waveform was never the floor - the Phase B walls belonged to the receiver architecture), W3 measured label-free probe-anchor trajectory estimation 11 dB inside the receiver's requirement (killing B3.4's "only labels provide" verdict), and W5 shipped `Ms110dMfbBlockDecoder` for QAM16 blocks: per-burst delay-profile window placement (acquisition can centre on the echo - the §B3.5b phenomenon at WN8), label-free composite-FIR probe anchors, matched projection, and a SISO-soft/hard cancellation schedule with convergence-gated decision-directed anchor re-fit, terminating on exact fixed points, detected period-2 cycles (label-free residual selection), or honest revert. Corpses: 269,237 → **112** canonical, 269,154 → **32** disjoint. Battery on the final binaries: AWGN WN8 **0 errors** at full budget; **Poor 2.90E-4 canonical / 2.27E-2 disjoint** at full §5.3 budgets (the disjoint tail = 4 single-block non-convergences across 8 bursts); **all 108 non-WN8 censuses byte-identical** to the W0 baseline, guard pins exact, hermetic 790/0; off-rig direction improves on both alternate geometries (slow-fade estimation recorded as the measured weak edge). Still measured-only vs the 1E-5 mask, and sim-only by rig physics - W6 takes the gate/verdict decision. Evidence `docs/ms110d/evidence/2026-07-31-wn8-w5a/-w5b1/-w5b2/`.
- **2026-07-31 - WN8 redesign program CLOSED (exit ii): the final measured state.** The W6 decision leg (`docs/ms110d/evidence/2026-07-31-wn8-w6/`) added a schedule-diversity fallback (hard-first rerun on revert - recovered one of the four disjoint block failures) and took the decision battery: **Poor WN8 2.90E-4 canonical / 1.75E-2 disjoint** at full §5.3 budgets, AWGN 0, all 108 non-WN8 censuses byte-identical. Not §5.3-green at the 1E-5 mask on both families, so the program's pre-committed rule closes it as **exit (ii) - a dramatically improved, honestly-recorded measured-only ceiling** (from coin-flip both families at entry; sim-only by rig physics - the +23 dB mask region is above the OTA rig ceiling). Phase B's B3.10/B3.4 banked negatives carry their earned amendments in the program record; exit (iii) is permanently refuted (the W1b matched-filter bound's 0/0 stands as the achievability witness). Successor levers with instruments waiting: the three disjoint bursts whose first blocks defeat both schedules, the slow-fade weak edge, schedule diversity beyond two, the unused 12PSK-ring moment observables. Umbrella issue #130 closed with this verdict; `docs/ms110d/wn8-program-plan.md` is now historical.
- **2026-07-31 - NinoTNC wiki corpus captured on the studybox bench; two fresh interop findings.** All 15 DIP modes captured at three TXDELAY tiers (45 WAVs, `tf@studybox:~/nino-corpus/`, settings + provenance in its README and `docs/bench/ninotnc-corpus-2026-07-31.md`), every file energy-verified complete and QC-decoded through the paired catalog mode (`NinoCorpusQcTests`): **34/45 full-copy**. The exceptions are ours, on good captures: **`qpsk600` copies 0-1/3 real NinoTNC transmissions even in a clean audio loop** - fresh, hard #11 evidence consistent with its measured ±5 Hz CFO brick wall (the #116 characterisation); **`qpsk3600` copies only 1-2/3** where the July bench scored 12/12 (issue filed - regression or level/firmware sensitivity); `c4fsk9600` confirms its ≥~250 ms TXDELAY acquisition floor; single-frame misses on `afsk300`@240 ms, `afsk1200-il2p`@120 ms, `bpsk1200`@500 ms. Also banked: three N9600A4 v3.44 behaviours (stale-TXDELAY first frame; fire-and-forget SETHW needing GETALL verification; a ~253 B AX.25 frame never transmitting in mode 1100) - details in the corpus doc.
- **2026-07-31 - QPSK detector default reversed to differential; qpsk600/qpsk3600 now copy the full NinoTNC corpus.** The corpus-decode goal's diagnosis chain (self-loopback 10/10 at every frame length; ±400 ppm resample scan ruling out constant clock offset; level/spectral/burst forensics exonerating the recordings) localised the QPSK failures to the coherent Costas default's fragility on real multi-second NinoTNC bursts - the same defect class the 2026-07-18 BPSK reversal fixed (#40/#42), compounded by the measured CFO walls (±5 Hz qpsk600, ±8 Hz qpsk2400 - the #116 CFO-0 characterisation). V.26A is differentially encoded by construction; under `PskDetector.Differential` **all nine QPSK corpus files decode 3/3** (previously 0-2/3 for qpsk600/qpsk3600), the corpus total moves 34/45 → **40/45** (the remainder are non-QPSK stragglers, each with a named symptom), and the sim CFO half-widths widen ±5→±10-40 (qpsk600), ±8→~±60 (qpsk2400), ~±60 (qpsk3600) for 0.4-3.5 dB at the AWGN 90% knee - the same robustness-over-knee trade as the BPSK reversal. `ModemCatalog.DefaultDetectorFor` now returns differential for every PSK family (coherent stays selectable via `ModemOptions`); hermetic 790/0 including the QtSM interop fixtures under the new default. Closes **#144** (the "regression" was coherent-detector fragility, moot under the new default); addresses **#11**'s mechanism pending a live QtSM retest; narrows **#116** to the remaining bank/offset-search work (qpsk600 to ±100-class). Evidence `docs/cfo/evidence/2026-07-31-cfo-1-qpsk-differential/`.
- **2026-07-31 - corpus-decode goal closed: 43/45, every miss attributed.** After the QPSK reversal (34→40), the remaining stragglers fell to forensics: `bpsk1200`@500/`afsk1200-il2p`/`afsk300`@240 failures were **capture-chain glitches** - isolated single-sample discontinuities (3.6-7× the band-limited slope, ~1 per 2.5-9 s, sample-localised inside exactly the failing bursts) - cured by recapture, with the capture driver now self-healing (band-limit glitch scan + retry); inter-burst demod state was exonerated by fresh-modem-per-burst splitting. `c4fsk9600`'s two cells are the final carve-out: clean recordings, with the mode's acquisition band characterised (50 ms below the hard floor 0/3, 120 ms marginal 2/3 repeatable, ≥250 ms reliable) - a documented detector limitation, future work. Corpus final on studybox with provenance; QC harness (`NinoCorpusQcTests`) gains split-burst and detector/report knobs.

- **2026-07-31 - c4fsk real-capture decode broken→working: corpus 43/45 → 45/45; the "acquisition floor" was a misdiagnosis.** The two `c4fsk9600` carve-outs (txd50 0/3, txd120 2/3) were **not** an acquisition floor and not recording defects: an instrumented replica of the demodulator, calibrated to zero errors on our own TX and on the txd250 capture, showed **every symbol error is an outer→inner demotion at 0.53-0.67 normalised** - pattern-dependent ISI in real NinoTNC transmissions squeezes outer symbols under the ⅔ slicing boundary, and the three TXDELAY tiers transmit *different payload text* (the tier tag is embedded in each frame), so the failure tracked content, not preamble length (20 ms of trimmed txd250 preamble decoded; 150 ms of grafted known-good preamble did not rescue txd50). Fix: a 5-tap symbol-spaced decision-directed NLMS equalizer at `C4fskModem`'s decision instants (identity start, silence-gated by the existing energy gate, reset with the deframer) - nine corpus bursts drop 103→25 symbol errors, all within RS budget, **corpus 45/45**, full suite green (790/790), hermetic ISI regression in `C4fskEqualizerTests` (both C4FSK modes fail the closed-eye channel without the equalizer, pass with it). Along the way the same investigation caught a separate TX-side bug in `M0LTE.Il2p` 0.1.0 - the IL2P+CRC trailer is computed over the original frame while Type 1 header translation is lossy (single C bit, PID canonicalisation), so degenerate-C frames (both C bits clear, the NinoTNC-observed wire style) always fail CRC at any conforming receiver and our whole c4fsk TX self-loopback scored 0/N. Package fix (Type 0 fallback when the header doesn't round-trip byte-exactly under CRC) published as 0.1.1 and bumped here, with a both-C-bits-clear loopback regression (`Il2pCrcDegenerateHeaderTests`) across three il2pc framings. One attribution made during the investigation is hereby **retracted**: the codec bug does *not* explain the "`sm-ota sim` render invalid for c4fsk" note - re-verified after the fix, `sm-ota sim` still scores 0/N for c4fsk even at 60 dB SNR with a 250 ms run-in (see the 2026-08-01 dig-in below for the real mechanism, which turned out to be a sim-harness artifact, not a structural limitation).

- **2026-08-01 - sim c4fsk 0/N dug in: a harness artifact plus a real equalizer weakness; c4fsk is now sim-characterisable.** The "structural" claim above is in turn **retracted** - two stacked causes: **(1) Sim-harness artifact:** `SimChannel`'s 0.15 s noise lead-in sat *inside* `EnergyBusyDetector`'s noise-floor seeding window (8 × 20 ms = 160 ms, seeded from the LOUDEST warm-up block), so the 8th seed block already contained burst audio, the floor seeded at ~signal level, and the burst never rose the 6 dB needed to assert - the energy-gated C4FSK modes discarded every sample at any SNR, while ungated fsk9600 sailed through and the FM ladder never hit it (6 s lead-in). Lead-in default now 0.5 s; ungated modes and datac re-verified unchanged. The 2026-07-28 "can't run through `sm-ota sim`" carve-out for c4fsk is obsolete. **(2) Real equalizer weakness, exposed the moment the gate opened:** the 2026-07-31 decision-directed equalizer random-walks along a null space while training on our own 0x77 preamble (±outer alternation: any taps with w[c]−w[c−1]−w[c+1]=1 fit it perfectly), so noise drove tap drift that *grew with TXDELAY* - an inverted preamble trend (25 dB AWGN, c4fsk9600: txd20 12/12 → txd250 4/12; clean at 60 dB where nothing drives the walk). Fix: freeze adaptation while the decision stream is an outer alternation, with hysteresis both ways (8 conforming freeze / 4 non-conforming unfreeze - a single-run gate leaked at 19200's noisier 5-sample/symbol decisions); a NinoTNC's 2-up-2-down preamble never matches the test, and the bench corpus stays **45/45**. An identity-leak variant was tried and **rejected on measurement**: its few-per-cent tap bias cost the most marginal real capture (corpus txd50 burst 1 sits at exactly its RS budget - 44/45 with leak). The hermetic canary recalibrated 0.62×→0.63× (preamble pre-convergence was what bought the last hundredth). **Known residual, filed as future work:** within ~5 dB of the modes' noise floors, long preambles still degrade decode *even with adaptation disabled* - not the equalizer; suspected envelope-tracker upward ratchet on noise spikes (attack 0.08 vs decay 1e-5). Post-freeze at 25 dB SNR3k: c4fsk9600 txd250 10/12 (mild), c4fsk19200 txd250 1-3/12 (its 14.4 kHz width makes 25 dB SNR3k ≈ 18 dB in-band - deep-threshold territory). Clean at 30 dB+ at every TXDELAY, both modes.

- **2026-08-01 - backtracking deframer shipped (M0LTE.Il2p 0.1.2); c4fsk energy-gate removal attempted, measured, and retreated.** Toward closing the C4FSK acquisition-floor gap vs the NinoTNC (our 10-20 ms is the energy gate's assert latency; theirs is one 16-bit word), the deframer was made **backtracking**: every physical bit lands in a 16 KB ring, and any failed collection (header RS, body RS, CRC) rewinds the read cursor to one bit past the failed sync and re-hunts - bit-for-bit equivalent to never having false-locked, so a real frame swallowed by a garbage collection is always recovered (a 23-bit hunt warm-up after register clears guarantees termination). This removes the *deafness* rationale for gating bit streams - RS+CRC already guaranteed no false frame could emerge - and hardens **every** IL2P mode against idle-channel false-locks; shipped as 0.1.2 with recovery tests (frame inside a failed garbage header; frame inside a header-passing bogus body; frame after 100k bits of 75%-ones garbage) and bumped here with a modem-level idle-robustness test. **The c4fsk bit-path ungating itself is NOT landed**: four equalizer-lifecycle designs (rising/falling edge tap resets, a frozen-state "in preamble" oracle guard, input-power and decision-error adaptation sanity gates) each fixed one regime and measurably broke another - hermetic ISI bursts, txd0 short-preamble parity, or (worst) real-capture decode, where seconds of pre-burst idle-noise adaptation wrecked the corpus to 39/45 while every hermetic test stayed green. The coupling is real: the old gate was silently doing double duty as the equalizer's per-burst lifecycle authority. Retreated to the proven gated receiver (suite 796/796, corpus 45/45, sim unchanged); ungating is follow-up work whose registration should treat the equalizer lifecycle as the design problem, with the corpus + preamble-floor ladder + hermetic ISI + txd0 parity + after-noise cold start as the mandatory five-regime battery. The measured floors (2026-08-01 bench table) stand: c4fsk9600 10 ms, c4fsk19200 20 ms behind real NinoTNC preambles.

- **2026-08-02 - ARDOP at a shifted centre: a real pdn↔pdn ARQ session, and the transmit inhibit under one.** `ardop` is a daemon modem entry rather than a `ModemCatalog` mode, so it has no row in the matrix above, but the standing rule applies: `becbdde` (#167) shipped ARDOP sharing a passband with the packet modems at a configurable centre, and both of the things that made that new were until now tested only in pieces - the frequency shift on a two-tone signal (`ArdopChannelShiftTests`) and `TransmitInhibit` against a synthetic predicate (`TransmitInhibitTests`). `ArdopSharedChannelSessionTests` closes the gap at the session level: two pdn stations wired exactly as `Daemon/Program.cs` wires one (TNC transmitter on the channel's inhibit-bypassing path, receive off a channel tap, `ArdopChannelShift` either side, `TransmitInhibit` pointed at the live engine), an afsk1200 modem sharing each channel, and a sound card substituted by real-time 20 ms block pacing. **At 950 Hz - the centre from #167's worked config - a session connects at 500 Hz, carries a payload byte-exact in both directions (including the BREAK turnover that makes the listener the sending station) and disconnects in order; a packet frame offered mid-session waits the session out and is decoded at the far station afterwards.** The same test at ARDOP's native 1500 Hz is the control. pdn↔pdn is the only possible test at a shifted centre: ardopcf cannot move off 1500 Hz, so it cannot be the far end by construction - `ArdopHostLiveTests` remains the interop ground truth, at 1500 Hz only. **Not proven: any of this over real RF** (no on-air ARDOP session at any centre, shifted or not), and no interop claim for a shifted centre beyond pdn↔pdn. Two things the bench measured on the way, neither asserted on: the first cut fed audio only during bursts and the packet modem's sample-clocked `EnergyBusyDetector` latched forever, which is *why* the bench now feeds continuous audio; and ARDOP's bursts, which bypass `TransmitInhibit`, still take the channel's p-persistent CSMA roll and so wait on the packet modems' busy state - at a shifted centre ARDOP's own signal is inside the packet modem's passband, costing a measured ~20 % of session time at the shipped p=63. The bench is load-sensitive (one run in seven stalled a transfer past its wait on a contended box); waits are patient rather than assertions loose.

- **2026-08-02 - `afsk300-il2pc` decoded off air for the first time, through a public web receiver; `bpsk300` confirmed on the same path.** The daemon gained `--device ubersdr:<instance>`, which streams a public [UberSDR](https://github.com/madpsy/ka9q_ubersdr) receiver's **iq48** (48 kHz complex baseband, ±24 kHz) and demodulates SSB from it in-process at the channel's DSP rate, so an ordinary band-plan config runs unchanged on somebody else's antenna. Run against `m9psy-1.instance.ubersdr.org` (RX888 + 40 m full-wave loop, GPSDO-disciplined, Dalgety Bay) on the three-modem 40 m plan `afsk300-il2pc` 7.050300 / `ardop` 7.050950 / `bpsk300` 7.051600 - dial computed at 7.049450 USB - it decoded **8 CRC-valid frames from 4 distinct stations in the first four minutes**: `GB7BEX-15`→`GB7IOW-1` on AFSK300 IL2P+CRC, and `GB7OXF-2`→`GB7RDG-2` (×5), `GB7WEM` and `GB7BWR` on BPSK300 IL2P+CRC, 0 RS corrections throughout. (The "−15 to −30 Hz carrier offset" originally reported for the BPSK frames was the winning branch's comb position, not a measurement - see the 2026-08-04 entry; the decodes themselves stand.) **`afsk300-il2pc` had no on-air validation at all before this** - it moves 🚧 not-yet-on-air → ✅ working, and it did so on a mode whose bench status was already corpus-proven both directions against a NinoTNC, so the new information is purely the RF leg. `bpsk300` was already ✅ working off the GB7RDG capture and the Flex→RSP1 BER waterfall; this adds a second, independent receive chain (someone else's front end, our SSB demodulator) reaching the same verdict, and incidentally exercises the frequency-diversity bank on real off-frequency carriers.
  What this does *not* claim: the front end is not ours, so nothing here characterises absolute sensitivity, and the station cannot transmit, so no interop direction is proven that was not already. Level measured off that instance is ≈ −26 dBFS RMS demodulated. Deterministic DSP correctness (sideband selection, absolute frequency placement, decimation-by-1, LSB) is pinned by `SsbDemodulatorTests` (in `M0LTE.Dsp`, where those demodulators live) and does not depend on a band being open; the live path itself has a gated end-to-end test (`UberSdrLiveStreamTests`, `UBERSDR_LIVE=<instance>`). Shipped in **0.10.0** (0.9.0 was taken by an unrelated release the same afternoon); design notes in [plan.md](plan.md) § 2026-08-02 and [CONFIG.md § Listening to a web receiver](../CONFIG.md#listening-to-a-web-receiver).

- **2026-08-02 (later) - afsk300-il2pc broken→working *at rate*: the receive filter was swallowing the neighbours; the narrow-branch diversity bank takes the live slot from 3/13 to 13/13.** Tom pointed the session at the live 7.0503 slot (m9psy-1 `ubersdr:` path) with the observation that this mode has never decoded well there. The investigation ran on captured IQ (corpus/ubersdr/, decoded offline through the daemon's exact receive chain with an instrumented branch matrix) and closed with a causal proof, not a correlation: static CFO was exonerated (±60-70 Hz single-demod tolerance measured on own TX; real stations at −3/+35/−35 Hz), and the mechanism is **discriminator capture** - the ±400 Hz receive filters shipped since July reach ~200 Hz past the slot each way, and the slot's everyday neighbour (an SSB QSO parked at ~7.0500) lands inside them at comparable power. Injecting a clean burst into the *recorded* interference bed: the old filters need the packet **+6 dB above** the neighbour; the new `Afsk300MultiModem` bank (±250 Hz branches stepped 35 Hz across ±175 Hz, now the catalog default for all three afsk300 framings) decodes 8/8 down to **−9 dB below** it - the off-centre narrow branches also act as interference-dodging filters, so diversity buys both offset coverage and QRM rejection. Scored on the first 30 minutes of captured traffic (13 unique CRC-valid frames: M0LTE→GB7IOW SABMs, GB7BEX-15→GB7IOW-1 retries, a GB7NOT beacon from Nottingham at −35 Hz): **old single 3/13 → bank 13/13** (narrowed single alone: 7/13). Fixture `samples/offair/m9psy-40m-afsk300-il2pc-qrm.wav` + `OffAirAfsk300Tests` guard the exact failure mode; `Afsk300MultiModemTests` guard the bank. Landed on the `afsk300-multi` branch (see the PR that carries this entry); released as 0.14.0. **Continuing:** a two-vantage overnight capture (m9psy-1 + wessex.zapto.org, auto-analysed per segment) is accumulating a wider corpus - bank-vs-single across more stations and conditions lands as an addendum here when it completes.
- **2026-08-02 - NinoTNC ID beacons caught on the PSK SSB channels (`afsk300`, in a new role).** A NinoTNC cannot identify itself inside 300 BPSK, 600 QPSK, 1200 BPSK or 2400 QPSK, so per the [operator's manual](https://tarpn.net/t/nino-tnc/n9600a/n9600a_operation.html) it idents *alongside* them - "a beacon in 300 AFSK AX.25 (1600/1800 Hz tones)", host callsign → `IDENT`, default 9.5 minutes while transmitting - and sends none at all in the three self-identifying modes (300 AFSK AX.25, 300 AFSK IL2Pc, 1200 AFSK AX.25). For everyone else on the channel that is a recurring burst they can see and cannot read: their data modem is a PSK demodulator and the ident is FSK. `IdBeaconGhost` closes it - a receive-only `afsk300` receiver attached per PSK SSB modem, placed at **+200 Hz relative to the modem it accompanies** (Nino's PSK carrier is 1500, the beacon centre 1700), so a retuned modem carries its ghost with it rather than the ghost being pinned to an absolute 1700 Hz that is only correct when both dials agree. It is built through the catalogue's `afsk300` entry rather than by naming a class, so it is whatever that mode currently means - since #191, the narrow-branch diversity bank, which matters more here than on a data slot: a ghost sits beside a PSK carrier by construction and a quadrature discriminator follows the strongest thing in its passband, while the bank's ±175 Hz of coverage is the right shape for the only error this placement has (a dial differing from the transmitting station's - the carrier-to-ident offset is fixed inside one TNC). It takes no KISS sub-channel, does not contribute to carrier sense, and never transmits. Idents are tagged onto their burst as `KK4HEJ · ID`, listed in the decode panel with an **ID** badge, and written to the frame log as `ID beacon (AFSK300)`; what a ghost does not get is a *band* of its own, having no slot to shade - its tag lands against the band of the modem it rides on, which is where the ident sits. No SNR figure - the per-burst SNR comes from a band tracker and the trackers are keyed by modem - but a real frequency offset, measured off the identifying station's carrier (see the following entry, which had to fix that measurement first). `"idBeacons": false` disables it. **Validation is file/simulation only**: `IdBeaconGhostTests` covers the mode rule, the offset arithmetic (including a drift guard pinning `ModemCatalog`'s 1500 Hz PSK default to the offset that assumes it), decode at the default centre, at a retuned one, and across ±140 Hz of dial error, and that the base PSK modem delivers nothing from the same audio; end-to-end through the real daemon wiring, an `sm-samples`-generated `KK4HEJ`→`IDENT` afsk300 file replayed under `--wav-loop` against a `bpsk300` modem logged 17 idents at 1700 Hz with the PSK modem silent throughout. **No real off-air NinoTNC ident has been copied yet** - that is the outstanding leg, and it needs a live PSK SSB channel with a NinoTNC on it rather than any new modem work. This does not change `afsk300`'s own matrix status (still on-air-blocked by #116 in its data role); it adds a second job for an already sim-characterised mode. The **FM half of the same manual rule - where a NinoTNC idents in 1200 AFSK AX.25 on every FM mode except 1200 AFSK AX.25 itself - is not implemented**; it is the obvious follow-up and the same mechanism, differing only in the ghost's mode and its fixed (baseband modes have no settable centre) placement.
- **2026-08-02 (later still) - the 300 AFSK bank's reported frequency offset was a branch index, not a measurement; it is now a measurement, to ~2 Hz.** Building the ID-beacon ghosts on `afsk300` put the bank's `FrequencyOffsetHz` onto the waterfall for a second kind of frame, and probing it across the bank showed it was not a frequency at all. `Afsk300MultiModem` emitted the *first* branch to complete a frame and feeds branches in ascending order, so what reached the display was the lowest-centred branch that could still copy the signal - measured: true 0 → **−105**, true −140 and −70 → **−175** (clamped at the bottom branch), true +140 → +35, and the value moved with buffer framing (the same on-frequency signal read −35 through the daemon's device-sized blocks and −105 through one large buffer). Fine for deciding *whether* to emit, since the deduper is content-based and the bytes are identical whichever branch wins; wrong as the number an operator tunes by. #191's own test recorded the weakness honestly ("a lower bound on the true offset, not a measurement") rather than overclaiming - this replaces the weak contract with a real one.
  **The measurement was already being computed and thrown away.** The slicer's threshold is the midpoint of the mark and space envelopes, which is the discriminator's DC level, which is the carrier offset: summing the two tones cancels the ±shift and leaves `sin(4π·offset/rate)·cos(4π·shift/rate)`. `AfskDemodulator.CarrierOffsetHz` inverts that - no new filter, no per-sample cost, and derived from the same envelopes the bit decisions come from, so a frame that decoded has a settled reading by construction. Min/max envelopes rather than a mean is what makes it survive an HDLC preamble's 87.5 % mark duty cycle. Two things fell out of the probing: the chain mixes I from the sine and Q from the cosine (the transpose of the usual convention), so **the discriminator runs downward as frequency runs up** and the reading needed negating - never noticed before because the sign only picks which tone is mark, and mark assignment is free here (NRZI is polarity-agnostic; both IL2P receivers hunt sync in both polarities). And the reading compresses beyond ~±40 Hz where the discriminator clamp truncates the outer peak (±53 reads ±45, ±70 reads ±53), staying monotonic, which is all the branch ranking needs.
  `Afsk300MultiModem` now holds every branch's copy to the end of its chunk and emits the one with the smallest measured residual - the best-matched branch, which with 35 Hz spacing is never more than half a step out and so always reads in the honest zone; `branch + residual` is the answer. Cost: a frame waits up to 100 ms (one dedupe chunk) against frames that take seconds at 300 baud. **Measured after: −160 → −158.0, −140 → −138.5, −105 → −103.4, −70 → −68.5, −35 → −33.4, −17 → −15.9, 0 → +1.5, +17 → +18.0, +35 → +36.6, +70 → +71.5, +105 → +106.5, +140 → +141.5, +160 → +161.1** - worst error ~2 Hz across ±160 Hz, against up to 105 Hz before, and correct even at the two half-step points where no branch is "the" branch. A residual ~1.5 Hz bias is left uncharacterised; it is 0.5 % of the baud rate and below what any tuning decision turns on. `Afsk300MultiModemTests.The_Reported_Offset_Is_A_Measurement_Of_The_Signal` pins all thirteen points (a return to naming branches would show up as a ~35 Hz step); a lone `Afsk300Modem`, which reported no offset at all before, now reports one too. Decode counts are untouched - the off-air QRM fixture and every other afsk300/afsk1200 test passed unchanged through the change. **Not applied to `Afsk1200MultiModem`**, which has the identical "first branch wins" shape and would take the same fix: out of scope for this leg, and afsk1200 carries the WA8LMF corpus baseline, so it wants its own before/after rather than a drive-by.
- **2026-08-03 - the overnight two-vantage validation: the bank holds at 2× (38/45 vs 19/45), and the slot turns out to speak three framings.** The addendum the 2026-08-02 entry promised. Overnight capture ran from `wessex.zapto.org` (southern England; 17 clean 30-min segments) alongside the original `m9psy-1` evening material - every segment auto-decoded through the daemon's exact receive path by an instrumented 33-branch matrix including the released classes verbatim. Across **45 unique IL2P-family frames from 7 stations** (GB7BEX-15, M0LTE, GB7NOT, GB7RDG, GB7BWR-2, IW2OHX-15, PD4R-11): **old ±400 single 19, released ±300 single 24, released `Afsk300MultiModem` bank 38 (84 %)** - and the shipped bank matched the harness prototype frame-for-frame (zero discrepancies). Winning branches ranged −140…+105 Hz; the IW2OHX-15 (Milan) and GB7NOT (Nottingham) decodes exist *only* because of off-centre branches. Three findings banked alongside the score. **(1) The slot is a three-framing neighbourhood**: UT1HZM (Ukraine) beaconed 22 frames of *classic AX.25* (NinoTNC mode 12) that only the diagnostic HDLC branch could hear; GB7BWR-2 (2/2) and PD4R-11 (1/1) decode rs=0 on *plain IL2P* and always fail the CRC check - the signature of mode-13 (no-trailer) transmissions read by a mode-14 receiver, which also retracts the previous evening's suspicion of a GB7BWR CRC-trailer interop bug; and IW2OHX beacons in two framings by turn. A single-framing modem structurally cannot hear two of the three populations; the operator fix is three modem entries on the same slot frequency (`afsk300-il2pc` + `afsk300-il2p` + `afsk300`), which the daemon's sub-channel model already supports. **(2) Banked negative - the DCD-falling-edge deframer reset**: across the corpus the instant reset cost the *old wide* single 4 frames (a no-reset variant decoded all four; reset saved none anywhere), but the shipped narrow single and bank showed no measurable reset cost, and a 16-bit-hysteresis prototype recovered none of the four even at wide filters - so no code change ships, the #61 rationale stands, and any future reset-policy work starts from this measurement (evidence: `corpus/ubersdr/analysis/`, harness in the session scratchpad, summarised here per the banked-negative discipline). **(3)** `m9psy-1`'s per-address listening quota is a rolling ~24 h window, not a midnight reset - its side of the capture stayed quota-blocked all night while Wessex carried the corpus (the capture loop's own backoff made 55 polite 10-minute probes over the night; 0.14.1's equivalent in-daemon patience was verified live separately, against the same refusing instance).

- **2026-08-03 (later) - the corpus answers "who is misconfigured vs who is auto-identing", and finds the slot's twin.** Re-scanning the same overnight IQ at the ident position (audio 2350, the bpsk300 carrier +200 Hz) found no AX.25 PSK-companion firmware idents at all - but it did find **a second, busier afsk300-il2pc activity centre at ≈ 7.0517 MHz** (62 unique CRC-valid frames in the first evening half-hour: a GB7BEX-15↔GB7RDG-2 connected session, GB7WEM-7↔GB7RDG-2, `EI0RSI-7>ID` node beacons naming ukpacketradio.network), which is exactly the NinoTNC-native convention - dial 7.050000 USB, audio centre 1700 - and which straddles the plan's bpsk300 slot at 7.0516. The **first off-air `IDENT`-addressed frame this project has copied** came with it: `GB7WEM>IDENT` "Sysop G3KEY IO91un … Access to GB7CIP", **framed IL2P+CRC in-mode at the station's own data centre** - a BPQ/operator ID beacon, not the operator-manual's "300 AFSK AX.25 alongside a PSK mode" firmware ident, so the #193 ghosts' outstanding on-air leg (an AX.25-framed companion ident beside a live PSK carrier) remains unwitnessed, and their AX.25-only framing choice now has a measured caveat: real ID traffic also arrives IL2P+CRC-framed. Meanwhile the 7.0503 slot's own oddballs sorted cleanly into deliberate configuration, none of them firmware idents: UT1HZM (classic AX.25, 5-minute APRS-grid beacons, ~+12 Hz of centre), GB7BWR-2 (IL2P *without* CRC, 15-minute MAIL beacons, on-centre), PD4R-11 (IL2P without CRC), IW2OHX (deliberate dual-framing beacons ~200 Hz apart). Evidence: `corpus/ubersdr/` + the ident-region analysis in the session scratchpad.

- **2026-08-03 (later²) - RETRACTION and the true mechanism: there is no 7.0517 AFSK network; an AFSK discriminator cross-decodes BPSK300, and Tom caught it.** The previous entry's "second afsk300-il2pc activity centre at ≈ 7.0517" is **wrong**. Tom challenged it; the decisive experiment took five minutes: our own clean `bpsk300-il2pc` transmission at carrier 2150 **decodes through the Afsk300 chain** (single modem at carrier+200, and the bank) - a phase reversal is a frequency impulse to a quadrature discriminator, the slicer turns pulse/no-pulse into the differential bit stream, and the polarity-agnostic IL2P deframer accepts it, CRC and all. Squaring the off-air bursts (BPSK² → one line at 2·fc; h=0.67 FSK² → two lines at 2·f_mark/2·f_space, verified on our own TX both ways) puts those stations' carrier at **2150.0 Hz = 7.051600 MHz exactly - the BPSK300 channel, where they belong**; the +60…+165 Hz "AFSK centres" were the cross-decode's preferred discriminator offset, not station frequencies. Consequences: the `GB7WEM>IDENT` frame is an ID beacon carried **over BPSK300-il2pc on-channel** (so the previous entry's caveat against #193's AX.25-framed ghosts is withdrawn - though the cross-decode itself is the new caveat: an IL2P-framed AFSK listener parked ±200 Hz off a live BPSK300 carrier will decode the *data channel's own traffic*; #193's ghosts are HDLC-framed, which this mostly protects - mostly, because BPSK-carried HDLC would cross-decode the same way). The overnight BPSK census, via the artefact: GB7RDG-2↔GB7OXF-2 (the main artery), GB7BEX-15, GB7WEM-7, EI0RSI-1, IW2OHX-8↔PD4R-12. The morning entry's "three-framing" reading of the 7.0503 slot is amended by the same toolkit: **UT1HZM is genuine FSK but 150 Hz shift** (h = 0.5 - twin squared lines 300 Hz apart; some non-NinoTNC soundmodem) framing classic AX.25 at ≈ 7.05031; **GB7BWR-2 and PD4R-11 are genuine FSK, plain IL2P** (mode-13 signature stands); but **IW2OHX-15 is BPSK300 IL2P+CRC at ≈ 7.05028** - right framing, wrong modulation, on the FSK channel - proven by the real `BpskMultiModem` decoding its beacon, whose full text self-describes: "IL2P A 300Bd - 850Hz - 20m/40m". Instrument lesson, banked: any AFSK survey near a live BPSK channel must carrier-classify by squaring before believing a decode's modulation; decodes prove the bit stream, never the carrier.

- **2026-08-03 (later³) - §E4: MS110D goes on the air for real, and every waveform of the ladder is copied over a propagation path.** Every MS110D result before today was made into a **dummy load** with the listener metres away (near-field leakage) or, later, down a wired 125 dB pad chain into the studybox RSP1. Both are rigs, not radio: the execution plan says so explicitly ("the path is local leakage… a property of the *room*, not of propagation"). Today ANT1 became a real antenna, and the campaign's gating item [A9] finally ran. Transmitting from M0LTE's FLEX-6500 on **7.1285 MHz** (waveform centre 7.126500, modem +2000 Hz) at a measured **43-48 W** forward *during the burst*, into `wessex.zapto.org` in southern England: **WN0, WN1, WN2, WN3, WN4, WN5, WN6, WN7 and WN13 all acquired, matched their WID, and decoded BIT-EXACT (BER 0.00E+000), every one ending on EOM** - 8 bits at WN0 up to 2272 bits at WN7 (8PSK, 4800 bps), CFO −0.1…−0.3 Hz throughout. **WN8 (16QAM, 6400 bps) is the ceiling of this path**: it acquired cleanly with a correct WID and then delivered BER 0.491 and `SignalLost`, which is a link budget running out rather than a demodulator fault. WN3 missed once at a search metric of 0.290 against a 0.32 threshold and was bit-exact on a retry 70 s later - a fade. This is the first evidence in this repo that the waveform survives a real ionospheric/ground-wave path at all, and it lands with no code change to the demodulator: revision `708dc87`, the same modem the mask suite scores. Evidence, per-burst figures, station facts and the harness defects found: [`evidence/2026-08-03-e4-first-on-air/`](evidence/2026-08-03-e4-first-on-air/README.md).
  **The blocker was not the modem, and is worth carrying forward: `--dial-correction` must be re-measured *per receiver*, not per session.** Against a second site (M0EYT, Wimborne) whose combined reference error was −64.8 Hz - *inside* the demodulator's ±75 Hz acquisition grid but at its edge - eight consecutive bursts at 45-50 W across WN0/1/2/4 produced **no acquisition at all**, indistinguishable from a broken receiver. Against Wessex, whose error measured +1.98 Hz, the identical commands decoded everything first time. A third site (M0XDK-1, Northampton) could not hear 50 W at all while hearing other UK stations at 20-29 dB, so receiver choice is itself a campaign variable. Also banked: a tone's apparent strength badly overstates a burst's - a carrier puts its power in one 5.9 Hz bin while MS110D spreads it over 2789 Hz, ~27 dB of difference before PAPR, so "I can see the tone plainly" and "the burst is 3 dB above noise" are the same signal.

- **2026-08-04 - the BPSK bank's reported frequency offset was a branch index too; it is now a measurement, to well under 1 Hz.** The same defect the 300 AFSK bank had (2026-08-02, above), in `BpskMultiModem`, found by Tom in the live frame log rather than by a test: 26 hours of the GB7RDG 40 m channel (Flex 6500, GPSDO-locked, `bpsk300` at 7.051600 MHz) produced 431 frames whose `offset_hz` took **nine distinct values and nothing else** - the 4 pairs × 7.5 Hz comb itself - with **354 of 431 (82 %) reading exactly −30 Hz**, the most negative branch. `EI0RSI-1` read +30, then −30 eight seconds later, then −15, inside 23 seconds; no carrier moves 60 Hz in 8 seconds. Mechanism identical to the AFSK case: branches are fed in array order, several copy every frame (the differential detector tolerates wide offset on any one branch, so overlap is the normal condition, not the exception), the deduper is first-wins, and what reached the log was *the lowest-centred branch that could still read it* - a label for where we listened, dressed as a reading of where the station was. On a GPSDO-locked rig it looks exactly like a channel mistuned by 30-odd Hz, and was diagnosed as one before anybody read the source (**issue #202**).
  **The measurement was, again, already being computed and thrown away.** The differential detector forms z·conj(z one symbol ago) and takes its *real* part as the bit decision; the imaginary part it discards is the carrier's per-symbol rotation. Squaring the normalised product strips the 0-or-π data step - for random payload and for the all-reversal training preamble alike - leaving a phasor at twice the offset, which is exactly `BpskCarrierOffsetEstimator`'s algorithm; it is now inlined into `BpskDemodulator.CarrierOffsetHz` at the cost of a `sqrt` and a handful of multiplies per sample, against the two transcendentals the mixer was already spending. One difference from the standalone estimator, and it matters: that class peak-holds since its last reset, which would freeze on the first strong burst of a 26-hour session, so this windows the recent signal (~1000 contributing samples) and a frame's reading describes *that* frame. Unambiguous over ±baud/4 - ±75 Hz at 300 Bd, far wider than any bank span. The coherent path needs none of this: its Costas NCO is already tracking the carrier, so the loop's own frequency correction is the residual, gated on DCD.
  `BpskMultiModem` now holds every branch's copy to the end of its dedupe chunk and emits the one with the smallest measured residual - the best-matched branch - reporting `branch + residual`; where no branch could measure, it reports **null** rather than a comb position, because "we did not measure it" is the honest answer and the step is not a substitute for one. Same shape, and now the same code path shape, as `Afsk300MultiModem`. **Measured after, clean loopback swept ±33 Hz in 3 Hz steps: worst error 0.09 Hz**, continuous and symmetric, with no pile-up anywhere on the comb; `bpsk1200` tracks ±100 Hz to within 0.16 Hz. Under AWGN at −11.25 Hz, 12 seeds per point: every frame that decoded also produced a measurement (the 0.5 coherence gate never fired on a decodable frame) with worst error 0.20 Hz at +4 dB, 0.61 Hz at 0 dB and 1.25 Hz at −2 dB, where the mode's own AWGN threshold is ≈ −3 dB. **On real RF**: the committed GB7RDG off-air capture, whose carrier the standalone estimator independently reads at ~8 Hz, now comes out of the shipping bank at **+8.56 Hz** - off-comb by 1.06 Hz, so the number could not have been produced by naming a branch (`OffAirBpskTests.The_Bank_Reports_The_Measured_Offset_Of_A_Real_Off_Air_Frame` cross-checks the two paths, which share no selection logic). `BpskMultiModemTests.The_Reported_Offset_Is_A_Measurement_Of_The_Signal` pins eight synthetic points chosen at exact half-steps of the comb with a 2 Hz tolerance, so a return to naming branches cannot pass any of them; a lone `BpskModem`, which reported no offset at all before, now reports one too. Decode counts are untouched - the GB7RDG off-air fixture, the NinoTNC parity corpus and the QtSM interop fixtures all passed unchanged. **Consequence for this document**: the −15 to −30 Hz offsets quoted for `bpsk300` in the 2026-08-02 web-receiver run were branch labels; the entries above are annotated and the decodes themselves are unaffected. **Still not applied to `Afsk1200MultiModem`**, which keeps the identical first-branch-wins shape for the same reason as before - it carries the WA8LMF corpus baseline and wants its own before/after rather than a drive-by.

- **2026-08-05 - the IL2P+CRC modes can be told to accept plain IL2P as well, so a CRC-less neighbour is heard instead of thrown away.** No mode changes status: this is an opt-in receive tolerance, off by default, and the default behaviour of every mode is unchanged. It is filed here because it changes what a configured mode *can* hear, and because the 2026-08-03 overnight corpus above already named the population it is for: GB7BWR-2 and PD4R-11 beacon plain IL2P (NinoTNC mode 13) on the 7.0503 slot, and a mode-14 receiver structurally cannot hear them. The prompting evidence is a second, independent sighting on the BPSK slot: a station running `bpsk300` at 2150 Hz on 7.0516 MHz had survey captures it could not read, one of which decoded offline as `bpsk300-nocrc @ 2116 Hz -> 46 B GB7BPQ>BEACON: =5828.54N/00612.69W- {BPQ32}` with the carrier measured at ~2123 Hz, 27 Hz off its own centre and well inside its own diversity bank - right frequency, right modulation, right baud, wrong IL2P variant, and a BPQ32 node at the far end. The previous operator answer was a second modem entry on the same frequency (2026-08-03, above); `"acceptPlainIl2p": true` on the modem entry is the one-modem answer, and it costs the CRC: a plain frame stands on Reed-Solomon alone, arrives with `crc_valid` null, and an IL2P+CRC frame whose trailer fails is now indistinguishable from one and delivered too (`CONFIG.md` says so where the option is documented). The seam is `Il2pReceiver`, which every IL2P-carrying modem now pushes its bits through. Measured and pinned while building it (`Il2pReceiverTests`): a `crcMode: false` deframer handed a well-formed IL2P+CRC frame emits the **same AX.25 frame, byte for byte, exactly 32 bits early** at every payload size, so a naive second deframer would have delivered every ordinary frame on the channel twice - the plain reading is held for those 32 bits and dropped if the CRC reading claims the same bytes. Proven through audio on two families (`Il2pPlainPassthroughTests`: `bpsk300` and `afsk300-il2pc` each ignore a plain frame by default, accept it once with the option on, and still deliver an ordinary IL2P+CRC frame exactly once). Simulation only so far - no on-air confirmation that the GB7BPQ beacon itself now copies at the station that reported it. **Superseded the same day by the 2026-08-05 (later) entry at the end of this list**: the reading is no longer optional, only the delivery is.

- **2026-08-05 - ARDOP can share a 48 kHz channel: the 12 kHz engine is bridged, in simulation.** Until now `ardop` alongside any 48 kHz mode (`fsk9600`, `c4fsk*`, `freedv-*`, `ms110d-*`) was refused at start-up - the engine is hard 12 kHz and the channel runs at one rate. `ArdopChannelShift` grew into `ArdopChannelBridge`: receive decimates the channel's 48 kHz down to 12 kHz before the existing centre shift, transmit upsamples the shifted burst back up, all outside the TNC (the `FreeDvDatacModem` pattern, whose engine is native 8 kHz), and the refusal is gone. Proven hermetically at the frame level (`ArdopChannelBridgeTests`): a real ARDOP data frame round-trips modulate -> bridge to 48 kHz -> bridge back -> demodulate **bit-exact** in all three wirings (12 kHz shifted - the pre-bridge control; 48 kHz shifted; 48 kHz default-centre), and the two-tone pair leaves at 48 kHz on the asked-for centre with its 50 Hz spacing unstretched. The env-gated pdn-to-pdn session bench (`ArdopSharedChannelSessionTests`, `ARDOP_SESSION_BENCH=1`) gained a 48 kHz-channel case with the same wiring the daemon uses. **Not proven: any of this over RF, and no live ARQ session on a 48 kHz channel has been run against real ardopcf** (which cannot be the far end of a shifted centre by construction; at the default centre it can, and that remains the interop ground truth to collect). The same change fixes a latent config bug: the ARDOP latency tunings (Flex packet buffer, ALSA capture depth, 20 ms RX blocks) keyed off the legacy `--ardop` flag and silently missed stations configuring ARDOP as a modem entry - they now key off the modem entry itself.

- **2026-08-05 - the spec-fixed waveforms became movable: ms110d-*/freedv-* take a `frequency`/`rfFrequency` like any carrier mode, in simulation, with the shift measured clean.** Their audio centres were pinned by their standards (1800 Hz / the OFDM centres), so an `rfFrequency` on one dictated the whole plan's dial and forced everything else above it on USB. `FrequencyShiftedModem` translates the audio either side of the unchanged, validated DSP (analytic SSB shift, 639-tap Hilbert - the 129-tap default rolls off exactly where MS110D's 170 Hz skirt sits relative to a 48 kHz rate), so the planner now places these modes like any other; the RF placement is exact either way, and on air a moved waveform is still standard, because interop is set by the RF centre alone. Proven hermetically: frame round-trips through moved modems (`ms110d-wn6` at 3000 and 5000 Hz, `ms110d-wn2` at 3600, `freedv-datac3` at 2500 - `FrequencyShiftedModemTests`); the mirror image of a 5000 Hz-centred WN6 burst measured **85.7 dB down** (gate: 50); probe-measured centres follow the override to tolerance for both families (`ModemCentreFrequencyTests`); a centre that would fold the skirts into DC or Nyquist is refused at start-up with the numbers; and the planner places Tom's 40 m layout (afsk300 7.0503 / bpsk300 7.0516 / **ms110d-wn6 7.0535, ABOVE them**) on a planner-chosen dial (`BandPlannerTests`), which the dictated dial could never produce. Decode-margin gates: a CI smoke pins zero lost decodes at a working SNR moved vs native, and the env-gated `MS110D_SHIFT_GATE=1` ladder (A/B/B' at native/3000/5000 across the AWGN knee) pins the knee unmoved; `sm-ota sim --centre` is the standalone instrument for the same A/B. The ladder earned its keep before merge: the first cut (shift only, no receive filter) measured a **3 dB knee movement at a 5000 Hz centre** (0/30 decodes at 5 dB against 27/30 native; a 3000 Hz centre lost ~1 dB), and raising the Hilbert to 1279 taps changed nothing - because it is not a Hilbert defect: downshifting a real signal by delta folds all channel noise below delta onto the wanted band (the analytic mix only suppresses the input's own conjugate), so a 3200 Hz shift doubles the demod band's noise. The decorator now bandpasses receive to the on-air band BEFORE the downshift, which removes the folding region entirely: re-measured 30/30 at 5 and 6 dB at both moved centres, native/3000/5000 within binomial wobble at 4 dB (23/22/18 of 30), the moved arms fractionally ABOVE native at 5 dB because the bandpass also strips out-of-band noise the native arm still ingests. ARDOP's sibling shift never met the fold because its shifts are a few hundred Hz. **Not proven: nothing on air at a moved centre yet.** Behaviour note for existing configs: an `rfFrequency` on one of these modes keeps its RF placement to the Hz but may report a different dial, since the planner now centres the ensemble instead of obeying the pinned mode.

- **2026-08-05 (later) - every IL2P+CRC mode now reads plain IL2P unconditionally; the option only decides whether the host is given it.** Still no mode changes status, and the default behaviour of every mode's *host* path is unchanged - but what a configured mode **hears** has changed on every IL2P+CRC entry in the matrix above, which is why this belongs here. The operator's reasoning is the point and it is worth carrying: IL2P+CRC exists precisely because Reed-Solomon alone was letting too much corrupt traffic through, so he does not want RS-only frames reaching his node on that port, and he does want to see them. One switch was answering both questions. Now the second, plain reading always runs; a frame it alone produces is marked `MonitorOnly` and reaches the waterfall (badged **`RS ONLY`** in warning orange, with a tooltip saying whether it went to the host), the frame log (`crc_valid` null), the journal line (`plain il2p (rs only, not passed to host)`) and the signal survey, where it counts as a decode and stops its burst being captured as `missed` - deliberate, and the better trade: a badged row naming the sender beats another WAV of the same beacon every ten minutes, and it leaves the capture budget for bursts nobody has explained. `acceptPlainIl2p` now only releases such a frame to the KISS host, and the KISS quality sidecar is skipped for a withheld one so a host is never told about a frame it did not receive. **New evidence out of the change, on audio that has been in this repository since issue #40**: the committed GB7RDG NinoTNC BPSK300 capture holds its connected-mode frame **twice, 12.5 s apart, and only the first one's CRC verifies** - every branch demodulates the second perfectly with zero FEC corrections and no branch can make its four trailer bytes check out. It was being discarded in silence on every run of the suite; it is now a badged, withheld row (`OffAirBpskTests.The_Second_Burst_In_The_Capture_Is_Reported_As_Plain_And_Withheld`). The same capture pins the invariant that matters for every other frame on such a port: **if any branch's reading verified the trailing CRC, that is the reading reported and delivered**, whichever branch produced it and whatever the option says - the branches nearest this capture's carrier are precisely the ones that cannot verify it, so a bank that chose on centring alone reported a good frame as RS-only (`A_Verified_Frame_Is_Reported_As_Verified_Whatever_The_Option_Says`; the choice is made on what each reading proved, not on where the frame was being routed). End to end on the operator's own 12 kHz survey capture at 2150 Hz through `bpsk300`: option off, host path empty and monitor path `46 B GB7BPQ>BEACON: =5828.54N/00612.69W- {BPQ32}` marked plain and monitor-only; option on, host gets it once, still marked plain. **Cost, measured rather than assumed**, because this puts a second deframer on every IL2P+CRC channel permanently and nine on a BPSK bank: the added work is 24-bit sync hunting at **5.0-5.8 ns per received bit**, so a nine-branch 300 baud bank spends 0.0014 % of one core and a 19200 bps C4FSK modem 0.01 %; over 120 s of real off-air audio, best of five passes, the whole receive path measures 4805 ms with it against 4828 ms without (`bpsk300` bank), 5895 against 5811 (`afsk300-il2pc`) and 556 against 550 (`qpsk2400`) - inside the run-to-run spread in both directions, and a Pi will not notice. Still simulation and archive audio only; no on-air confirmation that the GB7BPQ beacon itself now appears at the station that reported it.

- **2026-08-06 - ms110d-* transmit waveform switchable at runtime over KISS SETHW; ACKMODE hardened on the way.** No mode changes status, filed on the acceptPlainIl2p precedent: it changes what a configured mode can DO. `Ms110dModem.SetTxWaveform(wn, interleaver?)` is the typed public API (an immutable modulator swapped under a volatile reference, applied from the next burst; receive untouched - autobaud); KISS SETHW (type 6, `payload[0]` = WN 0-8/13, optional `payload[1]` = interleaver 0/1, no +16 form) is its wire face, echoed back to the sender on success and journalled either way; the shift decorator forwards both paths, so a moved MS110D is equally controllable. Proven hermetically (`KissHardwareControlTests`, `Ms110dRuntimeWaveformTests`): a WN6->WN2 switch more than doubles the burst airtime for the same payload (it really is the slower waveform, not a renamed string) and the burst decodes autobaud; invalid WN refused with the modem unchanged; the whole path works through a 3000 Hz-shifted modem. Three ACKMODE/server defects fixed and pinned while building the surface: a dedicated port's acks and echoes carried the REAL sub-channel nibble while everything else on that port is relabelled 0 (a nibble-0-only host would mis-file or drop them); the per-socket write path was unlocked, so a continuation-thread ack could tear a concurrently broadcast frame mid-FEND (both frames then vanish at the client's decoder); and an id-only ACKMODE frame (two bytes, no data) fell through a `> 2` guard and vanished - it is now acked immediately, since transmission of nothing is trivially complete. CONFIG.md gained "The KISS command surface" section as the driver contract.

- **2026-08-06 (later) - the noise-folding audit: every pre-existing frequency shift checked; ARDOP's bridge had the same defect and is fixed the same way.** The FrequencyShiftedModem gate's finding (downshifting a real signal by delta folds all channel noise below delta onto delta - f; Hilbert quality is irrelevant) is generic to any real-signal receive downshift, so every `FrequencyShifter` consumer was audited. Immune by construction: `SsbDemodulator`/`StreamingSsbDemodulator` (the UberSDR daemon path and `sm-ota monitor`) stay complex through the shift and apply a complex sideband bandpass before ever taking a real part, which is the step that folds; every transmit-side shift (up-shifts, or complex IQ out); the AFSK/BPSK banks (internal complex mixers, per-branch filters). **Guilty: `ArdopChannelBridge.Receive`** - unshifting a centre above 1500 Hz is a downshift by centre - 1500, and a white-noise PSD test measured **+2.93 dB** across 550-850 Hz of the engine band at a 2400 Hz centre (fold overlaps a session of bandwidth bw once centre > 3000 - bw/2, so 2000 Hz sessions suffer above centre 2000 - placements a nominal rig's Concern warning flags but a wide-window headless Flex plans legitimately). Fixed exactly as the decorator was: bandpass to the on-air band (widest session ± 300 Hz margin, 639 taps at the engine rate) BEFORE the unshift, only when shifted - the unshifted path stays bit-for-bit; re-measured flat (< 1 dB), every existing round-trip and the shifted-decode tests unchanged, and the PSD test stays as the regression pin. **Also fixed: `sm-decode --centre`** (same geometry on off-air recordings, same bandpass-then-unshift). `nino-compare shift` keeps its semantics - a generic instrument that cannot know the signal's band - and its help text now carries the caution plus the threshold; its documented slot-to-1500 use folds harmlessly below every reference band. The validated ARDOP placements were never exposed: centre 950 unshifts UPWARD (fold lands at Nyquist, far above the band), and the default 1500 has no shift at all.

- **2026-08-06 (later²) - the whole-codebase review campaign: `afsk1200-multi` cold-start TX broken→working; the last two branch-index offsets become measurements; the QPSK family reports offset at all.** A full-repo code review (10 finder angles, 33 adversarially confirmed findings, resolved across PRs #227-#234) landed three changes that belong in this ledger. **(1) `afsk1200-multi` transmit trained a cold receiver badly and now does not**: its `Modulate` still sent the all-flags TXDELAY fill that 4ab6d9e replaced with `TrainingPreamble` zeros in `Afsk1200Modem` and `Afsk300Modem` - the identical Bell 202 waveform, so frames a cold receiver took 10/10 from the single modem could be lost from the bank at short TXDELAY. The bank now sends the identical fill, held to the identical bar (`MultiDecoderTests.The_Bank_Transmits_The_Training_Fill_A_Cold_Receiver_Acquires_From`: 10/10 into a cold single receiver at TXDELAY 0). **(2) The 2026-08-04 entry's standing carve-out is closed**: `Afsk1200MultiModem` was the last bank still reporting the first-finishing branch's comb position as `FrequencyOffsetHz`; it now ranks candidates per chunk on each demodulator's own `CarrierOffsetHz` residual and reports branch + residual, with one wrinkle the PSK banks never had - the +6/+12 dB/oct emphasis pre-filters tilt the spectrum by design, which skews the envelope midpoint the residual is read from (measured: an emphasised branch 30 Hz out reported a near-zero residual and stole the win from the matched flat branch), so flat branches outrank emphasised ones and the residual decides among equals. Honesty is coarser than the other banks': ~±12 Hz on steady content and ~±30 Hz worst-case from the trailing flags' mark duty (`AfskDemodulator` documents ±200 for Bell 202), against a deterministic −60/−90 comb lie before. **Caveat, deliberately recorded: the WA8LMF before/after that the 2026-08-04 entry asked for has NOT been run** - the TNC Test CD audio is not on this box; the in-suite AFSK coverage (loopback, Dire Wolf cross-validation, NinoTNC TXDELAY-0 parity, the multi-decoder and centre-frequency tests) passed unchanged, and the emission restructure is the same count-preserving shape the 300 baud and BPSK banks measured as decode-neutral, but the corpus re-score remains the outstanding follow-up before the 972/970 figure is re-asserted. **(3) `qpsk600`/`qpsk2400`/`qpsk3600` - the live NinoTNC network's primary modes - reported no frequency offset at all**: `QpskDemodulator` never received the measurement `BpskDemodulator` gained in #202. It has it now (coherent: the Costas NCO's tracked correction, DCD-gated; differential: the normalised conjugate product raised to the *fourth* power - one doubling more than BPSK's squaring, since QPSK's data steps are 90° not 180° - into the same decaying window), and `QpskModem` forwards it into `FrameQuality` exactly as `BpskModem` does (`QpskCarrierOffsetTests`: both detectors, shifted and on-frequency, the shift sized to the coherent loop's documented ~5 Hz pull-in). No mode changes matrix status; the campaign's remaining fixes (transmit-path lifecycle, TCP server robustness, startup contract, the descriptor-table refactor that turned per-mode facts into one row each, and the receive-path efficiency work including the mixer NCOs becoming rotating phasors) are recorded in the PR trail rather than here, since they change no mode's validation state - with one exception worth a sentence: the ARDOP TX centre shift was truncating the last ~5.3 ms of every shifted burst (the trailer tones) and leaking them into the next burst's leader, fixed per-burst-shifter-with-flush like every other TX shift (PR #232), which tightens the 2026-08-02 shifted-session evidence without invalidating it (those sessions decoded despite the truncation).

- **2026-08-06 (later³) - the differential BPSK receive chain rebuilt against measurement: ~1 dB more AWGN margin for `bpsk300`, ~1.5 dB for `bpsk1200`, and the 24 h miss-corpus scoreboard moves 8 → 5 (PR #236).** No mode changes status - both were ✅ - but the numbers behind the primary NinoTNC live mode moved enough to record. Three independent losses were isolated on the `sm-ota sim` ladder, each with its own experiment: the **band-pass fronting the decode path** cost ~1.5 dB (its passband is not flat across the signal, breaking the matched-filter condition; it now gates EnergyBusy only, the truncated-RRC matched filter's own −54 dB-at-one-baud stopband exceeding what it provided); the **⅔-baud low-pass** was not matched to the TX shaping (now a root-raised-cosine mirror of the modulator's roll-off, cascade ISI-free at symbol instants, ~0.5 dB); and **Dire Wolf's 0.74 DPLL inertia** carried ~1 dB of timing jitter at the RS threshold (0.92 on this path; the all-reversal IL2P preamble re-locks a cold clock within a normal TXDELAY, and 0.96+ measured better in steady state but failed the 24-bit minimum preamble - so 0.92 it is). On top of those the bit decision moved from the single-sample differential product to **decision-feedback differential detection**: the symbol-instant sample decided against a remodulated ~20-symbol exponential carrier reference, which buys back the classic ~1 dB differential-vs-coherent give-away with no loop to lose lock - carrier offset handled by a narrow always-running rotation tracker (two bank steps, which also keeps the neighbouring-branch selection diversity the wide-tolerance plain detector had provided for free) plus a one-shot per-burst seed from the offset window covering the ±baud/4 a lone modem always tolerated. Ladder, 9-branch bank, 300 bursts/point: **AWGN −4 dB 46 % → 82 %, −5 dB 4 % → 49 %, −3 dB 81 % → 94 %**; offset sweep 0-33 Hz at −3 dB flat 92-97 % including the mid-branch points a naive reference nulls (the first cut measured 0/60 at +3.75 Hz, which is what forced the frequency state); CCIR Good −4 dB 25 % → 33 %; CCIR Poor 22 % → 29 % at +12 dB - **Poor remains equaliser-bound, the known mode limit stands**. `bpsk1200` inherits the chain wholesale: +2 dB 20 % → 86 %. Real audio: the miss corpus scoreboard (`NINOTNC_ASPIRATION=1`) moves **8 still-failing → 5** of 37 (three genuinely weak GB7WEM-7/GB7OXF-2 frames the GB7RDG NinoTNC copied and we did not, now copied; 32 of 37 are graduation candidates per the corpus discipline - not done in this PR), and **every branch of the bank now CRC-verifies the committed GB7RDG capture's first burst** where the four branches nearest the carrier had managed only the plain reading. NinoTNC parity, QtSM interop and the off-air fixtures pass unchanged; the coherent path keeps its issue-#5 configuration untouched as the acquisition cross-check, and `CoherentDetectionTests` records the deliberate inversion (the BPSK rows now pin differential ≥ coherent). Cost: the bank measures 9.3 % of one core at 12 kHz on the dev box, against 4.2 % before. **Found on the way, fixed first**: `Il2pReceiver`'s late-reading guard - one maximum-length frame = 34 s of wall clock at 300 Bd - swallowed the plain reading of a byte-identical retransmission as a "late second reading" of the previous burst; the GB7RDG capture holds exactly that pair 12.5 s apart, and only ever surfaced its second burst because the branches that could not verify burst one's CRC never armed the guard. Cleared on `Reset()` now (skew cannot cross a burst boundary; retransmissions always do), with a regression test that fails against the old code. Measurement caveat, documented in the tests: a matched filter centred on the channel asymmetrically truncates a far-off signal's spectrum, biasing a **lone modem's** offset reading by ~10 % of the offset (24.3 read at 22 true); a bank's winning branch reads within half a step, where the bias is under 0.1 Hz, and the sub-Hz bank measurement pins of #202 pass unchanged. Simulation and archive audio only - nothing on air with the new chain yet; the obvious next legs are an on-air re-run of the Flex→RSP1 AWGN waterfall and the QPSK family, which still runs the old-style chain.

- **2026-08-06 (later³ b) - a grazed trailing CRC now corroborates a frame instead of silencing it: the miss-corpus delivery rate moves 3/37 → 22/37, and the mechanism behind 29 stranded frames is named (PR #238).** The day's honest correction first: the aspiration scoreboard counts `FrameDecoded`, which since the 2026-08-05 always-read-plain change includes withheld RS-only readings - so "32 of 37 copy" meant 32 demodulate **byte-exact**, while only **3 verified their CRC and reached a host**. Probing the other 29 measured their trailers sitting 2-10 wire bits from the trailer their (proven, byte-exact against the NinoTNC's own decodes) payloads imply, with the damage clustered in the last bit positions - the transmit pulse truncates at the end of the burst, the matched filter loses the final symbols, and IL2P+CRC parks its only FEC-unprotected bytes exactly there. The NinoTNC's shorter filters graze less, which is how it copied what we would not deliver. The fix is receive-side and format-clean: `Il2pReceiver` already holds a plain-only frame for exactly the 32 bits its trailer occupies, so it now collects those bits, recomputes the trailer the payload implies (`Il2pCodec.Encode`), and **delivers the frame when they differ by ≤ 4 bits**, whatever `acceptPlainIl2p` says - an uncorrelated trailer lands that close with probability ~1.1e-5, the same order as the CRC check's own false-accept, so a corroborated frame is CRC-grade evidence, badged with its measured distance (`FrameQuality.TrailerNearBits`; `crc_valid` stays null, which remains the truth). One bit of damage never reaches this path - the trailer's own Hamming layer corrects it and the CRC verifies outright - and a genuinely CRC-less station (GB7BWR-2, PD4R-11) is never promoted by it: nothing follows its frames but noise, which sits ~16/32 away. Measured on the corpus: host-delivered 3 → **22 of 37** (3 CRC + 19 corroborated), 10 byte-exact frames beyond the threshold, 5 with no demodulation at all. A companion surprise fell out of the same change: the "historical wide filters lose the frame to the neighbouring QSO" off-air AFSK300 pin was wrong about the mechanism - those filters recovered the payload all along and lost only the trailer, so the pinned test now records the true contrast (wide filters: corroborated-only; current defaults: CRC-verified). All modes inherit the delivery change through the `Il2pReceiver` seam. Simulation and archive audio only; the live 40 m capture campaign (see plan.md 2026-08-06 (later) and docs/rx-roadmap.md) will say what it does on fresh traffic.

- **2026-08-06 (later⁴) - Watterson performance masks land for the audio modems (rx-roadmap workstream 0, Tom's proposal).** No mode changes status; what changes is that every mode's measured floor is now held by a test instead of by ledger prose. `WattersonMaskTests` runs the same `SimBench` rig `sm-ota sim` drives - a mask number and a CLI number are the same deterministic measurement - in two tiers: a CI-blocking smoke tier (one to five anchor points per mode at N=40, floors 2-3 binomial sigma under the 2026-08-06 measured values, sized to catch a decibel and ignore wobble; ~21 s) and an `SM_MASK_GATE=1` full ladder (the flagship bpsk300's whole SNR x channel x CFO grid plus bpsk1200 at N=100; ~140 s), the receive-path A/B instrument. Masks are measured reality, never aspiration: bpsk300's rows come from the post-#236 + erasure receiver (AWGN -4 dB 37/40, the 3.75 Hz mid-branch CFO point 39/40, Good 0 dB 22/40, Poor +9 dB 7/40 - the equaliser-bound floor pinned so it cannot collapse unnoticed), and the QPSK family's rows sit at their current uncampaigned levels on purpose, so their eventual campaign is measurable. The accept discipline joins CLAUDE.md: receive-path PRs run the affected mode's full ladder A/B, and a mask moves only with an entry here saying what moved it. Both tiers green at landing. Known limit, stated where the masks live: green means no regression against the model - no static crashes, no SSB tilt, no AGC, our own TX shaping - and the 40 m capture campaign stays the truth about the band.

- **2026-08-06 (later⁵) - workstream 5's MLSE equaliser: built to parity, measured honestly, default unchanged, ceiling claim retired.** No mode changes status and no mask moves - the default bpsk detector stays differential, which is why this entry records an *instrument* rather than a transition. `PskDetector.Mlse` (opt-in; `sm-ota sim --detector mlse`; QPSK rejects it) puts a 4-state adaptive Viterbi equaliser behind the unchanged differential front end: 3 complex taps under decision-directed LMS, add-compare-select margins as the erasure ladder's confidence, 16-symbol traceback inside the DCD-reset window. Reaching parity took four measured corrections worth remembering (each cost dBs until found): no mid-stream trellis flush ever (a pipeline refill is a frame-killing bit slip); outer-tap adaptation skipped exactly on alternating decision triples, because the all-reversal IL2P preamble makes a 3-tap channel physically unidentifiable (only g0-g1+g2 is observable) and unconstrained LMS walks into the sync word with the main energy split three ways; outer taps enter the branch metrics only on sustained smoothed evidence during a seeded burst (unconditional exposure cost ~5 dB AWGN; engagement between bursts, where a tap-power ratio is meaningless, scored 0/50 at -5 dB); main tap and rotation tracker adapt undelayed (they are a per-symbol feedback loop, and delaying it measured 1-2 dB of phase jitter) while the outer taps adapt on traceback-matured depth-6 decisions. Endpoint, N=100/point seed 1 with Poor's upper rungs re-confirmed on seeds 101-200: AWGN -5..-2 and the 0-30 Hz CFO sweep at parity within two points; Good/Moderate parity with +3 at the top rungs; **Poor +6/+9 dB pooled 51->58 and 56->64 of 200 (~+4 points)**; the 37-frame miss corpus identical at 32/37 demodulated. The 29 %->50-60 % expectation is retired with its mechanism named: Poor's residue is flat outage, DPLL timing wander and near-antiphase composite nulls (no static two-path setting separates the detectors through the full chain - DF-DD+RS digests any static echo the trellis can equalise, and what kills DF-DD is timing, which no symbol-rate equaliser restores), and the preamble's unidentifiability keeps the outer taps unconverged until after the sync word, so header-limited frames are structurally out of reach. Successors: A/B `mlse` against the capture campaign's misses-v2 on real audio before any default flip is considered, and workstream 4's two-pass receiver, which is the honest route to converged-taps-from-symbol-0. `MlseEqualiserTests` + `MlseDetectionTests` pin the behaviour, including the recorded negative (the static two-path fixture asserts decode-through, deliberately not separation).

- **2026-08-07 - the QPSK campaign's first landing: matched filter + decode-path band-pass removal + DPLL timing move qpsk600 ~2.5 dB and qpsk2400 ~2 dB at the AWGN knee, and qpsk2400's CFO dip is gone.** The campaign (docs/qpsk/plan.md, Tom-directed, MS110D discipline) baselined both SSB QPSK modes under both detectors, autopsied the pre-#236 chain one loss at a time, and landed the three chain fixes as one change to `QpskDemodulator`'s differential path: an RRC matched to each mode's own TX roll-off replaces the 0.75-baud low-pass (~1-1.5 dB, the dominant fix - and the Q1 lesson worth keeping: a band-pass bypass alone measured a NEAR-NULL, because the bpsk300 prior's 1.5 dB was measured against a matched filter; against an unmatched low-pass the ripple drowns, so matched-filter-first is the autopsy order); raw audio feeds the decode chain with the band-pass gating EnergyBusy only (worth points only at qpsk600's deepest rung, where its +-300 Hz passband ripples across the signal); DPLL inertia 0.92 on the differential path (~1 dB, the PR #236 number reproduced). Full ladder N=100 seed 1: qpsk600 AWGN 0 dB 6->69 %, +2 80->99 %; CFO wall 10-15 -> 15-20 Hz; qpsk2400 +7 15->72 %, +9 80->97 %, CFO row 72/76/71/58 -> 95/96/94/84 across 3.75-30 Hz. Fading rows essentially unmoved on both modes - the Q0 loss surface's prediction that Moderate/Poor are equaliser-bound at these symbol rates stands, and stays with the campaign's hard end. Three pins moved with the landing, all improvements outrunning fixtures: the QPSK rows of `CoherentDetectionTests` joined the BPSK rows in the inverted differential-matches-coherent gate (the QPSK decision is still the one-symbol conjugate product - the decision-feedback reference is the campaign's open follow-up); `SsbLadderPassTests`' sub-cliff rung moved 0 -> -6 dB; the qpsk600/qpsk2400 AWGN smoke masks rose 30 -> 36 and 32 -> 36 of 40. Coherent detection untouched as the cross-check variant. Interop gates green at landing (NinoTNC parity, QtSM, off-air fixtures, full suite). Both modes stay wire-identical: nothing transmitted changed.

- **2026-08-07 (later) - the QPSK four-phase decision-feedback reference lands as a CFO-robustness change, with its AWGN null and its two hard-won findings on the record.** Fix-set 2 of the QPSK campaign (docs/qpsk/plan.md): the differential path decides absolute quadrants against a decision-remodulated ~20-symbol reference once a burst's rotation is seeded from the fourth-power offset window (quartered angle, unambiguous over +-baud/8), with a decision-directed tracker from the seed on. Measured: qpsk600 at +8 dB with a realistic 150 ms TXDELAY moves 15 Hz offset from 71 % to 97 % and 30 Hz from 0 % to 38 % - the first configuration in this repo's history to decode qpsk600 at 30 Hz - while AWGN, fading and knee-SNR CFO rows are identical to fix-set 1 within confidence intervals: the reference buys QPSK no noise margin (a measured null, unlike BPSK's ~1 dB, and unlike the campaign's own prior). Two findings worth their cost: the seed must gate the DECISION HANDOVER, not just the rotation (a reference deciding from cold scored 60 % at 8 Hz where the plain product scores ~99 % - QPSK has half the preamble symbols and half the phase margin BPSK's cold-deciding reference got away with; unseeded and weak bursts keep the plain product, so nothing is ever worse than fix-set 1); and qpsk3600 is deliberately scoped OUT after the same machinery measured a clean-signal regression on its FM chain at 6 2/3 samples per symbol (22/30 vs 30/30) - `decisionFeedback: false` keeps the mode on the detector it was validated with, recorded in the factory and the demodulator parameter. Full suite green (1574 tests); coherent path untouched; nothing transmitted changes.

- **2026-08-07 (later2) - the ensemble decode-any knob lands as an opt-in: differential + MLSE banks in union, +6-7 points on CCIR Poor for double the bank CPU.** rx-roadmap workstream 2's demoted idea, revived by the capture campaign's opening-evening measurement (symmetric ~1.5 % exclusive-frame exchange between the detectors, +1.1 % deliverable union on real traffic). `ModemOptions.SecondDetector` gives every bpsk diversity position a branch under each detector; the bank's existing content dedupe reduces the union to single deliveries; transmit is pinned byte-identical (a receiver arrangement, not a wire change); non-bpsk modes throw rather than silently supply the single bank. Sim ladder N=100 seed 1, single differential -> ensemble with mlse: AWGN 56/88/95 -> 58/88/95 across -5/-4/-3 dB, Moderate 50/55/63 -> 50/58/67 across 0/+2/+4, Poor 21/24 -> 28/30 across +6/+9 - workstream 5's fading catches banked without losing a differential frame, never worse anywhere measured. Deliberately NOT the default (the CPU doubling is an operator's call); masks continue to pin the single-detector default, so no mask moves. `EnsembleDetectionTests` pins the mechanism: union delivery, one-delivery dedupe, duplicate-detector collapse, byte-identical transmit, and the loud refusal on non-bpsk modes.

- **2026-08-08 - ARDOP off-air, the first wild-corpus campaign result: ConAck decode aligned to reference policy (M0LTE.Ardop 0.4.0), the monitor centre corrected, and the top-rung null confirmed at reference parity.** The ARDOP campaign (docs/ardop/plan.md) put the A0 wild baseline - 636 frames acquired, 551 bodies ok, RXO over ~45 h of the 40 m capture - to ardopcf 1.0.4.1.3 frame by frame, over bit-identical audio via the new `ardop-cut`/`--emit` instruments (#258). Three results belong here. **(1) ConAck500 was the reference's only clean win, 5-0, and it was policy, not demodulation**: ardopcf's `Decode4FSKConACK` (SoundInput.c:3059) initialises Timing to 0 and accepts on `Timing >= 0`, so a body whose three timing bytes have no 2-of-3 majority still passes, reported as "timing 0 ms" - all five disputes were exactly that case, and deployed ISS peers proceed on such ConAcks (our ARQ engine dropped them in `IssConReq`, stalling connections a reference peer completes). M0LTE.Ardop 0.4.0 (M0LTE.Ardop#3, v0.4.0 on merge 7616ce9) accepts a ConAck on successful type decode and reports `ConAckLeaderMs` null without a majority, refusing to fabricate the reference's 0 ms; a loopback test pins it and the package's ardopcf oracle legs stay green (368/368). Corpus A/B bounded to the chunks both scans processed: **exactly 5 verdict flips @1650 and 8 @1500, every one ConAck500 fail->ok, nothing else moved** (wild ConAck500 now 35/35 at the corrected centre). **(2) The monitor's wild-slot centre was ~150 Hz wrong**: the A1 sweep measured true session centres clustering 1500-1560 Hz audio (48 of 52 cuts; the survey estimator's ~1650 was biased), and the corrected-centre rescan acquired **749/605 against 636/551** - the acquisition loss A0 called invisible, measured at +18 %. `ardop-monitor` now defaults to 1500 (native, unshifted). Honest negatives: the 4FSK.500.100 body rate did NOT improve at the corrected centre (50->47 ok of 84->90; its failures are GB7BPQ's half-hourly FEC beacon at the decode threshold, which ardopcf also fails 31 of 34 times), and the 1680-1760 Hz outlier sessions fall outside ±200 of 1500 (8PSK 3->2, 16QAM.2000 1->0 acquisitions). **(3) The wild 16QAM/8PSK zero is a confirmed null at reference parity**: every top-rung body fails in ardopcf too, at every trial centre (4 of 11 never acquired; the corrected centre grew the sample to 17 16QAM bodies, still zero) - weak one-sided copies, not a decoder gap, exactly the design doc's §3.6 thin-margin prediction. Genuine per-frame disputes net 9-4 in our favour. **Not proven: any live ARQ session exercising the new ConAck acceptance** (the corpus is RXO; the majority-less ConAck path has bench and wild-replay evidence only), and nothing here touches transmit.

- **2026-08-08 (later) - ARDOP joins the mask discipline: the sim seam lands and workstream 0's recorded gap closes.** The campaign's A3 leg (docs/ardop/plan.md) gave ARDOP the maskable sim baseline every other non-FM mode already had. `ArdopFrameProbe` is the `DatacPacketProbe` pattern applied to the session TNC: the library's own modulator renders a full-capacity even data frame, the shared Watterson rig injects the channel (same `SimChannel.Apply`, same deterministic seeds), a fresh default-constructed demodulator (ardopcf-default squelch 5 / ±100 Hz) decodes, and the score is payload byte-exact - driven as `sm-ota sim --mode ardop:<FrameName>` through the same `SimBench.RunPoint` the mask rows call, so a mask number and a CLI number are one measurement. The wild-relevant six-frame ladder measured at N=100: AWGN knees at ~-5 (4FSK.200.50S), ~-2.5 (4PSK.200.100S), ~-1.5 (4FSK.500.100, CFO-flat to 50 Hz), ~+1.5 (4PSK.500.100), ~+10 (8PSK.500.100) and ~+11 dB (16QAM.500.100); fading anchors per type in the plan's table. Pinned: 8 blocking smoke rows (N=25, floors 2-3 binomial sigma under measured) and 23 SM_MASK_GATE full-tier rows in `WattersonMaskTests`; the suite's coverage-rule paragraph and rx-roadmap workstream 0 both record the closure. Two measured truths worth the ink: **the top rungs are unusable on fading channels** (8PSK/16QAM decode essentially nothing under CCIR Moderate/Poor at any plausible NVIS SNR - the wild zero-of-eleven was overdetermined: weak copies AND a channel class the waveform cannot carry), and **the single-shot ceiling is ~98 %, not 100 %, and it is the receiver** (certain noise realisations false-trigger the leader detector during the lead-in and the capture swallows the frame; seeds 18/27 fail at every AWGN SNR +10..+25 and decode at +40; ARQ retries absorb it in deployment; a leader re-arm improvement is a named future receiver leg). **Not proven: nothing here is on-air evidence** - the masks pin the receiver against the model, and the wild corpus (A0-A2) stays the truth about the band; Rung 5 remains outstanding.

- **2026-08-08 (later2) - bpsk300's symbol clock is measured to be at its own ceiling: perfect timing is worth nothing, and rx-roadmap workstream 4's ~0.5 dB timing claim is retired.** Sized before building, as workstream 3 was. The instrument is `sm-ota oracle` over a new bench seam (`BpskDemodulator.SymbolInstantObserver` / `SymbolInstantSchedule`, `TimingOracleSeamTests`): the Watterson rig draws its fades from the burst seed BEFORE its noise, so the same seed at infinite SNR replays the identical channel with the noise left off, and a pass over that noise-free copy records the sample index of every symbol instant for the measured arms to decode the NOISY copy at. The DPLL keeps running in every arm - transitions, `PacketDcd` and so the deframer's gating held constant - so the arms differ in one variable, when each symbol is read. Four arms: causal (deployed), oracle (noise-free instants), grid (a perfect constant-rate clock at the noise-free phase - the transmitted symbol grid, and the best a perfectly smoothed non-causal estimate could ever be), and a control slipped half a symbol which **decoded 0 of 200 at every rung of 5600 bursts**, which is what licenses reading a null off the rest. Measured single-branch bpsk300, N=200/rung, seed 1 (docs/bench/timing-oracle-2026-08-08.txt): pooled **causal 2774, grid 2763 (-0.2 points), oracle 2591 (-3.3 points)**; at the AWGN knees the perfect clock buys +3 and +6 frames against a ~70 frames/dB slope, i.e. **under 0.1 dB**. Two mechanisms recorded: at 40 samples per symbol behind an RRC matched filter the timing-error slope is gentle enough that DPLL jitter costs under a tenth of a dB, and on a fading channel there are two candidate true clocks (the transmitted grid, and the composite's instantaneous group delay as paths swap dominance) so sampling perfectly at either loses to the DPLL's inertia-limited compromise - the inertia IS the smoothing a second pass would be trying to discover. A prior suspicion is withdrawn with it: DPLL timing wander during dominance swaps is a symptom of the fade, not an independent cause of Poor's ceiling (a perfect clock rescues none of those frames). A lead is recorded rather than built: the hindsight arm (decode at all 40 phases, count if any delivers) reads 70/100 vs 34 at AWGN -5 dB, which is selectable diversity rather than an oracle since a frame passes IL2P RS+CRC or it does not - but the deployed 9-branch bank already scores 56/88/95 at -5/-4/-3 dB on the same seeds, so at most ~14 and ~7 points remain at the knees before overlap, against a proportional rise in false-accept exposure. **Nothing in the receive path changes behaviour**: the seam is inert unless a schedule is set, the bpsk300 sim rows reproduce main's recorded 56/88/95 exactly, and the full mask tier is green (65/65). An instrument fault found en route is on the record because it looked like physics: the first displacement profile read exactly 0/100 across its negative half, which was a schedule stalling on entries that fell before the first sample, not a receiver that could not decode; the matcher now steps over passed instants and a test pins it.

- **2026-08-08 (later3) - the FM modes get a channel at last: a physical FM link model, the first FM ladders, and five blocking FM masks.** rx-roadmap workstream 6 item 6, and OFDM-AB's O1. `FmChannel` (tests/Channel, linked into sm-ota like the Watterson rig) does not approximate FM with a curve: the modem's audio is frequency-modulated onto a carrier, complex noise is added *there* at a stated carrier-to-noise ratio, and a limiter and discriminator bring it back, through the radio's real audio paths (microphone pre-emphasis and 300-3000 Hz passband, or a flat data port) and its IF filter. Every FM impairment therefore emerges rather than being asserted, and `FmChannelTests` pins them as physics: the output falls away faster per dB below the threshold knee than above it; discriminator noise power rises with the square of audio frequency and de-emphasis flattens it; under-deviation costs output SNR; a microphone path will not pass a 4 kHz tone; flutter drags a link under threshold at a mean CNR a static link survives. The axis is **CNR in the receiver IF bandwidth**, deliberately NOT the SSB modes' SNR3k - the two are different quantities and a number moved between them would be wrong, so `--channel fm-mic|fm-data` says so in its help and the masks say so in their failure message. **First measured FM ladders** (N=50, seed 1, TXDELAY 150 ms, 50 % knees): `afsk1200-il2p` +8 dB through mic and speaker and +7.5 through a data port; `fsk9600-il2p` +10.5 (data port, 25 kHz); `c4fsk9600` +19.5 narrow and +17.7 wide; `c4fsk19200` +16.5 (25 kHz). Three findings worth the ink. **(1)** The model reproduces, unprompted, the reason every 9600 packet station needs a data port: `fsk9600-il2p` through a microphone path decodes **0 of 50 at +15, +21 and +27 dB** - not a threshold, a structural impossibility, and it is now a blocking test. **(2)** Channel spacing is worth 4.5 dB and is part of the mode: `fsk9600` first measured a ~15 dB knee on a narrow channel before being corrected to the wide channel it is deployed on (Tom's ground truth, 2026-08-08: C4FSK 9600 is the narrow-channel mode, C4FSK 19200 the wide one, per MMDVM-TNC and NinoTNC practice); mode-modulation-reference.md now carries a channel column, and `c4fsk9600` is measured to pay ~1.8 dB for fitting into narrow. **(3)** A noiseless FM link is not arbitrarily clean - full deviation on a low tone spreads sidebands past a narrow IF filter and symmetric truncation returns as odd-order distortion (third harmonic ~-25 dB on 8 kHz), which is the ceiling a dense constellation meets before it meets noise. **Masks**: five blocking FM rows plus the microphone-path negative; smoke tier still ~40 s; full tier green. One methodological correction paid for by CI: the first cut pinned `fsk9600` at +12 dB CNR, which is the steepest part of an FM cliff (22 % at +9, 90 % at +12, 100 % at +15). It passed locally and failed on the runner, because a mask row on a cliff measures the machine's floating point as much as the modem - FM curves are far steeper than the SSB ones these masks were designed around. Every FM row now sits above its knee on the flat, and the knees live in the ladder tables where they belong. **Not proven: none of this is calibrated against a real radio.** The numbers are the model's own, so an FM mask means "no regression against the model"; tying them to hardware is the `sm-ota ladder` FM route over the Flex/RSP1 rig, and that leg has not run. Nothing transmitted changed, and no receive path was touched.

- **2026-08-08 (later4) - NinoTNC v3/4.43's switch map corrects our catalogue, and in doing so exposes a real defect in the day-old FM channel model.** Tom supplied the v43 release notes; they are ground truth and we were wrong in three places (all now fixed in docs/mode-modulation-reference.md, which gains the full v43 switch map with Nino's occupied-bandwidth figures). **(1) QPSK 3600 is switch 0101 alone, on a 1650 Hz carrier, and Nino groups it with the FM AFSK modes as a speaker/mic mode** - our table gave it 2079 Hz and "0001, 0101", conflating it with C4FSK 19200 (switch 0001 alone). `ModemCatalog` was already right at 1650 Hz; only the documentation was wrong. **(2) The shaped PSK modes (300/1200 BPSK, 600/2400 QPSK) are documented as suitable for SSB radios *or FM radios*** - we listed them SSB-only, and the FM channel model still refuses them for want of a deviation figure, now a recorded gap. **(3) Nino's OBW figures independently confirm the narrow/wide split**: 10 kHz for 9600 C4FSK and 4800 GFSK, 20 kHz for 9600 GFSK and 19200 C4FSK. **The defect the notes exposed:** the release notes predict 3600 AQPSK works through a microphone and speaker, and our model said it could not decode through one at any SNR - including 0/20 at +40 dB CNR, which is a path fault, not noise. A multitone probe found the microphone path tilted 15 dB across 500-2800 Hz: `FmChannel`'s pre-emphasis was a plausible-looking shelf with a separately-derived coefficient rather than the algebraic inverse of its one-pole de-emphasis, so the pair never cancelled. AFSK shrugged the tilt off, which is why every AFSK measurement looked healthy and nothing caught it; QPSK, having no equaliser, could not. Fixed by making the pair exactly inverse (same coefficient, de-emphasis applied at the modem's rate so it cancels precisely), and pinned by the test that was missing, `Emphasis_Round_Trips_Flat_Through_A_Matched_Pair` - measured with a multitone, because peak-normalised drive cancels a single tone's pre-emphasis and would measure the instrument rather than the channel. Consequences measured: **qpsk3600 now decodes through mic and speaker, 25/25 at +18 dB CNR** (NinoTNC's claim corroborated by our own decoder, and now mask-pinned on both fm-mic and fm-data), and every microphone-path number improved - `afsk1200-il2p` on fm-mic went 24/25 to 25/25 at +15, its mask floor rising with it. Also recorded from v43 for the capture campaign: pre-v43 firmware sometimes sent the first frame after an ID beacon **with no preamble**, which is structurally unacquirable and a candidate explanation for isolated corpus misses; v43 adds a **Morse ID for the HF modes**, so CW in an HF slot is now expected; and v43 widened the 300 AFSK transmit waveform and changed AFSK demodulator 1's correlator gain, so any NinoTNC interop A/B should record the firmware revision. **Open and not resolved: qpsk3600's 5.0 kHz deviation figure is disputed** - it appears inherited from the C4FSK 19200 row, a speaker/mic mode at 5.0 kHz on a narrow channel is over-deviated, and it measures that way (83-87 % plateau even at +40 dB CNR against 100 % at 3.0 kHz). The sim keeps the recorded 5.0 kHz because a calibration figure should not move on inference; it needs confirming with Nino before anyone transmits the mode.

- **2026-08-08 (later5) - a correction to the entry above, and a new open defect it uncovered: qpsk3600 decodes WORSE as the signal gets stronger.** The previous entry claimed qpsk3600's 5.0 kHz deviation figure "measures over-deviated", citing an 83-87 % plateau at +40 dB CNR against 100 % at 3.0 kHz and attributing it to IF truncation. Tom challenged it ("could just be your implementation?") and he was right: **the mechanism claim is falsified.** Controls: widening the IF does NOT fix the 5.0 kHz case (it gets worse - 90 % at +24 falling to 77 % at +40), and qpsk3600 degrades at high signal on a linear AWGN channel too, where no IF filter exists. The deviation figure remains doubtful **on provenance alone** (inherited from the C4FSK 19200 row it was wrongly sharing; 5.0 kHz is a wide-channel deviation for a mode Nino groups with the speaker/mic modes) and still needs Nino. **What the controls turned up instead is a real and previously unseen defect.** On the FM channel through a data port - flat audio, no emphasis, wide IF, so neither truncation nor the microphone path is involved - `qpsk3600` scores 30/30 at +18 dB CNR and then 73 %, 77 %, 70 % at +24, +30 and +40. On linear AWGN the same shape appears in miniature: 99/97/95/94/97 % across +15/+20/+25/+30/+40 at N=100. Four things ruled out by measurement: **not the channel model** (afsk1200-il2p and c4fsk9600 both 25/25 at +40 dB CNR on the same path); **not the shared QPSK code** (qpsk600 and qpsk2400 both 25/25 at +20/+30/+40 on AWGN); **not acquisition** (TXDELAY 150, 300 and 600 ms all give exactly 21/30, and the same frames fail each time); **not deviation or truncation** (wider IF is worse). That leaves the mode: qpsk3600 is the only catalogue mode at a non-integer 6 2/3 samples per symbol, and docs/qpsk/plan.md already recorded one clean-signal regression peculiar to it at that ratio. Hypothesis, untested: noise dithers a coarsely-quantised sampling instant and a clean signal does not. **Operationally this is the regime the mode is most used in** - a strong local signal is the normal case for FM packet - so it deserves its own leg, and the timing-oracle seam from workstream 4 is the instrument for it. Also settled this round: **afsk1200 is a narrow-channel mode**, justified rather than assumed - Bell 202's 1200/2200 Hz tones at 3.0 kHz deviation give a Carson bandwidth of 10.4 kHz, landing in Nino's own 10 kHz OBW bucket alongside 9600 C4FSK and 4800 GFSK, both of which fit 12.5 kHz. The sim already modelled it narrow; now it is on the record why.

- **2026-08-08 (later6) - the qpsk3600 high-signal anomaly is explained: a non-integer 6.667 samples per symbol, with QtSoundModem showing both the mechanism and the fix.** Tom pointed at the QtSoundModem source (GPLv3, the sanctioned cross-check per CLAUDE.md). Its `ax25_demod.c` settles it. **QtSoundModem never lets its symbol sampler see a fractional samples-per-symbol ratio**: `make_core_INTR` interpolates each PSK mode by `n_INTR = baud/300` - **6 for SPEED_Q3600** - and `decode_stream_QPSK` then hardcodes `baudrate = 300`, so every PSK mode reaches the decision point at exactly 40 samples per symbol. Its sampler is a different design from ours as well: an exponentially averaged envelope per position-within-symbol (`bit_buf[sample_cnt]`, 0.95/0.05) whose peak position selects the decision sample, an estimator that *improves* as noise falls - the opposite of what we measured. Our `qpsk3600` runs natively at 12 kHz, i.e. **6.667 samples per symbol**, and it is the only catalogue mode with a non-integer ratio; `qpsk600` (40), `qpsk2400` (10) and `bpsk300` (40) are integers and are exactly the modes that show no effect. **Confirmed by direct measurement** (AWGN +40 dB, N=30): 28/30 at 12 kHz (6.667 sps), 29/30 at 10.8 kHz (6.000), **30/30 at 36 kHz (20.000)**; and on the FM path where the effect is severe, 20 samples/symbol removes it entirely - **30/30 at both +24 and +40 dB CNR against 26/30 and 21/30 at 6.667**. **The fix must be internal interpolation in the receive path, not a catalogue rate change**: no divisor of the 48 kHz soundcard rate yields an integer samples-per-symbol at 1800 baud (48000/1800 = 26.667), which is presumably why the mode sits at 12 kHz. Interpolating by 3 (20 sps) or 6 (40 sps, matching QtSoundModem) inside `QpskDemodulator` is the change; **not built, and no mask moves until it is**. Recorded because it is operationally real: a strong local signal is the normal case for FM packet, so this is the regime the mode is most used in, and the mode was validated at 30/30 on clean audio in the QPSK campaign precisely because that campaign never measured it above +18 dB.

- **2026-08-08 (later7) - qpsk3600's high-signal anomaly is FIXED: the differential decode chain now runs on a whole number of samples per symbol, and the mode improves everywhere measured.** Tom: "land the full fix, nobody uses pdn-soundmodem yet". `QpskDemodulator` upsamples its differential decode chain by the smallest whole factor that makes samples-per-symbol an integer - **3 for both of qpsk3600's rates** (6.667 -> 20 at 12 kHz, 26.667 -> 80 at 48 kHz) - which is QtSoundModem's own answer arrived at from the other end (`make_core_INTR` interpolates by `n_INTR = baud/300`, 6 for SPEED_Q3600, and `decode_stream_QPSK` hardcodes 300 baud so its sampler always sees exactly 40 samples per symbol; ax25_demod.c, GPLv3, the CLAUDE.md cross-check). Band-pass and energy detection stay on the input grid - they answer "is something there" and need no sub-sample resolution - so upsampling costs the decode chain only; coherent detection is untouched as the cross-check variant. **Measured A/B**: AWGN +15/+20/+25/+30/+40 dB goes 99/97/95/94/97 -> **100/100/100/100/100** at N=100 (the droop eliminated); fm-data +24/+30/+40 goes 22/23/21 -> **30/29/30** of 30; and the knee improves too - **AWGN +8 dB 57/100 -> 73/100** - so the coarse grid was costing sensitivity everywhere, not only at high signal. fm-data and fm-mic at +15 and +18 all reach 50/50 from 48-49/50. Unchanged: the three real recordings decode in full (NinoTNC hardware 3/3, QtSoundModem 10/10, our own 10/10), and qpsk600 (40 sps) and qpsk2400 (10 sps) keep a bit-identical sample path. **Masks**: the qpsk3600 fm-data floor rises 20 -> 21 of 25, and a **high-signal row is added** (fm-data at +30 dB CNR, floor 21/25, measured 25/25 and 19/25 before the fix) - the suite had no high-signal rows at all, which is exactly why a mode that decodes worse as the signal improves went unseen: every mask sat near a knee, where such a defect looks healthy. Two process notes worth keeping: the first attempt at this change silently did nothing to two lines because a `str.replace` pattern had gone stale, giving a chain that ran its filters at 36 kHz and its one-symbol delay ring at 6 samples - 0/30 everywhere, which at least fails loudly; and an earlier partial fix (interpolating the decision instant, PR #268) is retained only as the fallback for a hypothetical mode where no small whole factor works.

- **2026-08-09 - a station can run a modem this repository does not contain, and OFDM-FM is the first: the plugin seam lands, and the OFDM core moves out.** Two things, one because of the other. **(1) Runtime modem binding** (`docs/modem-binding.md`, designed 2026-08-08, built now): `IModemPlugin` + a public `ModemDescriptor`, a `ModemPluginRegistry`, and a `ModemPluginLoader` that loads an assembly from an explicit path into its own collectible `AssemblyLoadContext`. `modemPlugins` in the daemon config names the paths; nothing is scanned for, because a file appearing on disk must never change what a station transmits. Plugin modes are `pluginId:mode` prefixed, so `ofdm-fm:nb` can never shadow a built-in and an uninstalled plugin says "no modem plugin registered for 'ofdm-fm'" instead of denying the mode exists. **Registered modes deliberately do not join `KnownModes`** - that set is what this repository is answerable for and what five whole-catalogue sweeps iterate, including the pinned count; `AllModes` is built-ins plus registered. Which is why **this ledger's matrix has no OFDM-FM row and should not grow one**: the matrix tracks `KnownModes`, and a mode whose waveform is not in this repository is not a mode this repository can vouch for. **(2) The OFDM core moved to the private `packet-net/pdn-ofdm-fm`** and became a real `IModem` there. Not a licence problem, a buildability one: a public GPL work has to stay buildable and distributable by anyone who clones it, so it cannot contain or build against something we are not free to ship, and OFDM-FM's profiles were sized against IP400 parameters we are not free to publish. **Verified rather than assumed, four ways.** A fresh clone of this repository, with no plugin and no geometry file present, builds and passes 1748 tests - which is the entire point of the exercise. The rule that makes the load context work is pinned by a test that walks the contract's public surface and fails if it grows an assembly the host does not provide (it already has one beyond the obvious: `FrameQuality.HeaderType` is an `M0LTE.Il2p` enum, and a plugin's own `.deps.json` would happily resolve a private copy from the NuGet cache); deleting the shared-contract rule fails eight tests, so they are measuring it. With the plugin staged in a directory outside both repositories the daemon loads it, logs its four modes, and comes up on `ofdm-fm:nb` at 48 kHz; with it absent it prints one named failure and either starts anyway or refuses by name if a modem wanted one of its modes. And two regressions against what the OFDM campaign recorded, because a move is exactly when a waveform quietly stops being the same waveform: the coded FM ladder reproduces its table (uncoded 8/8/8/2/0/0 identical, rate 1/2 one frame better at +20 dB on different seeds, so the 5-6 dB coding gain holds), and all 32 transmitted sample bursts - four profiles by eight constellations - come out at exactly the durations the 2026-08-08 set had, which pins the geometry, the header sizing and the framing. **The real work in the plugin was the streaming adapter**, and it is worth naming because the obvious implementation is wrong: the burst codec is whole-buffer, `IModem.Process` is a stream, and accumulating the stream to re-run a whole-buffer search over it is quadratic in the search and unbounded in the buffer. It runs the same self-correlation metric incrementally, tracks the plateau to its peak, and calls the codec's own `DecodeAt`; two tests hold it honest - one asserts the streaming search times a burst at the *same sample* the whole-buffer search does, and one feeds twenty seconds of noise and asserts the buffer never grows (deleting the compaction fails it). **Not proven, and the reason there is no status here to upgrade: no OFDM-FM mode has masks in either tier, no ladder row blocks CI, and none of it has been on the air.** What landed is reachability - a station *can* run it - not a validated mode. `docs/ofdm-ab/plan.md` O4 is retitled accordingly.

- **2026-08-09 (later) - the FM channel model had a rate-dependent defect: its filters are specified in hertz now, every FM mask was re-measured through the fix, and none of them moved.** `FmChannel` was extracted to the public `M0LTE.FmChannel` package (#274) so this repository and the private OFDM one share one definition rather than a copy; the extraction found the defect and 0.3.0 fixes it. **Every filter in the model used a fixed tap count.** A windowed-sinc's transition width is roughly `rate/taps`, so a fixed count makes a filter twice as sloppy each time the sample rate doubles - which is not how a radio behaves, its audio filters being analogue and no wider because a sound card samples faster. The consequence was that the same waveform measured at two sample rates went through two different channels, with the higher rate penalised, so no measurement at one rate could honestly be compared with one at another: a bad property in an instrument whose entire job is comparison. Filters are now specified by shape and the tap count derived at whatever rate the stage runs at, across all four rate-dependent stages (IF filter, audio band limit, and both resampler anti-alias filters; the flutter generator's own filter already ran at a fixed low rate of its own). For a 300-3000 Hz microphone path that is 119 taps at 12 kHz where it was 127, 239 at 24 kHz and 475 at 48 kHz - the 12 kHz case barely moving is the shape you want, the sharpness being the same in hertz everywhere instead of accidentally best at the lowest rate. **Measured on the audio-band OFDM waveform**, 16 seeds a rung: before, 24 kHz scored 16/16/16/16/14/4/0 against 48 kHz 16/16/16/16/13/4/0 across +28 down to +10 dB CNR; after, 24 kHz 16/16/16/16/14/2/0 against 48 kHz 16/16/16/16/13/2/0. The two rates now agree within the noise of 16 seeds, which is what a rate-independent model has to do. **Every mask re-measured: the full `SM_MASK_GATE=1` tier is 74/74 green through 0.3.0 and no row moved in either direction**, so the five FM rows and the qpsk3600 high-signal row keep the floors the entries above set them. That is the outcome worth recording rather than a set of new numbers: the modes most exposed to the change are the 48 kHz ones (`fsk9600-il2p`, `c4fsk9600`, `c4fsk19200`), and they were unaffected. The masks are still the model's own numbers, so nothing here moves any mode toward on-air validation. **One instrument sensitivity found and left alone, because it is faithful rather than wrong**: the model calibrates drive from the burst's own peak, the way a bench does, so a waveform's peak-to-average ratio feeds back into its effective deviation and two bursts of different length or content are not driven identically. It is real behaviour, but it means a code-rate A/B on a single burst length is not a clean single-variable comparison, and it explained a puzzling 8-of-8 acquisition failure in the OFDM ladder that had nothing to do with the receiver.

- **2026-08-09 (later2) - OFDM-FM's channel estimate is denoised, and two plausible improvements measured nothing and were reverted.** In `packet-net/pdn-ofdm-fm`, recorded here because the negative results are the durable part and the private repository's `docs/receiver-findings.md` carries the detail. **What landed**: the preamble-derived channel estimate is one noisy division per carrier and it is that receiver's dominant impairment, since equalisation divides by it and the waveform always has carriers 20 dB down on the edges of a voice passband. The estimate is transformed to a delay profile, each tap shrunk by how much of it is signal rather than noise, and transformed back - the channel being a handful of filters, its energy sits in a short run of delays, while noise is spread over all of them. Measured 8 seeds through the FM link, both legs through FmChannel 0.3.0: uncoded 8/8/7/2/0/0 -> 8/8/8/5/0/0, rate 2/3 8/8/8/8/8/3 -> 8/8/8/8/8/7, rate 3/4 8/8/8/8/3/0 -> 8/8/8/8/7/0 across +40 to +16 dB CNR, with no rung going backwards. Two details are load-bearing and both were got wrong first: it must transform the occupied bins only (inventing values for the unoccupied ones rings, and rang hard enough to break QAM-256 on a *noiseless* loopback while QPSK never noticed), and the bulk delay must be removed before the transform and restored after (the burst is not sampled where the channel's response peaks, so the estimate carries a phase ramp of non-integer slope, and a non-integer ramp does not transform to a spike - it transforms to a sinc smeared over every tap, at which point the shrinkage eats the channel; it showed as a loss at *high* CNR, where there is nothing to denoise and only the distortion is visible). **What did not land, both built and measured before being reverted.** *Decision-directed estimation*: when a payload fails its CRC, slice every equalised carrier of every symbol to its nearest constellation point, average the residual over all of them (pilots free, being known exactly), fold it into the estimate and decode again - in principle beating a single-symbol preamble estimate by roughly the square root of the number of symbols. It won **0 of about 30 attempts**, across the plain ladder and a flutter ladder at 2, 5, 10 and 20 Hz Doppler; by the time a payload fails it is not marginally wrong, so slicing gives a reference too corrupt to help, and the cases a better estimate would have rescued are already taken by the denoiser, which is cheaper and runs on every burst. *Lowering the sync threshold*: the whole rate 3/4 rung at +16 dB was failing acquisition outright (worst normalised correlation 0.695 against a 0.8 threshold), which makes the threshold look like the binding constraint - but sweeping it to 0.6, 0.45 and 0.3 changes the ladder in **no cell at all**, acquisition succeeding and the payload failing instead. It was masking the failure, not causing it. **The finding worth acting on next** is the failure budget under flutter, counted by the stage that lost each frame: at 20 Hz Doppler and +20 dB CNR, of 14 lost frames **7 are the header**, 4 sync and 3 payload. The header is the only part of the burst with no coding on it - 40 uncoded bits, hard sliced, behind their own CRC - while the payload underneath survives conditions it does not. That is a waveform change rather than a receiver one, and it is now the largest single lever. **Still not proven, unchanged from the entry above: no OFDM-FM mode has masks in either tier and none of it has been on the air.**

- **2026-08-09 (later3) - OFDM-FM's header is coded and spread over two symbols, and the fix was decided by measuring the header on its own rather than by reasoning about it.** In `packet-net/pdn-ofdm-fm`; recorded here because the method is the transferable part. The entry above named the uncoded header as the largest single cause of lost frames on a fading link and called it the next lever. It was, but **not for the reason the name suggests**, and the instrument that settled it is the point: a header ladder that hands the receiver the sync position and counts headers read correctly out of bursts actually acquired, so acquisition and payload cannot contaminate the number. Measured that way the old header read **64 of 64 at every carrier-to-noise ratio where the burst could be acquired at all with the flutter off**, and 18 of 40 at +20 dB with 20 Hz of Doppler. It was never noise limited; it was fade limited. The flutter is a flat fade on the RF carrier, so all 110 carriers go down together, and a header living inside one symbol has no diversity of any kind to exploit when that symbol is in a fade - no code, however strong, changes that. What a fading channel pays for is diversity order, and the only axis with any left is time. **So the header is now tail-biting convolutional K=9 at rate 1/2 (40 bits in, 80 out), soft-decision Viterbi, laid across TWO header symbols** by a stride coprime to the coded length so every coded bit appears in both. The sizing was done before building: adjacent symbols are 45 ms apart at this geometry and the model's Gaussian Doppler shaping puts the fade correlation between them at 0.13 at 20 Hz, 0.60 at 10 Hz and 0.88 at 5 Hz, so the diversity lands where the header was broken and is barely needed at 5 Hz where it already read 44 of 46. Two details are load-bearing: the soft metric weights each carrier by `|H|^2` as before **and now each symbol by its own pilot-measured amplitude** (two symbols carrying copies of a bit are two different fades, and without that the faded one gets equal say and most of the second copy's value is discarded), and the bit-to-slot mapping walks all header symbols as one run of slots rather than assuming exactly two - the first version left most of the code untransmitted on profiles narrow enough to need three. **Measured end to end**, flutter failure budget, same instrument and seeds both legs, every burst counted once in the stage that lost it: at 20 Hz Doppler, +20 dB CNR **2/16 recovered becomes 8/16** (header bucket 7 -> 3), +24 dB 8/16 -> 12/16, +28 dB 13/16 -> 16/16; at 10 Hz +20 dB 7/16 -> 9/16; at 2 Hz +20 dB 10/16 -> 13/16. Over all twelve cells **134 frames recovered becomes 155 and the header bucket falls from 16 to 6**. The payload bucket falls too, 25 to 16, which is not a payload change but the consequence of ordered buckets: bursts that used to die at the header now reach a payload that is coded and interleaved over ten symbols and was always the more robust half, so a fragile header had been discarding bursts that were perfectly decodable underneath it. **Costs**: one extra symbol on every burst, which on the plain non-fading ladder shows as the uncoded row losing two frames at +24 dB and rate 2/3 gaining one at +16 - the channel model calibrating drive from the burst's own peak the way a bench does, so a longer burst has a higher expected peak and marginally less average deviation. **Two tests were passing for the wrong reason and are fixed rather than deleted**: both claimed to wreck samples "past preamble and header, into the payload symbols" and both computed that offset as a hand-counted symbol index which was in fact the header symbol; they passed because wrecking the old header killed the burst, and the fault only became visible because the new header survives one wrecked symbol, which is exactly what it is for. **What this does not settle**: sync is now the largest bucket (15 lost frames against the header's 6) and the recorded threshold sweep warns that acquisition failures at the bottom rung were masking payload failures rather than causing them, so that is the next leg and it needs its own instrument first. **Still not proven, unchanged: no OFDM-FM mode has masks in either tier and none of it has been on the air.** Worth stating plainly for anyone reading the Doppler figures: the axis is Doppler *spread*, not Doppler *shift* - a carrier offset is irrelevant to an FM link, which turns it into a DC offset well below the waveform's lowest subcarrier at 305 Hz. What is being modelled is mobile flutter, the rate at which a moving station's multipath pattern sweeps past, `v/lambda`: 6.3 Hz for 13 m/s at 145 MHz and 19 Hz at 433 MHz, 14.5 and 43 Hz at 30 m/s. So the rows that were broken are a station driving, and the 0 Hz row - fixed antennas, base to base - is precisely where the old header was already perfect.

- **2026-08-09 (later4) - a correction to the entry above: the header result holds at 64 seeds and is stronger than stated, but one of its claims was noise and is withdrawn.** The header change was landed on a 16-seed budget, which was enough to see the effect and not enough to quote it properly. Both legs were re-run at **64 seeds a cell**, in a git worktree of its own so that nothing else building in the checkout could contaminate them, with two-sided Fisher exact tests against every bucket. **What holds, and is now stated in the right place: the header bucket falls from 53 of 768 to 25, p = 0.0016.** That is the direct measure of the thing that was changed and the only strongly significant number in the set; frames recovered follow it, 580 of 768 to 616, p = 0.031. Per cell at 20 Hz Doppler: +28 dB 50/64 -> 56/64, +24 dB 37/64 -> 46/64, +20 dB 12/64 -> 23/64; at 10 Hz +20 dB 32/64 -> 40/64. **What is withdrawn: the claim that the payload bucket also fell.** The 16-seed run showed it going 25 to 16 and the entry above explained that as bursts which used to die at the header now reaching a payload that decodes. It was noise and the explanation was a story fitted to it - at 64 seeds the payload bucket is 82 against 87, unchanged (p = 0.74), and the ordering of the buckets in fact predicts a slight RISE, because bursts that newly survive the header at 20 Hz are precisely the ones most likely to fail the payload. The general lesson is worth the ink: a bucket that moves the way your story predicts is not evidence for the story until it moves further than noise would, and buckets that are ordered rather than independent will always tempt one into a narrative. **A second methodological point, made against my own write-up:** the headline quoted above and in PR #276 was a single cell, 2 of 16 against 8 of 16, and that cell did not clear significance at 16 seeds at all (p = 0.054). It does at 64 (p = 0.047), but it was the weakest evidence in the set and it was put at the front of the argument; the aggregate and the header bucket were always the stronger claims and should have led. **Also now visible with the seeds to see it, and honest about cost:** the extra symbol is paid for where the diversity is worth least. At 5 Hz Doppler the two header symbols are 0.88 correlated so there is little to gain, and that is the only column that goes backwards - 50 of 64 recovered becomes 47 at +20 dB and 60 becomes 58 at +28. Neither is significant alone (p = 0.68 for the first) but the sign is what the theory predicts, so it is probably real and small. **Unchanged: no OFDM-FM mode has masks in either tier, none of it has been on the air, and no mask in this repository moves.** The seed count is now an environment variable (OFDMFM_SEEDS) so the deep run reproduces without an edit.

- **2026-08-09 (later5) - a second and larger correction to the header entries above: the coding was the whole win and the time diversity was worth nothing, which two dated entries got backwards.** Tom, reading the write-up: "I highly doubt that this modem will ever be used for mobile operation tbh. Seems an odd thing to optimise for." He was right, and measuring it properly says so more strongly than the challenge did. **The change did two separable things and the entries credited the wrong one.** It CODED the header (tail-biting convolutional K=9 rate 1/2, 40 bits in, 80 out, soft Viterbi), which costs **no air time at all** because 80 coded bits fit inside the single symbol the uncoded header already used on every real profile (110 data carriers at the narrowest). And it REPEATED the header into a second symbol for time diversity, which costs a whole symbol, about 8 % of a short burst. Three arrangements measured at 64 seeds a cell over fifteen cells from no flutter out to 20 Hz Doppler, 960 bursts each: **uncoded one symbol 772/960 recovered (53 lost to the header, 82 to the payload); coded one symbol 806/960 (32, 79); coded two symbols 808/960 (25, 87).** Uncoded to coded is +34 frames, p = 0.049, for nothing. **Coded-one-symbol to coded-two-symbols is +2 frames, p = 0.95** - indistinguishable from nothing, for 8 % more air time. Every bit of the gain was the free part; none of it was the part the entire design argument rested on. **The mechanism was not wrong, which is what makes it worth the ink.** A flat fade does take one symbol and usually not the next, and the second copy does exactly what it was built to do: header losses fall from 32 to 25. It gains nothing overall because a burst 8 % longer is 8 % more exposed to the next dropout, so the payload loses as many frames as the header saves (79 becomes 87). It trades a header failure for a payload failure, roughly one for one. Air time on a burst that has to survive the same channel attacks the same quantity the benefit does, and nothing but an end-to-end measurement across the whole burst was ever going to show it. **The general lesson, which is the transferable part: a mechanism being real does not make it worth its cost, and a stage-level measurement cannot see a cost that lands on a different stage.** The header ladder was the right instrument for choosing what to fix and the wrong one for deciding whether the fix was worth it; the failure budget across all stages was the one that could tell. **What is in the code now**: coded header, ONE symbol, `OfdmFmParameters.HeaderRepeats` defaulting to 1. A fixed station - which is what this modem is for - gets the coding free and pays nothing for a fade it never sees; measured with no flutter at all every arrangement reads 64 of 64 at every level tested, which is the other half of the challenge: **the header was never failing for a fixed station, so there was nothing there to rescue**. The setting is kept rather than deleted only because the trade depends on payload length (at 64 bytes the extra symbol is 8 % of the burst, at 256 bytes nearer 2 %, and nobody has measured whether the header saving survives at that ratio), with a doc comment warning against raising it on the strength of the mechanism sounding right. **An instrument fault en route is on the record because it produced a table that looked like a result**: a three-legged comparison reverted a source file the tests still referenced, so the third leg's build failed with six errors and the runner silently re-used the previous binary, giving numbers byte-identical to the leg before it. Identical columns gave it away. Build error counts are checked now rather than glanced at. **Unchanged: no OFDM-FM mode has masks in either tier, none of it has been on the air, and no mask in this repository moves.**

- **2026-08-09 (later6) - a self-inflicted channel-model error, caught by the mask suite within the hour, and the genuinely useful finding underneath it: C4FSK 19200 cannot pass a Tait TM8100's IF at all.** Tom supplied a full TM8100/TM8200 documentation set (20 manuals, 1083 pages, at M0LTE/tait-tm8100-tm8200-docs) with the instruction not to trust its index but to go back to the markdown, which is what every figure below was read from. **The error first.** The service manual measures Tait's own total IF 3 dB bandwidths at **12.6 kHz wide, 12.0 kHz medium, 7.8 kHz narrow** (MMA-00005-05 issue 5, p.73, Table 3.1; 12.0/9.0/7.6 on the K5 band), against `FmChannel.IfBandwidthForSpacing`'s generic 16 kHz and 8 kHz. Since the carrier-to-noise axis is *defined* in that bandwidth, that looked like a citable 1.1 dB error in the instrument, and M0LTE.FmChannel 0.4.0 duly replaced the generic figures with Tait's. **That was wrong**: one manufacturer's measurements do not belong in a function whose argument is a channel spacing, and substituting them made the library less accurate for every radio that is not a Tait while looking more authoritative - the exact failure mode this ledger has recorded before, and the one the research brief for this very extraction warned agents against. **The mask suite caught it on the first full run**: `c4fsk19200` at fm-data +24 dB CNR went from its pinned 25/25 to **0 of 25**, a collapse rather than a degradation. 0.4.1 restores the generic figures and all 74 mask rows are green again with no row moved, so nothing in this repository's pinned numbers changed in either direction. **The finding underneath is worth more than the error cost.** The collapse was physically correct and is now measured rather than inferred: C4FSK 19200 occupies about 20 kHz (Nino's own OBW figure, recorded in the 2026-08-08 later4 entry), and a 12.6 kHz IF does not attenuate that, it truncates it. **So C4FSK 19200 will not work through a TM8100 on a 25 kHz channel at all, and by the same arithmetic neither will 9600 GFSK**, whose OBW is also 20 kHz. That is a property of the radio, not a defect in either mode, and it is an operational fact for anyone building a station on TM8100s - the fast NinoTNC modes are off the table on that hardware whatever is done to the modem. It also sharpens the case for the radio-model work Tom asked for: a caller who wants a Tait should ask for a Tait and get all its figures together, including the ones that say a mode does not fit, rather than a generic helper quietly carrying one radio's IF filter and nobody else's. **Also extracted from the set and recorded in docs/hardware/tm8100-cm108-interface.md (#279)**, all cited: 100% deviation is +/-2.5 kHz NB, +/-4 kHz MB, +/-5.0 kHz WB (MMA-00072-03 p.6), and Tait's own internal 1200 baud modem defaults to 60% of the class ceiling; the audio tap out is a 600 ohm source on a +2.4 V pedestal behind a 12 kHz Butterworth (MMA-00005-05 p.84, board values from MMAB12-B1-00-814); the tap in is 4k7 series on a +1.5 V internal bias with 10 nF at the connector; RSSI is specified over **-115 to -50 dBm** (MMA-00072-03 p.14) with the firmware carrying values to -119 dBm, and 12 dB SINAD is measured at -121 dBm for B1 narrowband, which together answer what a low RSSI reading can and cannot mean. **Two things the documents do NOT contain**, searched exhaustively and stated so that an empty search reads as an answer: no input-level-to-deviation figure at any tap in either direction, so drive must be bench-calibrated per station (the note gives a Bessel-null method needing only an SDR); and **no occurrence of "T13" anywhere in the 1083 pages** - the only tap-in points Tait document are r2, r5, t1 and t5, the only transmit tap-in ever configured in a programming form is T5, and Tait qualify their own transmitter response as "below limiting" for a tap-injected signal (p.480), so **the hard limiter is downstream of every documented transmit tap**. If a mode is to be injected at T5 it meets the 300 Hz high pass, pre-emphasis, that limiter and the 3 kHz low pass, which matters to any waveform whose lowest carrier sits near 305 Hz. **Unchanged: no mask moved, and none of this is on-air evidence** - it is documentation, and the bench tests that would settle the tap question are named in the interface note.

- **2026-08-09 (later7) - the claim in the entry above that C4FSK 19200 and 9600 GFSK cannot pass a TM8100's IF is WITHDRAWN. It was wrong twice over, and Tait's own product disproves it.** Tom: "Nonsense. The radio channel needs setting to wide, that's it. Think you need to read the manual again." He is right. **The disproof, from the same documentation set** (MMA-00038-06 p.9): "The over-air data rate is 1200 or 2400 bit/s for FFSK data, **12 kbit/s for Tait High Speed Data (THSD) narrow band and wide band, and can be set to 19200 bit/s for THSD wide band**." So 19200 bit/s over the air fits the 12.6 kHz wide-channel IF, and 12 kbit/s fits the **7.8 kHz narrow-channel** IF, in that same radio, as a shipped Tait feature. A 12.5 kHz channel carrying 12 kbit/s is 1.54 bit/s per hertz of IF, which is unremarkable for a well-designed mode and far beyond what the withdrawn claim said was possible. **Error one: the 9600 GFSK half was pure extrapolation, and the measurement disproving it was in the file being quoted from.** The claim was extended from C4FSK 19200 to 9600 GFSK "by the same arithmetic" with nothing measured. The 0.4.0 mask run had `fsk9600-il2p` at fm-data **passing 25 of 25 through the same 12.6 kHz IF**, one line above the failure that prompted the claim. **Error two, and the transferable one: comparing a 99 % occupied bandwidth against an IF 3 dB bandwidth and concluding a mode does not fit is not sound reasoning.** Occupied bandwidth includes low-energy tails, an IF filter has skirts rather than walls, and `FmChannel`'s IF filter is a windowed sinc with a transition of 15 % of its bandwidth, which is sharper than a crystal-plus-FPGA cascade. **So the `c4fsk19200` collapse at 12.6 kHz (0 of 25, against 25 of 25 at 16 kHz) is now a defect to investigate rather than a finding to report**, and the prime suspect is already on this ledger: the 2026-08-08 (later5) entry flags that mode's **5.0 kHz deviation figure as doubtful on provenance**, inherited from a row qpsk3600 was also wrongly sharing. At 5.0 kHz deviation and 9600 symbols per second Carson's rule gives 19.6 kHz, which is exactly the too-wide signal that dies in a 12.6 kHz filter, and Tait's own 19200 bit/s mode evidently does not occupy anything like that. **Nothing in the code changed and no mask moved**: 0.4.1 restored the generic bandwidths before any of this, all 74 rows are green, and the correction is to the conclusion rather than to any number. What the round leaves behind is a genuine open item - **verify c4fsk19200's deviation figure against NinoTNC before trusting any measurement of that mode**, since a wrong deviation makes every simulated bandwidth result about it meaningless - and one reusable warning, which is that a claim of the form "this mode cannot work on this hardware" contradicts whatever the manufacturer ships and should be checked against that before it is written down."

- **2026-08-10 - a deviation sweep across every FM mode: two modes are leaving 3 to 4 dB on the table, two are already right, one is not established, and qpsk3600 drops to 2500 Hz which is free and reduces its emission.** Fourteen agents, six modes swept on the FM channel model and each independently re-run by a second agent; verifier numbers are the ones quoted. **Measured, mode by mode**: `fsk9600-il2p` 2400 -> 5000 Hz is **+4.1 dB** (knees +9.89 to +5.76 dB CNR, N=400, reproduced bit-for-bit on an independent build and seam, +4.12 on an independent seed block, and it GROWS to +5.4 dB on a narrower 11 kHz IF); `fsk4800-il2p` 1200 -> 2500 Hz is **+3.4 dB** pooled over two 200-burst blocks, with the instrument calibrating against theory below stock (1000 -> 1200 Hz measures +1.62 dB against +1.58 predicted by the deviation-squared law); `c4fsk9600` and `c4fsk19200` are **already at their class ceilings and gain 0 dB in class**, every deviation below being strictly worse, which makes them the controls that prove the seam; `afsk1200-il2p` is **not established** because the recommendation's sign flips with the modelled IF bandwidth, +1.2 dB at 8 kHz against -0.6 dB at 10 to 12 kHz, and its own mask channel was never swept; `qpsk3600` 5000 -> 2500 Hz is **+0.4 dB at the knee and takes Carson bandwidth from 15.4 to 10.4 kHz on a 12.5 kHz channel**. **Only the last is applied.** It is a level on phase modulation of a 1650 Hz audio carrier so the far end normalises it, it REDUCES emission rather than increasing it, it brings a 200 %-of-ceiling mode onto its channel class for the first time, and the 5.0 kHz it replaces is the one figure in the table already doubted on provenance. All 74 mask rows stay green at the new deviation, including all three qpsk3600 rows. **The divergence is expressed as a divergence, not an edit**: `FmModeProfile` gains `RecommendedPeakDeviationHz` alongside `TargetPeakDeviationHz`, because the latter is a record of what NinoTNC publishes and overwriting it would falsify that record and leave nobody able to see that a disagreement had happened; a test refuses any divergence without a stated reason. The Bessel-null tone consequence is recorded with it: an operator following the documented procedure needs 1039 Hz, not 2079. **The two big wins are NOT applied and must not be until they have been on the air.** Both `fsk9600` and `fsk4800` sit at deviation = baud/4, which is minimum shift keying by design, and raising them moves the modulation index from 0.5 to 1.04 - a change to the signal's geometry rather than its level. Our receiver is a discriminator with an envelope-midpoint slicer and gains nothing from MSK orthogonality, which is exactly why we measure a gain; a NinoTNC's receiver is probably the same, but probably is not ground truth and the live network is. Nino's own position (#ninotnc-help, 11 July 2026) is permission to test rather than permission to ship: "more deviation will be more effective until you exceed the capability of the transmitter or receiver (IF filter)". **Four findings worth more than the numbers.** (1) **Carson's rule must not be used to pick deviation**: turnovers land at 1.0 to 1.9 times the modelled IF, never consistently at 1.0, and fsk9600's naive Carson limit of about 2800 Hz would have left the entire 4.1 dB unclaimed. (2) **AFSK 1200 fails by discriminator clicks, not truncation**: past its optimum the post-detection SNR keeps IMPROVING, 6.2 dB at 3000 Hz against 3.8 dB at 1750, while frame success falls, because error kurtosis rises from 9 to 19 and IL2P's Reed-Solomon is worst against runs - anyone reading only an SNR column picks the wrong deviation for this mode. (3) **The model has no adjacent-channel victim anywhere in it**, so it can only ever reward deviation until the wanted receiver truncates, and every out-of-class optimum found here (c4fsk9600 at 4500, c4fsk19200 at 9000, qpsk3600 at 3500) is uncosted for everybody else on the band and is not an available setting. (4) **On the two modes that have both levers, crest factor is the bigger one**: fsk9600 carries 3.2 dB of transmit pulse-filter overshoot, a defect rather than a property, on top of its 4.1 dB of deviation headroom and at constant Carson bandwidth - while AFSK 1200 on a data port measures 3.38 dB against a sine's 3.01 dB floor, so the OFDM crest precedent does not transfer to it at all. **An operational hazard for anyone calibrating**: a NinoTNC sets deviation with a Bessel-null sine at 3.01 dB crest, so if the tone and the data are emitted at equal RMS rather than equal peak, the data over-deviates by the difference, 4.1 dB on qpsk3600 and 2.9 dB on fsk9600. **Corrections to this ledger's own bookkeeping**: there are eight FM mask rows across five modes, not five rows, and `fsk4800-il2p` has no FM mask row at all, which is why its sweep had no external pin to check its seam against. **Still not proven: none of this is on-air evidence**, the model has no adjacent-channel term, and the two largest gains are explicitly held pending a NinoTNC A/B.

- **2026-08-11 - OFDM-FM negotiates its own rate, and the measurement campaign behind it says eleven of the 24 rates a header can name should never be sent.** In `packet-net/pdn-ofdm-fm`; here because the method and the negative results are the transferable part, and the "no matrix row for OFDM-FM" rule from 2026-08-09 stands. **What the wire gained.** The header already named the constellation and a receiver already used it, so one end could follow another from BPSK to QAM-256; the *coding* was not named, so the receiver decoded with whatever its own configuration said and both ends had to be set alike in advance, a mismatch looking exactly like a bad path. The coding index cost nothing: the length field was 20 bits and validated at 65535, so four of them could never be set. A recommendation for what a station would like to **hear back** cost 8 more, and the header is still one symbol on every real profile, so the whole of negotiation costs no air time at all. The change that mattered was not the bits but the burst sizing: a streaming receiver works out how long a burst is from its header before it has the burst, and sized from its own configuration rather than the signalled one it stops collecting part way through and hunts for sync inside the remainder. **The ladder, measured not derived** (`RateLadderGridProbe`, R1/T13 on a TM8100 narrow at 2500 Hz, 256-byte payload, K=9, coarse at 16 seeds to bracket each cliff then 64 on 1 dB steps): of the 24 constellation-by-coding combinations, **thirteen survive and eleven are dominated** - for each of those there is another rate at least as fast that also needs less signal, so a scheme able to select one eventually will and will then be slower AND less robust at once. Two results nobody would have guessed. **Rate 2/3 owns the top of the ladder**: every rung above QAM-16 3/4 is a 2/3 code, so the obvious design of one code rate with the constellation varying misses most of the range. **QAM-128 is dominated at all three code rates**, because a burst is a whole number of symbols and seven bits per carrier usually rounds up to the same symbol count as eight - it pays QAM-256's noise penalty for QAM-64's air time. Payload size is not a detail either: at 64 bytes the same rounding merges whole groups of rungs and the ordering does not hold. The surviving ladder spans 17.1 dB for 5.2x the throughput. **The margin measure**: re-encode what the Viterbi decoder decided, compare against the demodulator's own hard decisions, and the difference is how much of the code's correcting power that burst spent. It rises smoothly as signal falls, where "the CRC passed" is one bit that arrives too late, and **the value at which a rate gives out is a property of the code rate and not the constellation** - about 0.09 at rate 1/2, 0.05 at 2/3, 0.03 at 3/4 - so a station needs three numbers rather than thirteen. It also has a measurement floor that shaped the policy: an error rate is a count over a few thousand coded bits, good to about 8 % at the cliff and to about 45 % at 3 dB of margin, so it is sharp exactly where a station is in trouble and vague exactly where it has room. **The policy, and the two wrong diagnoses on the way to it.** Closed against an oracle - every fixed rate over the same link and seeds, best taken afterwards, air time counted for failed bursts too - the first version reached 72 to 91 %. I blamed the step-up rule for wanting a *run* of consecutive clean bursts (real, worth about 3 points) and then blamed using one slope for how fast the error rate falls with signal (also real - 0.36 per dB on BPSK against 0.81 on QAM-256, so one fitted 0.7 reads anything from 2.4 to 11.3 dB where the truth is 4.0 - and also worth about 3 points). Neither explained the gap. **Counting the retreats instead of theorising about them found it in ten minutes**: at +16 dB, zero retreats on failures and fourteen on bursts that had decoded perfectly. Stepping down whenever one successful burst looked expensive was the whole fault. A burst that decoded is proof the rate works, whatever it spent getting there. What replaced it is the durable part: **choose the rung with the best expected THROUGHPUT, not the best margin** - translate the current rung's margin to every other rung through the difference in their cliffs and each rate's own slope, turn the implied spending into an expected frame success, multiply by goodput. A rate losing one frame in seven at 4107 bit/s is well ahead of one losing none at 3227, and deciding on margin alone always picks the safest rate. Final: **87 to 93 % of an oracle that knew the answer in advance**, settling on the right rung at three of five signal levels. **Wired into the modem**, where three more things were needed that the component did not have: a burst reported once rather than once per retry attempt (a failed payload is retried a sample on, so one wrecked burst otherwise reads as a run of failures); opening at the configured rate rather than the bottom of the ladder (the first burst of any link goes out before anything is heard back); and expiring a recommendation after two minutes, which took a `TimeProvider` and whose fake clock immediately found a real bug - the "have I heard anything" sentinel was a zero timestamp, which a real clock never returns and a test clock always starts at. **Off by default, and not for compatibility.** Two of the three situations it can be in are ones it makes worse: a beacon or one-way link never hears anything back and sits where it started, and on a shared channel several correspondents' reports average into one nonsense recommendation, because the payload here is an opaque AX.25 frame and this layer never reads an address. It is right for a point-to-point link. **Still not proven, unchanged: no OFDM-FM mode has masks in either tier, and none of this has been on the air.** Everything above is one geometry, one deviation, one tap, one payload size, a static link, in simulation.

- **2026-08-11 (later) - a Tait manual arrived that contradicts two negative findings recorded on 2026-08-09, and both hardware notes are rebuilt on it. No mode changed, no mask moved.** Tom added `MMA-00011-01`, the **3DK Hardware Developer's Kit Application Manual** (issue 1, March 2006, 156 pages) to `M0LTE/tait-tm8100-tm8200-docs`. It is the document Tait wrote for people building hardware that plugs into a TM8100, and it answers the two questions the 2026-08-09 (later6) entry above recorded as unanswerable. **That entry stands as the record of what the 20-manual set contained; it is superseded, not wrong.** What the new manual says: **input level to deviation is published for every tap in both directions** (Table 2.7, p.22), specifically **0.87 Vp-p at T13 for 3 kHz deviation** and **0.60 Vp-p at R1 for the same into 600 ohm**, with full scale 2.0 Vp-p either way; and **T13 is a fully documented tap-in point** (Figure 3.14, p.93) with its own level, group delay and frequency response, so the earlier "no occurrence of T13 in 1083 pages" was a property of the set, not of the radio. The service manual's CCTM tap list is a test command's subset in an older numbering. **Three findings that change what a station should do**, none of them measurable here and all of them cited in `docs/hardware/`: Tait's own external-modem configuration specifies **R1 tap out, type D-Split, unmute Except on PTT** and **T13 tap in, type A-Bypass In** with **EPTT1 as Data at highest priority** (p.112), which is the tap pair both notes had already argued for from the block diagram; **PTT to full carrier power with valid modulation is 14.8 +/- 0.5 ms** via T13 (Table 5.2, p.113), which is the floor for TXDELAY and is eight times the 1.8 ms group delay a reader would otherwise size on; and the **T13 level carries a +/-10% spread**, so a divider computed from the typical figure can over-deviate by 9% on a radio at the sensitive end, which is the argument for keeping the Bessel-null check per radio rather than per model. **Two errors found in our own notes while cross-checking, both fixed.** The transmit divider in the CM108 note had its shunt leg on the radio side of the DC block, which would have pulled the tap's 1.5 V bias to about 15 mV through its 100 kohm bias network and clipped half of every waveform; and the internal-board note specified Rt as 2k9 in prose and 3k3 in its schematic, now 3k0 throughout, the nearest E24 value that lands under the deviation ceiling rather than over it. **Unchanged: this is documentation, not measurement.** No mask row was touched, no mode was re-run, and nothing here has been on the air.

- **2026-08-13 - the CM108 interface note splits: build instructions for the board actually deployed, with the reasoning kept as extended notes.** Tom's review of the note: he wanted simple instructions for wiring one specific board to one specific radio, and the note, however correct, was a treatise. The board is [tomwardill/cm108radiowidget](https://github.com/tomwardill/cm108radiowidget), and reading its KiCad netlist changed real values, which is the substantive point beyond presentation. It has **no microphone bias network** (VBIAS unconnected, MICIN reached through 1 uF), so the stock-dongle 1k || 2k2 shunt arithmetic does not transfer and the receive divider computes against Rp alone, about 3 dB different. Its **PTT is already an open-drain BSS138 on GPIO3**, so it wires straight to AUX_GPI1's internal pull-up, and the generic add-a-MOSFET advice applied to this board would have inverted the logic. And it carries a **Schottky-into-volume-down SQUELCH input**, which maps onto Busy Status programmed active Low, with a hazard worth recording: the signal arrives as a HID consumer key, so a host that maps those to a mixer will wind the calibrated capture gain down whenever the channel is busy. `tm8100-cm108-interface.md` is now the five-wire build note with computed values (Rb 1k with Rt from a measured-full-scale table and C4 on the radio side of the divider for transmit; Rs 6k8 with Rp 1k receive at nominal full scale; all passives in the D-sub backshell); `tm8100-cm108-interface-notes.md` keeps the full 3DK treatment from the entry above plus the widget's deltas. This leg and the rebuild above found the same 3DK figures and the same bias trap independently, which is a cheap cross-check of both. **Unchanged: documentation only, no mask moved, nothing on air.**

- **2026-08-15 - `freedv-datac1` and `freedv-datac3` cross a real ionospheric path, out of the production node's own transmit chain. Neither mode's status changes; what changes is the class of evidence behind it.** The 2026-07-28 campaign that turned all six datac modes green transmitted through the `sm-ota` harness into an RSP1 on the same bench, across a 125 dB attenuator chain (`docs/ms110d/evidence/2026-07-28-freedv-ofdm-oncar/`). Everything between "our modulator" and "our demodulator" was ours and was in the room. This run replaced all of that: the **production GB7RDG daemon** on 40 m at 7053.600 MHz, ANT1, 50 W, sharing one Flex slice with three modems carrying live traffic, received by a **second pdn-soundmodem instance on somebody else's receiver** (`ubersdr:wessex.zapto.org`, terminated delta loop, SW England) with an ordinary band-planned config and nothing but the device string different. Frames went in over KISS (`scripts/kiss-send.py`) and came out as AX.25 at the far end. **`freedv-datac3`: 6 of 6, 100 %. `freedv-datac1`: 11 of 14, 79 %.** Every decode `crc ok`, `fec 0` - not one Reed-Solomon correction spent across 17 frames - at a measured **+2 Hz** offset, which is what a GPSDO-locked Flex and a disciplined receiver are worth. The gap between the two modes is the expected ~5 dB: datac1 needs +4.6 dB on a fading channel against datac3's -0.7 dB (the rows above), and this path did not have 5 dB to spare. **The path measurement is the other half of the result.** A first attempt into `m9psy-1` (Dalgety Bay, ~550 km) decoded nothing at all, and the survey was *empty* - heard nothing, rather than heard-and-failed. An IQ capture taken through the transmissions settled it rather than leaving it to guesswork: `sm-iqcapture` at 7053.600 plus a time-resolved in-band-minus-reference analysis showed our bursts arriving at the right instants at only **+6 to +10 dB** of in-band excess, about +5 dB SNR3k - right on datac1's fading threshold, which is exactly consistent with no copy. So the mode was never the problem; a 550 km hop on 40 m at 09:10 UTC in August was. Moving to a receiver ~120 km from the transmitter turned 0 % into 100 % on the same waveform, same power, same config. **New instrument:** `scripts/iq-occupancy.py`, the pre-transmission clear-channel check (Welch over an sm-iqcapture WAV, mean and peak per bin against a median floor); it validated itself on the first run by finding GB7RDG's own bpsk300 at +19 dB exactly where it sits, and found a narrow persistent carrier at 7055.15-7055.25 that moved the test slot from 7054.000 down to 7053.600 for guard. Also proven on air, and not previously proven anywhere but a mock: the **per-modem Morse identification** shipped in v0.35.0 (PR #303) - `id[3] M0LTE in CW` at 09:18:50 and again at 09:28:56, an interval of **10 min 06 s** against a configured 10 minutes, with the 6 s being the 5 s poll granularity plus channel access. Campaign plan, placement reasoning and the band plan constraint (7053-7060 is *not* the segment designated for unattended data stations, so this ran attended and came down afterwards) in [freedv-ota-plan.md](freedv-ota-plan.md). The node was restored to its exact pre-campaign config and filters and verified back in service working EI0RSI-1.

- **2026-08-15 (later) - the `freedv-datac1` misses are the channel, not the receiver, and the measurement that says so also found a bug that takes a transmitting node off the air.** Tom asked what happened to the 3 frames of 14 that did not decode in the morning's run. Answer, from 30 more frames at 25 s pacing with an IQ capture running through the whole run and each burst's received level correlated against the receiver's own verdict: **the misses are the weak bursts, with no overlap at all.** Decoded bursts (n=10) sat at **+17.2 dB mean in-band excess** (range +15.1 to +20.0); missed bursts (n=13) at **+5.7 dB** (+3.5 to +13.7) - a **separation of 11.5 dB and not one inversion**. Everything at or above +15.1 dB decoded; everything at or below +13.7 dB did not. That is a threshold device behaving exactly as one should, and it means there is **no "strong but undecoded" population** - nothing here indicts the demodulator. Two further details fall out of the same data. The path changed state sharply at **10:54:25**: before it, bursts arrived weak *and short* (0.3-1.8 s of a 4.4 s transmission poking above the detector), which is within-burst fading rather than a shortened transmission; after it, full 4.4 s bursts at +13 to +20 dB. And bursts 20 and 21 (mean +13.4 and +13.7, full length) are the knee itself - the only two full-strength misses in the set. **Do not read the absolute figure as an SNR3k**: it is a power-spectral-density ratio of the occupied band against a 400 Hz reference just above it, so it is a clean *relative* instrument and an uncalibrated absolute one. The relative result is what the question needed. **The bug:** pacing frames every 8 s to gather this data took the production GB7RDG daemon off the air at 10:28:50 with `receive feed dead: 30 s of unbroken digital silence from the radio`. A keyed Flex is not receiving and its DAX stream delivers exact zeros, which is byte-for-byte identical to the dead feed `DeadFeedWatch` exists to catch, and nothing told the two apart - so a station that transmits enough restarts itself. Pre-existing since the watch landed on 2026-08-07, and invisible until now because nothing had ever driven this station's transmit duty cycle that hard. Fixed by giving `Observe` the station's keyed state and re-arming rather than accumulating (PR #305); 25 s pacing produced 30 transmissions and zero dead-feed events afterwards. The trade-off is recorded in the class: a station transmitting more often than the threshold can no longer detect a dead receive feed, which is the right way round, and the receive-only capture station the watch was built for is unaffected.

- **2026-08-15 (later2) - chase decoding lands after all, and the 2026-08-06 null it reverses is explained: the null was the confidence metric, not the idea. bpsk300 gains ~0.5 dB at the AWGN knee, ~1 dB on short frames, and reads 18 of the week's real missed bursts.** rx-roadmap workstream 1's named remainder, CRC-arbitrated chase decoding, had already been built once (M0LTE.Il2p branch `chase-decoding`, archived 2026-08-06 as a measured system-level null with the condition "re-open only with a demonstrably better per-bit soft metric"). That condition is now met. The mechanism: a differentially-decoded bit compares symbol k against symbol k-1, so one hit symbol flips TWO consecutive bits - and the demodulator's per-bit confidence measured only the current symbol's projection magnitude, leaving the second bit of every such pair looking confident. A chase hunting the weakest bits could never assemble the true flip set: instrumented at the AWGN -4 dB knee, **0 chase hits in 50 header attempts** with the old metric, **40 of 70** once `BpskDemodulator` emits the weaker of the two symbols' magnitudes per bit - and the accepted flip sets come back as adjacent pairs, the mechanism's own signature. Each factor alone is a measured null (pair-min without chase moved nothing; chase without pair-min was the 2026-08-06 null, reproduced exactly); together they compose. M0LTE.Il2p 0.3.0 (PR M0LTE/M0LTE.Il2p#4) carries the chase: per-bit confidence through the deframer, header rescue as any combination of the 12 weakest bits accepted only as an exact codeword (the 2-parity header affords nothing speculative - one random word in 17 sits within a byte of a codeword), payload rescue as up to 3 flips with at most 7 located errors on top, every attempt keeping the erasure ladder's own two parity symbols in reserve; `Il2pDecodeInfo.ChasedBits` and a `chased_bits` frame-log column keep the honest account, the `erased_bytes` precedent. Full ladder A/B (N=300, seed 1, `sm-ota sim`, this PR #307): AWGN -5 dB **55 -> 67 %**, -4 dB **88 -> 93 %**, -3/-2 within a point; the CFO span at -3 dB **92-97 -> 98-99 %** (chase rescues the header bits a residual rotation grazes); Good -4/0/+4 39/62/79 -> 40/64/80; Moderate 40/52/60 -> 41/53/60; Poor 24/28/32 -> 24/30/32 (the outage ceiling, exactly as workstream 5 said). Short 15-byte frames - the live slot's modal supervisory traffic, where the header is most of the frame - move hardest: AWGN -5 dB **62 -> 76 %**, -4 dB 88 -> 93 %. Wild evidence, not just the model: against the new **misses-v2 corpus** (573 full-length undecoded bursts cut from the 40 m capture's whole 166.9 h raw archive, built this evening - see the campaign section of rx-roadmap.md) the isolated-decode score moves **14 -> 32 bursts, zero lost**, 11 of the 18 new reads CRC-verified end to end and every one of them a known station of the week's traffic (GB7WEM-7, EI0RSI-1/-7, GB7BPQ>SA6DAZ-3, GB7BEX-15, PD4R-12); 17 of 18 leaned on chase, and the flip counts are again even pairs. The GB7RDG 37-frame corpus is unmoved at 32/37 demodulated - measured before and after, its residue is genuinely not RS-limited, as the roadmap has said since 2026-08-06. Full-tier mask rows raised with this entry per the measured values: AWGN -5 35 -> 47, -4 68 -> 73, the four CFO rows 75 -> 80; smoke floors unchanged (still 2-3 sigma under the new measurements). Nothing on the wire changed: every candidate the chase accepts decodes to the same bytes a clean receive would have produced, and NinoTNC interop stays pinned by the parity suites.
- **2026-08-15 (later3) - every decoded frame now records the strength of the burst it arrived on: `snr_db` in the frame log, `snr x.x dB` on the journal line, `snrDb` in the KISS quality frame, one figure across all of them.** No mode changes status - filed on the acceptPlainIl2p precedent: this changes what a station writes down, not what it decodes. rx-roadmap workstream 9, the cheapest item on the list and the one that makes every other workstream measurable in the wild: the daemon has computed a per-burst SNR since the waterfall's `BandActivityTracker` landed and threw it away unless a browser was watching - the 2026-08-15 FreeDV investigation had to stand up a parallel IQ capture to recover a number the daemon had already measured. The measurement now lives in the channel (`BurstSnrMonitor`), bands probed by the same `ModemBandProbe` as the waterfall overlay and RF planner, fed only non-transmit audio, and enriches `FrameQuality.SnrDb` at the single point every modem's quality passes through - so every consumer records the identical figure and the waterfall panel prefers it over its own tracker for the same reason. **The convention is stated everywhere the number appears** (the roadmap's warning, honoured): mean in-band power over the burst against a rolling minimum noise floor, in dB - NOT the 3 kHz-referenced SNR of the sim ladder and the Watterson masks; comparing the two without converting is wrong. A quiet band yields null, never zero; a frame log from an earlier daemon gains the column by additive migration with old rows honestly null. Pinned by `BurstSnrTests` (a strong burst carries a figure; a cold-start burst that cannot be attributed carries none), `FrameLogTests`, `ActivityLogTests`, and the KISS quality suite. PR #308.
- **2026-08-16 - the chase landing's continuous-replay verdict: +78 frames over the whole 166.9 h archive, 41 CRC-verified, zero content lost - and it outreads the MLSE detector swap with the deployed default.** Overnight addendum to the 2026-08-15 chase entry, run on the merged main (M0LTE.Il2p 0.3.0 from nuget): the full raw archive replayed continuously - not the isolated per-burst rig - reads **4394 decoded / 3914 deliverable** against the pre-chase baseline's 4318 / 3872 (+76 / +42, +1.8 % / +1.1 %), 4442 DCD bursts of which 3481 now carry a decode (was 3456), and 102 of the week's decodes leaned on chase (flip histogram again pair-dominated: 40x4, 23x2, 21x6). The diff's two apparent baseline-only frames dissolve under autopsy: both are copies inside retry chains whose payload the chase arm also read seconds away, so no content the old receiver heard is missing from the new one. For workstream 2's ledger: a same-window `--detector mlse` pass (old receiver generation) reads 4343 / 3889 - the chase landing gains more on the deployed default detector than the MLSE swap offers - and the mlse-vs-chase exchange stays roughly the known shape (75 frames only mlse caught vs 127 only chase-differential caught), so the ensemble opt-in remains sized at about +1.7 % union, an upper bound until an mlse arm carries pair-min + chase too. CSVs beside the others in `/home/tf/capture-40m/replays/`.

- **2026-08-16 (later) - a survey capture now really carries its trailing margin: the WAV waits for the audio instead of shipping at verdict time.** No mode changes status - a record-quality fix, found the first time somebody wanted to listen to the misses. Tom asked for the day's 18 full-length missed bursts with a second of context either side for by-ear characterisation; the files had their full 1 s lead-in and ~0.2 s of tail, because `SignalSurvey` wrote the capture the moment the detector closed the burst - one 0.2 s grace period after the last hot line - and clamped the trailing margin to the audio that existed. CONFIG.md's `marginSeconds` ("audio kept either side of the burst") was the promise; the code kept half of it. A triaged capture now waits in a pending queue until the ring holds the whole trailing margin and is written then; a keyup or shutdown flushes it early and honestly short, because splicing post-transmission audio onto a pre-transmission burst would manufacture context that never happened. Pinned both ways in `SignalSurveyTests` (a 2 s burst's WAV measures 4.0 s with a 1 s margin; a keyup flush stays under it). PR #310.
- **2026-08-16 (later2) - Tom characterised the day's misses by ear, and the instruments turned his "unknown modem at ~2150 Hz" into three findings: it is our own network's bpsk300, most of the "misses" were fade-split fragments of transmissions the station DECODED, and the survey now lets a decode claim its fragments.** Tom listened to the 19 full-length missed-burst WAVs of the v0.37.0 rollout morning and reported an unrecognised modem around 2150 Hz on most of them (plus VARA near the ARDOP slot, static, and fragments of HF misc). Spectral fingerprinting answered him: every strong burst carries a razor 300.0 Hz envelope baud line and a 31-41 dB carrier line at exactly twice its centre under squaring (none under 4th power) - 300 Bd binary PSK, our own mode, RRC-shaped, in our own slot. The 2fc line measures each burst's true carrier to a fraction of a Hz, and the offsets cluster into exactly three station fingerprints the frame log already knows: -18 Hz (GB7BPQ and PD4R-12), -8.4 Hz (GB7BEX-15), +1.6 Hz (EI0RSI-7). Then the timing correlation: **9 of the 11 strong-BPSK "misses" have a same-fingerprint decode landing 0.6-3.2 s after the burst closed.** Frame timestamps are decode-completion times and a 118-byte beacon is ~4.3 s of wire, so these bursts are leading fragments of transmissions the station went on to read: a fade outlasting the burst detector's 0.2 s grace splits the transmission, and Triage's attribution window (one second past the burst) closes before the decode exists. The fix, built on v0.37.1's pending-capture queue: a Missed capture waits `decodeClaimSeconds` (default 5 s) and is re-checked against the decodes at release - a decode inside the window claims it and it is never written, counted in `ClaimedByLaterDecode`; a real miss releases unchanged when the window passes. Pinned both ways in `SignalSurveyTests`. Two honest riders: the same fragment class colours **misses-v2** (a share of that corpus's 573 "full-length undecoded bursts" are fragments of decoded transmissions, which the retry-sibling attachment partially marks and the isolated re-decode score already reflects - fragments do not decode alone); and of the day's 11 in-slot bursts only 2 (a GB7BPQ-fingerprint at 08:43 and an EI0RSI-fingerprint at 09:34) were genuine unclaimed misses. Tom's "sounds overdriven" stays open: what a fragment of a QSB-mangled BPSK burst sounds like and what an overdriven transmitter sounds like are not separable by ear on this evidence, and the drive question belongs to the stations, not this receiver. PR #311.
- **2026-08-20 - MS110D: the Poor-gate successor program is registered, the measured-only counts become a bank the suite asserts, and the record is reconciled with the evidence.** No mode changes status. Three weeks after the WN8 program closed, the documents still told three different stories: `docs/ms110d/README.md` and `roadmap.md` ended at Phase B or called the WN8 program live; `phase-b-closeout.md` §4/§5 still said WN8's two walls stood (both fell on 2026-07-31, now recorded there as dated amendments, the Phase B text left as written); the OTA planning documents still called §E2 and §E4 unrun and the RSP1's AGC undefeatable, when the 2026-07-27 full campaign ran eighteen live ladders through the pad-chain rig with the AGC off and §E4 ran from a real antenna on 2026-08-03. The rows above for WN2/WN6/WN13 drop their retired caveats (#102 closed 2026-07-28; the confirmation evidence has been committed since the same day). The substantive gap was in the suite: the W6 closeout's obligation that `MS110D_POOR_GATED`'s expected-red values re-bank to the measured figures had been discharged in prose only - no test referenced 2.90E-4 / 1.75E-2 or 2.56E-5 / 1.48E-5, so a regression from either was invisible. `Ms110dMaskTests.MeasuredOnlyBank` now pins WN7's and WN8's closing counts (both seed families, exact, in the battery configuration) as assertions; `MS110D_POOR_GATED=1` keeps its meaning and is how a chase leg gets past the bank. The program of record is `docs/ms110d/poor-gate-successor-plan.md` (issue #312): G0 closes the books and re-baselines (registration and battery under `docs/ms110d/evidence/2026-08-20-poorgate-g0/`); G1 puts the MFB-form receiver on WN7, which nobody has tried and which the W1 truth lane licenses (WN7's oracle-residual corpse decodes to zero on all 11 blocks under per-symbol truth); G2 takes WN8's residual, now localised to three first-block non-convergences; G3 covers the production path through `FrequencyShiftedModem`; H1/H2 are Tom's hardware legs, H2 being the pad-chain ceiling measurement that is the only route by which the +19/+23 dB points ever run through a real transmitter. Both Poor points stay sim-only by physics until H2 says otherwise.
- **2026-08-20 (later) - G0 PASS: the MS110D re-baseline reproduces every digit, and the measured-only bank holds.** No mode changes status. The successor program's entry battery (`docs/ms110d/evidence/2026-08-20-poorgate-g0/`) on current main: all five Phase B closeout §6 guard pins exact; every Poor error count identical to the W0/W6 baselines on both seed families (WN7 83/3,243,776 and 48/3,243,776, WN8 1,254/4,325,120 and 75,713/4,325,120, now asserted in code by `Ms110dMaskTests.MeasuredOnlyBank` and passing); AWGN 10/10, static WID2 and the three Doppler checks zero errors; 120 of 120 census CSVs byte-identical. Run on the Release test exe directly rather than `dotnet test` on Debug: 40 minutes wall for the 33 legs where the W0 and W6 batteries took 3.5 hours, with identical digits, so that is the battery form from here on. G1 (WN7 under the MFB-form receiver) is next.
- **2026-08-20 (later2) - MS110D WN7 Poor: the MFB-form receiver alone does not beat the DFE-chain receiver, but the two fail on disjoint blocks and a label-free selector picks right every time.** No mode changes status yet. Leg G1 of `docs/ms110d/poor-gate-successor-plan.md` (`docs/ms110d/evidence/2026-08-20-poorgate-g1/`): the W5 MFB-form prototype generalised to 8PSK (calibration: byte-identical on the WN8 specimens, so the QAM16 numbers are untouched) leaves 238 errors pooled over the seven bursts that carry all 131 of WN7's battery errors, zeroing three of them and losing on the rest; the pre-committed rule kills it as a standalone receiver and the Phase B B3.9 verdict stands under the MFB class. The measured lever is the disjointness: on all 88 blocks of the eight specimens the two receivers are never wrong together, and the MFB reconstruction residual - re-encode each candidate decode, reconstruct through the estimated trajectory, keep the lower residual - selects a zero-error decode on every block (18 contested; the wrong decode prices 0.17 to 22 % above the right one, about 4 sigma at the thinnest). Per-block ensemble ceiling on these specimens: 0. G1d (the shipped build and its battery) decides whether the WN7 row moves; nothing ships from this entry.
- **2026-08-20 (later3) - MS110D WN7 (8PSK r3/4) Poor: measured-only -> sim hard-gated. The 8PSK per-block ensemble takes the point from 83/48 to 0/0.** The broken-to-working transition for `ms110d-wn7` on the D.6.1 Poor channel at +19 dB. Leg G1d of `docs/ms110d/poor-gate-successor-plan.md` (`docs/ms110d/evidence/2026-08-20-poorgate-g1d/`, issue #312): the shipped `Ms110dMfbBlockDecoder` speaks 8PSK as well as QAM16 and prices any block decode by its reconstruction residual; on every 8PSK block the demodulator runs it beside the DFE-chain path and keeps the decode with the larger evidence - the MFB's Gaussian log-likelihood gain (rows x ln of the residual ratio) against the chain's summed |LLR| at the disagreeing wire bits. No constant is tuned. Two things were found on the way and are part of the record: the residual alone is blind to an MFB maximum-likelihood error (the first battery took AWGN WN7 from 0 to 8 on one nearest-neighbour codeword block; the chain's |LLR| of 4.5 on those five bits against 1-3 where the chain is wrong is what separates them, and the two-sided rule separates all 19 contested blocks with an 8x margin at the thinnest), and the MFB's block-0 probe geometry was wrong for one-frame interleavers (the preamble's closing probe is unshifted), caught by the hermetic WN7 UltraShort loopback and fixed for QAM16 too. Battery: **Poor WN7 0 / 3,243,776 both families** (97.5 % bound 1.13E-6), AWGN WN7 0, every one of the other 114 censuses byte-identical to G0 (WN8 1,254 / 75,713 untouched), guard pins exact (WN7 w1/b0 now 0 coded, selected 2). `Poor_Channel_Mask_Gate` arms WN7 by default; `MeasuredOnlyBank` drops its WN7 rows. Sim-only by rig physics: the +19 dB mask region is above both rigs' ceilings, so the on-air column does not forward and the row stays partial (successor plan §6; H2 is the route). Latent hole in the shipped MFB filed as #316. The 9 of 10 Poor points are now hard-gated; WN8 remains the one measured-only point.
- **2026-08-20 (later4) - the Watterson rig learns to fade about a moved centre; every MS110D waveform passes the shift gate at 3750 and 5000 Hz on AWGN and WN6 on Poor.** No mode changes status. Leg G3 of `docs/ms110d/poor-gate-successor-plan.md` (`docs/ms110d/evidence/2026-08-20-poorgate-g3/`). The first Poor arm of the new shifted-modem smoke read native 10/12 against 4/12 at 3750 Hz and 0/12 at 5000 Hz, the same at 14, 20 and 26 dB, with AWGN at parity everywhere - the signature of a band-limited instrument, not a receiver. `WattersonChannel` forms its complex envelope about the 1800 Hz sub-carrier with a 2.2 kHz low-pass; it had never been asked to fade a signal centred anywhere else. It now takes `CentreHz` and scales its low-pass taps with the sample rate (129 at 9600 Hz exactly as before, so the five guard pins and four corpses reproduce their banked digits). Corrected: Poor at 14/20/26 dB, native 10/11/11 of 12 against 10/11/11 at 3750 and 10/11/11 at 5000. The shift ladder now runs all ten `ms110d-wn*` (50 frames per cell, native / 3750 / 5000, rungs re-centring on each knee) and every waveform passes per rung and in aggregate; the smoke joins the hermetic suite; the daemon prints WN7's and WN8's Poor standing at start-up and on SETHW. The #221 gate's AWGN-only verdict stands; what it could not see, it now can.
- **2026-08-20 (later5) - MS110D WN8 (16QAM r3/4) Poor: 2.90E-4 / 1.75E-2 -> 7.17E-6 canonical / 1.78E-5 disjoint, the canonical family at the mask; still measured-only.** Leg G2 of `docs/ms110d/poor-gate-successor-plan.md` (`docs/ms110d/evidence/2026-08-20-poorgate-g2/`). The whole disjoint 1.75E-2 was three blocks, mid-burst not first, each a coin-flip the shipped schedule never descended from, and the matched-filter bound with nothing but label-free probe anchors decoded all three to zero, so the model was never the deficiency: the cold rung was. `Ms110dMfbBlockDecoder` now starts every QAM16 block with a per-symbol linear MMSE estimate on its anchored trajectory (known probes subtracted, neighbouring data chips as Es-power unknowns, the anchor-fit residual as the noise estimate) instead of an ISI-inclusive matched projection; every block begins at 4-13k errors instead of 24k and converges in four to seven rungs where it took ten to forty or never. Battery: **Poor WN8 31 / 4,325,120 canonical (7.17E-6, direct BER under the mask) and 77 / 4,325,120 disjoint (1.78E-5, 1.8x over on one block at 62 and one burst at 15)**, AWGN WN8 0, all 113 other censuses byte-identical (WN7's ensemble untouched by the QAM16 scope). `MeasuredOnlyBank` re-banks to 31 / 77. The row stays partial and measured-only: one family clears, one does not, and both are sim-only by rig physics. Successor levers recorded: the stability-driven soft handover (measured to convert exactly these stragglers before the cold rung existed), schedule diversity priced by residual, and the cold rung on 8PSK (unmeasured, its own registration).
- **2026-08-20 (later6) - MS110D WN8 (16QAM r3/4) Poor: measured-only -> sim hard-gated at 12 / 18 per 4,325,120 bits. All ten MS110D Poor points are now hard-gated.** The broken-to-working transition for `ms110d-wn8` on the D.6.1 Poor channel at +23 dB, completing the program. Leg G2's second half (`docs/ms110d/evidence/2026-08-20-poorgate-g2/`, G2e/G2f): with the MMSE cold rung in, four blocks still settled on nearly-right decodes (62 / 15 / 12 / 19 errors), every one of them a soft phase that ran to its cap without settling and then a period-2 oscillation accepted as converged at the first hard rung - the #316 hole, measured. The QAM16 schedule now has a soft-phase floor, accepts a cycle only between hard-phase decodes, runs the hard-first schedule after any cycle-accept (an exact fixed point wins; two cycles are priced through the same probe-anchor model, because residuals through different re-fit models are not comparable - banked), and hands the soft phase over when its residual stops falling rather than at the cap. Battery: **Poor WN8 12 / 4,325,120 canonical (bound 4.85E-6) and 18 / 4,325,120 disjoint (bound 6.58E-6)**, from 31 / 77 this afternoon and 1,254 / 75,713 at the WN8 program's close; AWGN 0; every other census byte-identical, WN7's ensemble untouched (8PSK keeps the W5b2 schedule; #316 stays open for it). `Poor_Channel_Mask_Gate` arms WN8; `MeasuredOnlyBank` is empty. Sim-only by rig physics: the row forwards to "Poor sim hard-gated" and stays partial until the pad-chain ceiling (H2) lets +23 dB through a real transmitter.
- **2026-08-21 - MS110D 8PSK: the MFB decoder's MMSE cold rung and reworked schedule reach the WN7 ensemble; nothing the ensemble emits changes.** No mode changes status. Leg G4 of `docs/ms110d/poor-gate-successor-plan.md` (`docs/ms110d/evidence/2026-08-21-poorgate-g4/`): the QAM16-only scope of G2d/G2f lifted, so WN7's second receiver starts each block with the per-symbol MMSE estimate and stops on the same rules as WN8's. Battery: Poor WN7 0 / 3,243,776 both families, AWGN 0, WN8 12 / 18, all 120 censuses byte-identical to their baselines; the 8PSK MFB terminates every block in 11 rungs where it took 10-40 with a soft oscillation accepted on one; wall-clock flat. #316 (a cycle accepted between soft-phase decodes) is closed on both branches.
- **2026-08-21 (later) - `bpsk1200` read off a real FM receiver, and the FM/SSB split in this document turns out not to be a split the receiver cares about.** No mode changes status: `bpsk1200` was already working (on-air AWGN validated 2026-07-28 over the Flex->RSP1 **SSB** rig). What is new is the carrier. Tom recorded five noisy captures off an **FM** radio on 2026-08-21 with the mode not written down, as the first real material for the new `pdn-decode` sweep tool, and all five turned out to be one `bpsk1200` IL2P+CRC QSO (N2IRZ-2 to WA2M-2, a BPQChatServer-6.0.21.40 session). The sweep recovers **7 distinct frames from 3 of the 5 files** on the shipping differential bank with no tuning - CRC-clean, 0-8 bytes FEC-corrected, one frame rescued by 6 chased bits, measured offsets -4 to +1 Hz. The corpus and its analysis are committed at [`samples/offair/2026-08-21/`](../samples/offair/2026-08-21/README.md). **The finding worth carrying forward** is the one that cost the tool its first default: `FmModeProfiles` and `docs/mode-modulation-reference.md` classify a mode by how it reaches the air, which is a fact about modulators, and `pdn-decode` first read that as the set of modes an FM capture can contain. It is not. Nino's own switch map groups the shaped-PSK modes "Shaped PSK - SSB radios, **or FM radios**" and puts `qpsk3600` with the speaker-and-mic modes; a shaped-PSK signal through an FM radio is an ordinary thing on the air, and the FM-native sweep read none of this corpus. Nothing in the modem was wrong - the same `bpsk1200` bank that reads the GB7RDG SSB capture reads this FM one - but any future tool or document that treats the FM column as "what an FM radio can carry" will be wrong the same way. The four hard files are hard for an ordinary reason and not a mode limit: they are open-squelch recordings whose noise floor never drops (in-band and above-4 kHz energy within 1 dB, nothing clipped, -7 dBFS peak), against the one squelched file whose bursts sit 63 dB over its floor and which yields five of the seven frames.
- **2026-08-21 (later2) - `qpsk600`: a real off-air burst at +1.7 dB that the shipping receiver cannot copy, and the measured reason is the QPSK family never got PR #236's chain or a diversity bank.** No mode changes status; what changes is that the QPSK receive chain now has a named, committed, real-signal failure instead of a roadmap note. Tom said `packet-24738.wav` "definitely contains one of the slower modes"; it does. Finding it needs the right polarity - the file is loud almost throughout and goes **quiet** from 3.06 to 5.31 s, which is an open-squelch FM receiver quieting on a carrier, so the transmission is in the quiet window rather than the loud one. Measured in it: a two-tone preamble 3.03-3.63 s at **1350.6 / 1649.4 Hz** (centre **1500.0**, shift **298.8**), symbol rate **300.00 Hz** off the `abs(z)^2` cyclostationary line, and a **4th-order** constellation - the `z^4` line lands at 0.00 Hz, 20 dB above `z^2`, which has no DC line at all, and against both NinoTNC references degraded to matching noise the burst looks *more* 4th-order than the `qpsk600` reference does. 300 sym/s, 1500 Hz carrier, 4th order is **`qpsk600`** (switch 1001). **SNR is not the excuse**: +4.1 dB on the preamble and **+1.7 dB on the data**, 3 kHz-referenced, against a published `bpsk300` AWGN threshold near -4 dB; the noise is ordinary Gaussian (kurtosis 3.0, nothing beyond 4 sigma, so not FM click noise), the carrier offset is ~0, and nothing is clipped. Tried and failed: the whole 46-entry sweep; the burst isolated to 2.9-5.5 s so the surrounding full-scale hiss could not disturb AGC or deframer; a manual diversity sweep at 17 offsets from -30 to +30 Hz; and the coherent detector on every PSK mode. **Structural cause, already predicted here**: `bpsk300`/`bpsk1200` run `BpskMultiModem` (nine-branch bank) with the #236 rebuilt differential chain worth ~1.5 dB, and `afsk300` runs an eleven-branch bank, while **the entire QPSK family is a single `QpskModem` with no bank and the pre-#236 chain** - exactly the leg the #236 entry above lists as undone ("the obvious next legs are ... the QPSK family, which still runs the old-style chain"). Degrading the clean references to matched noise puts numbers on it: `qpsk600` 3/6 at +2 dB, 2/6 at 0 dB, **0/6 at -2 dB**, where `bpsk300` holds 3/6 throughout - a gap of roughly 4 dB, and this burst sits in it. Absolute calibration of that ladder is rough and the rows mean nothing except against each other. The corpus is committed at [`samples/offair/2026-08-21/`](../samples/offair/2026-08-21/README.md) with the full measurement, so the QPSK campaign has a real-signal fixture with a known answer to aim at rather than only a simulation ladder. Registered as **issue #326**, which also notes that #11 and #116 (`qpsk600`'s measured +-5 Hz CFO wall and its 0-1/3 copy rate on real NinoTNC transmissions over a clean audio loop) are very likely the same root cause.
- **2026-08-21 (later3) - the QPSK family gets BPSK's diversity bank and the rest of PR #236's chain, plus two timing fixes of its own: qpsk600's AWGN knee moves ~0.8 dB, qpsk2400's ~1 dB, and both modes' carrier-offset tolerance goes from a wall to the bank's comb (issue #326).** No mode changes status. What moved, measured on the `sm-ota sim` rig (N=100, seed 1, 60-byte frames, single modem before -> nine-branch bank after): **qpsk600 AWGN** -2/-1/0/+1/+2 dB 1/26/69/86/99 % -> 9/45/82/94/98 % (zero TXDELAY; at 150 ms TXDELAY, N=200, -1/0 dB 23/66 % -> 58/90 %); **qpsk600 CFO at +4 dB, 150 ms TXDELAY** 0/3.75/7.5/15/30 Hz 99/93/96/75/**3** % -> 100/99/100/100/**100** %, and at zero TXDELAY 3.75/7.5/15 Hz 100/99/71 % -> 100/100/100 %; **qpsk2400 AWGN** +5/+7/+9/+11 dB 3/72/97/99 % -> **35/90**/99/100 %, **CFO at +9 dB** 3.75/7.5/15/30 Hz 98/94/95/89 % -> 100/100/100/100 % (and 39-40 of 40 at 60 and 120 Hz, the comb's edge at 1200 Bd, bpsk1200's RSP1 figure from #116); fading rows up a few points on both modes (qpsk600 Good +4/+8/+12 57/74/80 -> 63/78/88, Moderate 27/40/49 -> 38/50/56, Poor 6/6/10 -> 6/8/11; qpsk2400 Good 24/34/42 -> 37/49/55, Moderate 8/13/17 -> 7/17/17, Poor 2/2/2 -> 2/3/5) - still equaliser-bound, the known limit stands; **qpsk3600** (one-branch bank by default, FM, its detector unchanged) AWGN +8 dB 66 -> 81 %, fm-mic +8 dB CNR 62 -> 78 %, fm-data 54 -> 80 % (N=50), the 15-30 dB FM rows 100 % as before. Four things did it, each isolated on the ladder and on the real burst below. (1) **The decision-feedback reference decides from cold, as BPSK's does.** The 2026-08-07 campaign gated the handover on the fourth-power offset window turning coherent and recorded an AWGN null for the reference; on the real burst that window is coherent through the all-reversal preamble and incoherent for the entire payload (the fourth power of a noisy phasor is far noisier than BPSK's square), so the reference never decided the part that matters, and the gate even handed the payload back to the plain product mid-frame. (2) **The rotation tracker's gain drops 0.05 -> 0.01 once DCD asserts.** At 0.05 the tracker's own jitter left the reference no better than the product at the 0 dB knee (18 degrees rms either way - the mechanism of that null); at 0.01 it reaches the 14 degrees a tracker-free reference reaches, and 12 against the product's 16 on the off-air burst. The acquisition gain stays 0.05 until DCD because a low gain cannot pull a half-step residual in from cold, and the tracker now runs from the first symbol (clamped to one bank step, leaking toward zero between bursts), which is what the campaign's "60 % at 8 Hz from cold" lacked. (3) **Sub-sample crossing interpolation into the DPLL**, which BPSK's demodulator always had and QPSK passed as zero: the single biggest lever, worth ~1.5 dB on qpsk2400 at 10 samples per symbol (N=200, +6/+7 dB: 97/163 -> 156/187 at zero TXDELAY, 111/171 -> 182/193 at 150 ms). With the truth of a synthetic 0 dB burst in hand, a fixed clock at the right phase decoded it with zero wrong bytes where the DPLL left six: the knee was timing-bound, not detector-bound. Inertia 0.94 on top (0.96 loses the 16-symbol zero-TXDELAY acquisition, as #236 found at its 24-bit minimum). (4) **The product the DPLL and DCD see is de-rotated by the seed-plus-tracker estimate**, and **the seed re-arms on the window's rising edge and is released on the DCD falling edge** (with a three-second backstop decay), never on window collapse alone: releasing it mid-payload had handed an 18-degree-per-symbol burst to a 9-degree tracker on whichever bank branch's window emptied first. A lone branch now copies +-30 Hz at +4 dB where the campaign measured 3 %. **`QpskMultiModem`** is `BpskMultiModem` on the QPSK factories (best-residual branch choice, honest null offset, the coherent second detector as the ensemble knob; `ModemOptions.SecondDetector` now accepts the qpsk banks), wired into the `qpsk600`/`qpsk2400` catalogue arms at 4 pairs x baud/40 and into `qpsk3600` at 0 pairs (FM tones arrive on frequency and its chain runs threefold upsampled; `OffsetPairs` widens it on request). QPSK frames now carry per-dibit confidence into `Il2pReceiver`, so the erasure ladder and chase decoding that were BPSK-only reach the QPSK family. **Masks moved, all upward:** `WattersonMaskTests` gains qpsk600 awgn +2 (39/40, floor 34), 0 dB (33/40, floor 26), +4 dB at 15 and 30 Hz CFO (40/40, floor 36), qpsk2400 awgn +9 (39/40, floor 34), +7 (38/40, floor 32), +9 dB at 60 and 120 Hz CFO (39 and 40 of 40, floor 34); the existing +4 and +11 dB rows stand at 40/40. **The fixture does not copy, and its README now says exactly why.** A fixed-clock bench receiver swept through all 40 clock phases recovered the frame (a TARPN ID beacon, N2IRZ-2 > ID, 103 bytes; the hex is in the README): hard Reed-Solomon decodes it at one phase with exactly 8 corrected bytes, the code's limit. Against that truth the deployed chain syncs with 0 bit errors, decodes the header with 0 corrections and leaves **10 wrong bytes** in the single 103-byte payload block, six of them consecutive in the first 70 ms of payload and wrong at every clock phase - damaged in the signal, with high decision margins, so no erasure ranking tried (amplitude-scaled or phase-only, pair-min or windowed) can flag them inside the 16 the code affords. The bank's best branches leave 9. "+1.7 dB" was an average over a burst whose payload start is not there; the fixture keeps its value as a burst that can now be scored in wrong bytes against an 8-byte floor. **Found on the way:** QPSK's `PacketDcd` was already marginal on main - it asserts only with the carrier on frequency and noise present (never under a 7.5 Hz offset, never on a noise-free burst, never with the 16-symbol zero-TXDELAY preamble), because the product's quadrant flickers at every phase-change null and only about half its transitions land inside the eighth-symbol window; the de-rotation above adds the +7.5 Hz case and the bank tests pin the counters and the offset measurement rather than DCD. The tracker gear and the seed release therefore only engage on bursts with a real preamble, which is every real one. Also: `sm-ota sim` aborted with "Internal CLR error" in 5 of roughly 100 runs on the dev box during this work (a minidump is kept under the job's scratch directory); not reproduced in 25 isolated runs, not seen in the suite, not understood. **#11 and #116:** qpsk600's +-5 Hz wall is gone in simulation (a lone branch at +-30 Hz, the bank at its comb, the seed to +-37.5 Hz beyond it) and the QtSM fixture still passes, but neither is closed here - #11 needs its live QtSM retest and #116 still covers afsk300. On-air for the QPSK family remains, as the campaign left it, without a slot to monitor.
- **2026-08-21 (later4) - the off-air `qpsk600` fixture copies, and every PSK mode gains ~1 dB at the knee: seven timing phases per symbol and a clock that holds once a burst is established.** No mode changes status; `qpsk600` has its first real-signal copy of a burst the shipping receiver could not read. Tom's question after the #326 PR was why a receiver could not do what the bench had done to the fixture, which was to try the clock phases and keep the one that passed, and the first answer ("it copies at the luckiest clock phase, not at the clock the receiver recovers") was wrong in the way that matters: the receiver's clock was the problem, not the clock phase. Two things, both measured on the sim rig (N=200, seed 1, deterministic, so every difference is real) and on the fixture. **(1) Timing diversity.** `TimingDiversity` gives `QpskDemodulator` and `BpskDemodulator` seven decision phases per symbol - the recovered clock instant and 2.5, 5 and 7.5 % of a symbol either side, interpolated from a short ring of baseband samples (the late phases wait for samples that arrive after the instant, so decisions are emitted a few samples late, which nothing downstream notices); the matched filter, mixer, clock and offset window are shared, each phase keeps its own reference, tracker, polarity and confidence state, and `QpskModem`/`BpskModem` run one `Il2pReceiver` per phase behind a symbol-clocked content dedupe shorter than any frame, so a frame any phase reads is delivered once. Diversity off -> three phases at 5 % -> seven at 2.5 %: **qpsk600** -1/0 dB 100/166 -> 131/185 -> 146/187 (zero TXDELAY), 112/180 -> 145/193 -> 157/198 (150 ms); **qpsk2400** +6/+7 dB 155/180 -> 184/192 -> 190/195; **bpsk300** -5/-4 dB 133/184 -> 164/195 -> 174/197 (zero TXDELAY), 131/182 -> 160/192 -> 173/194 (150 ms); **bpsk1200** +1/+2 dB 131/186 -> 160/197 -> 173/198; CFO rows hold or improve (qpsk2400 at +9 dB, 15 Hz: 84 -> 98 %; bpsk1200 at +2 dB, 30/60/120 Hz: 84/85/89 -> 97/98/99 %); qpsk3600, detector unchanged, AWGN +8 dB 81 -> 95 %, fm-mic +8 dB CNR 78 -> 85 %. **(2) The clock holds once a burst is established.** A rigid clock copies the fixture at one phase two samples from where the DPLL acquires; the DPLL at 0.94 then wandered 2-10 samples through the payload, which is why the three phases around it landed on 10-12 wrong bytes, and inertia 0.96-0.99 from cold loses the 16-symbol zero-TXDELAY acquisition (#236's finding). So the clock acquires at 0.94 (BPSK 0.92) and holds at 0.995 from the moment the offset window seeds or DCD asserts. With that, `pdn-decode samples/offair/2026-08-21/packet-24738.wav` **copies the frame** - N2IRZ-2 > ID, the TARPN beacon the README records as the truth - with 6 bytes corrected against a limit of 8 (8 with the three-phase set; the hold alone, without the wider phases, is what crosses the line). On the sim knee rows the hold is exactly neutral, because nothing establishes a burst there at zero TXDELAY (the numbers above are identical with it on or off); on BPSK, where the gate is DCD itself (its square-law seed fires before the clock has converged, and a clock frozen then measured the QtSM bpsk300 fixture 10 -> 8 frames and DCD itself no longer asserting), it is neutral on the sim rows too; a 0.5 % correction per transition still follows a 500 ppm symbol-rate error with a tenth of a symbol of lag. **Two faults the measuring found and fixed.** The first cut of the phases measured a null on every row and the fixture: a static-initialiser ordering slip had them at [0, -0, 0], and it was the per-phase fixture scoring (byte-identical errors on all three) that caught it, not the ladder. With the phases live, `Qpsk2400_Differential_Acquires_A_Short_Preamble_After_Idle_With_Noise` lost a burst, and the probe showed the QPSK rotation tracker free-running on noise between bursts at the acquisition gain and parking at its clamp (30 Hz at 1200 Bd) more often than not, where a 24-symbol preamble cannot unlearn it; the #328 chain carried the same walk and a two-sample change to its path exposed it. `QpskDemodulator`'s trackers now integrate only while DCD, a coherent window or in-band energy says a signal is there, and restart from zero at a burst's first energy; the scenario is 10/10 at every noise level from 0.03 to 0.16 and qpsk600's knee gained a few frames on its own (94/162 -> 100/166 with diversity off). **BPSK's tracker stays free-running**, as #236 measured: gating it the same way cost bpsk1200 131/186 -> 120/170, the selection diversity its 90° margin can afford and QPSK's 45° cannot. **Masks:** four rows added a rung below the existing knee rows, floors 2-3 binomial sigma under the N=40 smoke measurement (bpsk300 awgn -5 dB floor 30, measured 37/40; bpsk1200 awgn +1 dB floor 22, measured 34/40; qpsk600 awgn -1 dB floor 22, measured 32/40; qpsk2400 awgn +6 dB floor 26, measured 37/40); every existing row measures at or above its recorded value (bpsk300 -4 dB 40/40 against 37, good 0 dB 24/40 against 22, moderate +2 dB 25/40 against 21, poor +9 dB 7/40 against 7; qpsk600 0 dB 37/40 against 33; qpsk2400 +7 dB 38/40, +9 dB 39/40; every CFO row 40/40). Cost: the decision stage and six extra deframers per branch; the front end is untouched. The fixture README carries the outcome beside its truth; `samples/offair/2026-08-21/` is now a real-signal regression test that passes, pinned in `OffAirQpskTests`.
- **2026-08-21 (later5) - the two C4FSK modes get seven timing phases and the clock hold, and every knee moves 1 to 2 dB.** No mode changes status; `c4fsk9600` and `c4fsk19200` were both working and are now working at materially lower signal. PR #330's technique applied to `C4fskModem` per issue #331: the decision stage runs at the recovered clock instant and 5, 10 and 15 % of a symbol either side, each phase interpolated from a ring of the slicer's normalised input, each with its own 5-tap equalizer, its own freeze hysteresis and its own `Il2pReceiver`, and the modem delivers whichever phase reads a frame once, behind a 32-symbol content dedupe; the filter, energy gate, envelope and clock are shared, and the decision is emitted three points (a third of a symbol) after the instant so the late phases have their samples. The clock acquires at Dire Wolf's 0.74 and holds at 0.995 from the moment DCD asserts, DCD here being the 30-of-32-well-timed-transitions kind, which is the clock having converged. **Measured** on the sim rig, N=200, seed 1, deterministic in (mode, channel, SNR, seed, bursts), before -> after of 200: **c4fsk9600 AWGN** TXDELAY 0, 16/17/18 dB 0/32/104 -> 10/106/182; 150 ms, 20/21/22/23 dB 4/15/36/69 -> 16/39/83/138. **c4fsk19200 AWGN** TXDELAY 0, 19/20/21 dB 3/35/107 -> 23/118/180; 150 ms, 23/24/25/26 dB 1/9/39/89 -> 21/59/123/171. **c4fsk9600 fm-data** (the deployment channel, carrier-to-noise in the receiver IF) TXDELAY 0, 13/14/15/16 dB 1/8/33/73 -> 16/54/110/159; 150 ms, 15/16/17/18 dB 2/12/43/76 -> 8/43/123/176. **c4fsk19200 fm-data** TXDELAY 0, 13/14/15/16 dB 3/10/45/93 -> 22/74/133/174; 150 ms, 14/15/16/17 dB 1/9/33/70 -> 6/26/84/159. As 50 % knees that is c4fsk9600 fm-data 16.6 -> 14.8 dB CNR at TXDELAY 0 and 18.9 -> 16.8 at 150 ms, c4fsk19200 fm-data 16.2 -> 14.4 and 17.9 -> 16.3, c4fsk9600 AWGN 18.0 -> 16.9 and 23.9 -> 22.3, c4fsk19200 AWGN 20.9 -> 19.9 and 26.2 -> 24.6. **The step is twice the PSK modes'**, and that was measured, not assumed: both modes decide at 10 points per symbol at 48 kHz, so the PSK set's 2.5 % is a quarter of a point and c4fsk19200's underlying audio is only 5 samples per symbol. Summed over the eight knee rows (of 1600): no diversity 681, 2.5 % x 3 pairs 1223, **5 % x 3 pairs 1339**, 7.5 % x 3 pairs 1341, 10 % x 2 pairs 1292, 5 % x 4 pairs (nine phases) 1366. Reaching past ~15 % buys nothing and a ninth phase buys 27 frames of 1600, inside the noise, so the mode keeps seven phases and the PSK family's phase count. **Attribution**, same eight rows: the hold alone (one phase) 721, the phases alone (no hold) 1311, both 1339. The hold is exactly neutral on AWGN and worth 4 to 8 frames of 200 on each fm-data row, which is where these modes actually live; it is kept for that and because it is what the phases need to be able to reach anything. **Cost**: the decision stage and six extra deframers, no change to the front end. A fixed 100-burst point at one worker measures 2.68 -> 2.51 s (c4fsk9600) and 2.13 -> 2.29 s (c4fsk19200) of wall clock, so the sim rig is unchanged within its own scatter. **Masks**: nothing moved. Both C4FSK rows in the FM tier sit at 24 dB CNR, where both modes measured 25/25 before and after; the FM tier's rule is that rows sit on the flat, above the knee, and the knee is where this change shows. **Two honest negatives.** `fm-mic` is 0/25 at 30 and 40 dB CNR for both modes, before and after: a 4800 or 9600 symbol-a-second baseband does not pass a 300-3000 Hz emphasised voice path at any signal level, which is a mode limit and the reason these modes are data-port-only, not something a decision stage can move. And **TXDELAY costs these modes several decibels**, before this change and after it: c4fsk9600's AWGN knee is 16.9 dB at TXDELAY 0 and 22.3 dB at 150 ms, c4fsk19200's 19.9 and 24.6, with fm-data showing 2 dB of the same. A longer run-in making a receiver worse is backwards, it is the rank-deficient preamble training the equalizer's freeze hysteresis only partly cures (see the freeze comment in `C4fskModem`), and it is worth more than this change was. **The instrument.** Per-phase scoring of a known frame (`C4fskTimingDiversityTests`) - each phase's decided bits aligned on the clock instant's own sync word and compared byte by byte with what was transmitted - is asserted rather than assumed, after PR #330's [0, -0, 0] slip read a clean null on the ladder. It also found something the ladder cannot see: on a **clean** burst the +15 % phase misreads one symbol of the sync word while -15 % is byte-perfect, because the clock's wrap can only land on a sample, so the decision instant is already up to a tenth of a symbol late and the phases are not centred on the eye. Reading the decision back to the instant the clock asked for, as `QpskDemodulator` does with its wrap overshoot, is the obvious next thing to measure here.
- **2026-08-21 (later6) - the AFSK family gets the PSK chain's timing diversity and clock hold; every AFSK knee moves about a third of a decibel, and the measurement says plainly which half of the change earned it.** No mode changes status. `AfskDemodulator` now keeps its slicer's input in a short ring and decides every bit at the seven `TimingDiversity` phases (the recovered clock instant and 2.5, 5 and 7.5 % of a bit either side, interpolated, the late phases waiting for samples that arrive after the instant so a bit's decisions are emitted a few samples late); the front end, the clock, the envelope trackers and DCD are shared, phase 0 is byte-identical to the bit the plain sink has always been given, and each wrapper runs one deframer per phase behind a content dedupe clocked in bits - an `Il2pReceiver` per phase in `Afsk300Modem` and `Afsk1200Il2pModem`, an `HdlcDeframer` (plus the `Fx25Deframer` where there is one) per phase in `Afsk1200Modem`, and per phase per branch in `Afsk1200MultiModem`, whose own bank dedupe already merged copies. The clock acquires at the DPLL's usual 0.74 and holds at 0.995 once `PacketDcd` asserts, gated exactly as BPSK gates it. **Sim ladder** (N=200, seed 1, deterministic, before -> after): `afsk300-il2pc` awgn -3/-2/-1/0 dB 2/61/177/197 -> 4/92/187/198, and at 150 ms TXDELAY 1/80/174/199 -> 3/97/187/200; CFO 35/70 Hz at -1 dB 183/183 -> 191/190 and at 0 dB 200/199 -> 200/200. `afsk300` (plain HDLC, no FEC, so the benefit is any phase whose FCS checks) awgn -1/0/+1/+2 dB 8/70/161/195 -> 13/86/175/197. `afsk1200` awgn +4/+5/+6/+7 dB 3/27/107/152 -> 6/49/129/173; on the FM channels at +10/+11/+12 dB CNR, fm-mic 80/143/189 -> 85/147/189 and fm-data 80/148/185 -> 87/149/185. `afsk1200-il2p` at +7/+8/+9 dB CNR, fm-mic 8/93/168 -> 11/98/170 and fm-data 50/143/185 -> 60/151/188. **Where it buys nothing, said plainly.** On the fading channels `afsk300-il2pc` is flat (N=60 at 0-12 dB: good +12 dB 49 -> 50, moderate +12 dB 27 -> 30, poor +12 dB 3 -> 2, every other rung within a frame); a fade is not a timing problem and no number of decision phases makes it one. On the FM channels the gain is a fraction of the AWGN gain, because a limiter-discriminator link fails in bursts of carrier noise rather than at a mistimed sample - `afsk1200` at +12 dB CNR is identical either way. **The two halves, measured apart.** Diversity alone (hold disabled) reproduces the whole result to within a frame on every row tried (`afsk300-il2pc` -2 dB 92 and 97 with TXDELAY, `afsk1200` +6 dB 128 against 129, `afsk300` 0 dB 85 against 86), so the phases are the entire sim gain. The hold alone (phase 0 only) is exactly neutral on `afsk300-il2pc` (-2/-1 dB 61/177 and 80/174 with TXDELAY, the before numbers to the frame) and slightly positive on `afsk1200` (+5/+6/+7 dB 27/107/152 -> 28/110/157). It is kept for the reason `QpskDemodulator` measured on the off-air fixture rather than for a sim row: nothing establishes a burst at zero TXDELAY, and a clock that no longer wanders is the one the phases can reach past on a real capture. **Why 300 baud gains less than the PSK modes did.** Sweeping the decision instant plus or minus 15 samples over a known bit stream (`AfskEyeSweepProbe`) draws the eye: at 300 baud, 40 samples per bit, the error count is flat across a nine-sample plateau (offsets -6 to +2 all within 34/30 errors of each other), so the phases at 1 to 3 samples out usually decide the same bit and only the ones near the plateau's edge differ; at 1200 baud, 10 samples per bit, the useful window is about [-2, 0] samples and one sample late nearly doubles the bit errors (21 -> 39), which is why fractions of a sample matter more there than their size suggests. **The instrument.** `AfskTimingDiversityTests` scores a known 1088-bit stream at every phase against what was sent and requires every phase to disagree with the clock instant somewhere, at a noise level that costs bits; it is the guard against the [0, -0, 0] slip that made the first PSK cut read a clean null, and it also pins phase 0 byte-identical to the plain bit sink. **Masks:** one row added, `afsk300-il2pc` awgn -1 dB floor 30, measured 37/40 (N=40, seed 1); the existing 0 dB row measures 40/40 against its floor of 30 and its own recorded 39/40. The rung where the change actually shows, -2 dB, measures 16/40 -> 25/40 and is deliberately not pinned: at 62 % it sits on the cliff, where a mask measures the machine's floating point as much as the modem. **Cost:** user CPU +6 % on `afsk300-il2pc` (an 11-branch bank, so 77 IL2P receivers running) and +12 % on `afsk1200-multi` (15 branches); the front end is untouched and it dominates. Full suite green, and the real-audio fixtures are unchanged: the off-air `samples/offair/m9psy-40m-afsk300-il2pc-qrm.wav` QRM capture still decodes three ways, the NinoTNC AFSK parity captures and the QtSM `afsk1200` corpus still decode exactly. Issue #331, PR #334.
- **2026-08-21 (later7) - the direct-FSK modes take the timing phases and gain 1.5 to 2 dB at every knee; the clock hold that came with them on PSK is measured, rejected and explained.** No mode changes status. Issue #331's second item, rx-roadmap workstream 10. **(1) Timing diversity, which is the whole gain.** `FskModem` decides every symbol at seven phases - the recovered clock instant and 10, 20 and 30 % of a symbol either side, interpolated from a short ring of the slicer's own input, the late phases waiting for points that arrive after the instant so decisions come out a few points late - and gives each phase its own deframer, plus its own G3RUH descrambler and NRZI state on the classic leg, since both are bit-serial and a phase that reads a different bit must carry the difference forward. A frame any phase reads is delivered once, behind a symbol-clocked content dedupe 48 symbols wide. Phase 0 is the clock's own instant and reads exactly the level the DPLL sliced, so the long-standing behaviour is preserved as the first branch. Measured on the sim rig (N=200, seed 1, deterministic), before -> after: **fsk9600-il2p** AWGN +9/+10/+11 dB 45/128/177 -> 135/183/197 at TXDELAY 150 and 0/26/98 -> 10/92/164 at TXDELAY 0, fm-data +10/+11/+12 dB CNR 106/156/183 -> 171/188/199 and 108/164/186 -> 175/196/200; **fsk4800-il2p** AWGN +6/+7/+8 dB 16/74/133 -> 51/147/180 and 0/4/43 -> 1/26/112, fm-data +9/+10/+11 dB CNR 78/138/185 -> 137/181/193 and 81/146/184 -> 137/183/195; **fsk9600 classic HDLC**, where the only FEC is "some phase's FCS checks", AWGN +14/+15/+16 dB 63/119/159 -> 167/195/199 and fm-data +12/+13/+14 dB CNR 27/83/135 -> 136/188/198. Every rung above the knee stays at 100 % (AWGN and fm-data at +16, +20 and +30 dB), so nothing was bought by decoding worse when the signal is good. Two negatives that stay negative: **the classic leg still cannot acquire at TXDELAY 0** (0/200 before and after, at every rung to +20 dB - two opening flags is not a run-in and no number of decision phases invents one), and **9600 GFSK still does not pass a microphone path** (0/50 at +15, +21 and +27 dB CNR, so `Fast_Fsk_Cannot_Pass_Through_A_Microphone_Path` holds unchanged). One small positive nobody asked for: **4800 GFSK now gets its first frames ever through a microphone path**, 0/50 -> 4/50 at +27 dB CNR and 8/50 at +33, which is a trickle and not a usable mode, recorded because it was 0 at every level before. **The step is this mode's own, and was measured rather than inherited.** At 48 kHz these modes decide on ten points per symbol (9600 is interpolated x2, 4800 already has ten), so the PSK set's 2.5 % is a quarter of one point. At fsk9600-il2p AWGN +10 dB, N=200, TXDELAY 150: 128 of 200 with no diversity, 172 at 2.5 %, 178 at 5 %, 183 at 10 %, 183 again at 15 % - the union saturates by 10 %, which reaches 30 % of a symbol either side, so that is the step and going wider only spends deframers. Per-phase scoring of a known burst (the instrument #331 requires, and `The_Timing_Phases_Read_The_Same_Burst_Differently` as a pinned test) shows the phases are real and, interestingly, asymmetric: on one fsk9600 burst at the edge the instant got 21 bits wrong of 1360 while -10 % got 13 and +10 % got 62, because the clock wraps on the sample that overshoots and this chain does not interpolate its crossings, so phase 0 reads systematically late. Recentring the whole set 15 % early was tried and is worth nothing the symmetric set does not already cover (138/183/197 against 135/183/196). **(2) The clock hold: built, measured, rejected, and this is the honest negative of the workstream.** On the sim rows a DCD-gated hold at 0.995 is neutral, exactly as it is on BPSK: with diversity on, hold on against hold off, fsk9600-il2p AWGN +9/+10/+11 dB 135/183/196 against 135/183/197, fsk4800-il2p AWGN +6/+7/+8 dB 51/147/180 both ways, fsk9600 fm-data +12/+13/+14 dB CNR 136/188/198 both ways, and fsk9600-il2p fm-data at N=600 (+10/+11 dB CNR) 520/564 with it against 524/569 without, about one sigma down; with diversity off it is neutral to a frame (45/128/176 against the 45/128/177 this mode has always measured). It was verified live rather than assumed neutral - forced to 0.0 the same rows read 45/127/166, so the gate does fire and does change the clock. **What settled it was the cost.** A test this repository did not have, `The_Clock_Tracks_A_Mistuned_Transmitter`, resamples the burst as heard from a transmitter whose crystal is out by a given number of parts per million: as the mode stands it copies at +-2000 ppm at both bauds and both signs, and with the hold in it fails from +-500 ppm (both bauds, both signs, clean signal, 200 ppm the last rung that passes). The mechanism is this chain's one deliberate difference from the PSK chains, and it is in its own source comment: it does not interpolate its zero crossings, so a nudge is quantised to a tenth of a symbol and the loop's entire correction is (1 - inertia) times the measured phase. At 0.74 that is 26 % per transition and follows any real crystal; at 0.995 it is 0.5 %, the loop bandwidth falls by twenty, and a mistuned transmitter walks the clock past DCD's own window - DCD drops, the clock re-acquires, DCD asserts, the hold re-engages, and the frame dies in the limit cycle. A stiff clock and an unresolved crossing do not go together, so the hold is not shipped for these modes; `FskModem`'s inertia constant carries the whole measurement so the next person does not repeat it, and the ppm test is the guard. Worth revisiting only if this chain ever gets matched-filter timing, which is a standing note in `Process` from long before this work. **Masks:** six rows added, one per mode per channel, a rung below where each old knee sat, floors 2-3 binomial sigma under an N=25 seed-1 TXDELAY-150 measurement - fsk9600-il2p awgn +11 dB floor 21 (measured 25/25, was 22/25), fsk4800-il2p awgn +8 dB floor 17 (22/25, was 17/25), fsk9600 awgn +16 dB floor 21 (25/25, was 19/25), fsk9600-il2p fm-data +12 dB floor 21 (25/25, was 23/25), fsk4800-il2p fm-data +11 dB floor 21 (25/25, was 22/25), fsk9600 fm-data +14 dB floor 21 (25/25, was 15/25). fsk4800-il2p had no mask of any kind before this and now has two. No existing floor moves in either direction and the whole suite is green, including the Dire Wolf cross-validation, the NinoTNC parity set, the QtSM RUH-4800 interop pin and the `samples/pdn/` corpus QC. Cost: the decision stage and six extra deframers per modem; the filter, the envelope-tracking slicer, the clock and DCD are all shared and untouched.
- **2026-08-22 - QPSK packet DCD is scored on the symbol decisions rather than on slicer transitions: it now asserts on every decodable burst of the differential family (clean, off-frequency, minimum preamble), never on noise, and the tracker gear and clock hold it drives engage as designed (issue #329).** No mode changes status. Before this the differential QPSK path fed Dire Wolf's transition-timing `PacketDcd` with the quadrant changes of the one-symbol conjugate product, and the issue's measurement stood: DCD asserted for a qpsk600 burst only with the carrier on frequency and some noise present. The reason is worse than the issue put it. The product's angle is the phase CHANGE between consecutive symbols, so it changes quadrant only when consecutive dibits differ; at every phase change the product's magnitude nulls and its angle there is whatever noise makes it, so a noisy product flickers through the other quadrants around each null, and on the all-reversal IL2P preamble (a 180-degree change every symbol) a clean product never changes quadrant at all. The transition phases the DPLL reported were 40-60 % inside the eighth-symbol window in every condition, so 30 of 32 was never reachable by that route, and the de-rotation of #328 could only move the hump, not narrow it. **The envelope-null timing source the issue suggested first was built, measured and rejected** (an honest negative): feeding the DPLL one event per phase change at the matched-filter output's null, parabolic sub-sample interpolation, half-symbol hold-off, gave 39-54 % in window and a clean +60 dB burst still spread its nulls over half a symbol, because at qpsk600's 0.20 roll-off the neighbouring symbols move each null by up to a third of a symbol (data-dependent ISI self-noise), and the 0.995 clock hold then froze the clock at whatever offset acquisition had left (the hump's centre moved between the 40 and 60 dB rows). No per-transition scorer can reach 30 of 32 from those nulls. **What ships instead is `QpskDecisionDcd`**: at every clock instant the detector already has the symbol-instant sample relative to its reference phasor (the decision-feedback chain) or the de-rotated conjugate product (qpsk3600's plain-product chain); the fourth power of that unit phasor strips the data and leaves cos(4e), e the angle error from the nearer axis, +1 on a clean decision and zero-mean on noise; an exponential average over 32 symbols estimates exp(-8 sigma^2) of the decision's angle error, asserts DCD at 0.55 (about 16 degrees rms, a dB or so below every QPSK knee) and releases at 0.10 (about 31 degrees rms). On noise the average is 0.09 wide: ten minutes of idle noise at 300 Bd and at 1800 Bd produced no assert and a maximum of 0.36 and 0.37; the 10 s idle rows at three noise levels are 0 of 100 blocks. `PacketDcd` and `BitDpll` are untouched, the coherent QPSK path keeps both and its deframer-reset rule exactly as they were, and BPSK is not involved. **The public-API gate** (`QpskModem.Qpsk600`, differential, one 60-byte burst in 2.5 s of noise, DCD polled per 100 ms block, seed 1), before -> after in DCD blocks of 25: +8 dB at 150 ms TXDELAY, 0 Hz 14 -> 17; -7.5/+7.5/-15/+15 Hz 0/0/0/0 -> 15/15/14/14; +8 dB at zero TXDELAY (the 16-symbol preamble) 0 -> 16; +40 dB clean 0 Hz 0 -> 18 and 11.25 Hz 0 -> 12 (and that lone-modem burst now copies, see below); +60 dB 0 -> 18; 0 dB 0 -> 16 (150 ms) and 0 -> 14 (zero TXDELAY); -1 dB 0 -> 14. DCD asserts 24-54 symbols into a burst on frequency (inside a 150 ms preamble, 45 symbols) and 86-124 symbols in for a lone modem two bank steps out. qpsk2400 at +8 dB asserted 171 symbols into its 180-symbol preamble before and asserts at 46 now, covers the whole burst at zero TXDELAY (0 -> 3 of 3 blocks) and at +-7.5/+-15 Hz; qpsk3600 (plain product, FM chain) asserts at +8 dB on and off frequency too. The frame decodes in every one of those rows except the single -1 dB realisation, which decoded before and does not now while the N=200 row below gains six; its cost is the noise. **Release** is the cost: 60 to 130 symbols after the burst's end (200-430 ms at qpsk600, 50-110 ms at qpsk2400), where the old DCD released in 31 on the one row it had asserted on, because the average has to decay through the noise floor's own excursions; a release level of 0.25 (about 45-75 symbols) was measured first and rejected below. **What DCD drives, measured one at a time on the knee rows (N=200, seed 1, deterministic):** the tracker gear (acquire 0.05 -> track 0.01 on DCD) is worth 40-50 frames: with it disabled qpsk600 at -1 dB falls 149 -> 101 and qpsk2400 at +6 dB 188 -> 101; the clock hold's inertia is immaterial on the sim rows (0.995, 0.98 and 0.94 give 140, 142 and 141 on qpsk3600 at +8 dB) and the hold now gates on DCD alone rather than on the offset window's seed as well, which cures a #330 side effect the probe found: a lone modem at +40 dB and 11.25 Hz (beyond the tracker's one-step clamp, so the seed has to do it) had its clock frozen by the seed while the pre-seed rotation residual was still biasing it, and the burst did not copy; with the hold waiting for good decisions it copies. **The one real regression found and fixed on the way:** with DCD now asserting at the knee, `QpskModem`'s reset of its deframers on the DCD falling edge fired mid-burst whenever the average dipped, and on qpsk3600 at +8 dB (where the plain product's coherence sits near 0.5, inside the first cut's 0.55/0.25 band) that cost 43 of 200 frames on AWGN and 30 on fm-mic: 16 of 50 probe bursts had an in-burst DCD drop and every one of those lost its frame, and disabling the reset alone gave every row back. The release level went to 0.10 (183/175 of 200 back from 140/144) and the differential path's reset now fires when the channel goes quiet, DCD and in-band energy both down, rather than on a DCD dip alone, which gave back the last one or two frames on two rows. **Ladder, before -> after (N=200, seed 1, 60-byte frames):** zero TXDELAY: qpsk600 AWGN -1/0 dB 146/187 -> 152/188; qpsk2400 +6/+7 dB 190/195 -> 190/194; qpsk600 +4 dB at 3.75/15/30 Hz 200/200/200 -> 200/200/200; qpsk2400 +9 dB at 15/60/120 Hz 192/199/199 -> 192/199/199; qpsk3600 +8 dB AWGN/fm-mic 183/174 -> 183/175. 150 ms TXDELAY: qpsk600 -1/0 dB 157/198 -> 163/199; qpsk2400 +6/+7 dB 195/199 -> 198/199; qpsk600 +4 dB at 3.75/15/30 Hz 200/200/200 -> 200/200/200; qpsk2400 +9 dB at 15/60/120 Hz 200/200/200 -> 200/200/200; qpsk3600 +8 dB AWGN/fm-mic 180/163 -> 189/175. Fading (N=100): qpsk600 Good +4/+8/+12 dB 69/81/88 -> 68/82/88, Moderate 44/54/62 -> 46/56/64, Poor 5/13/15 -> 6/13/14; qpsk2400 Good 12/34/48 -> 13/34/48, Moderate 3/9/19 -> 3/10/21, Poor 1/2/4 -> 1/2/4. The one row down is qpsk2400 +7 dB at zero TXDELAY, 195 -> 194, one seed; every other row holds or gains, the gains being the gear and the hold engaging on bursts they previously could not. No mask moves. **Fixtures:** `pdn-decode samples/offair/2026-08-21/packet-24738.wav` copies the N2IRZ-2 beacon with 6 bytes corrected of 8 as before and `OffAirQpskTests` passes; `NinoTncParityTests` (the idle-with-noise qpsk2400 scenario 10/10), `QtsmInteropTests`, the NinoTNC captures and the full suite are green; `QpskMultiModemTests` regains the two DCD assertions its BPSK twins have (the damaged burst asserts DCD, the offset is read while DCD holds). New: `QpskDecisionDcdTests` (unit, including a million-symbol noise run) and `QpskDcdTests` (the public-API gate as a test: nine burst rows, the noise rows, reset).
- **2026-08-22 (later) - a longer TXDELAY no longer costs the C4FSK modes anything on the data port and costs them under a decibel on AWGN, and the instrument that found the cause says it was not the equalizer.** No mode changes status; `c4fsk9600` and `c4fsk19200` were working and now work at lower signal after any run-in. Issue #336, from the 2026-08-21 (later5) negative: c4fsk9600's AWGN knee was 16.9 dB at TXDELAY 0 and 22.3 dB at 150 ms, c4fsk19200's 19.9 and 24.6, with 2 dB of the same on fm-data, and the named suspect was the 5-tap equalizer training on the rank-deficient 0x77 alternation. **The instrument** (`C4fskTxDelayProbe`, env-gated `C4FSK_TXDELAY_PROBE=1`, on the new `C4fskModem.DecisionObserver` seam, over the sim rig's own bursts, N=40 seeds) reads the receiver at the moment the sync word arrives, after a 20 ms run-in and a 150 and 300 ms one, at 18 and 22 dB (9600) and 21 and 25 dB (19200). **What it showed, before:** the equalizer's taps were within 0.03 of identity and frozen at the sync in every long-run-in case (dev 0.023 to 0.036), so the suspect is cleared; the envelope tracker was the cause. It was a per-point peak follower (attack 0.08, decay 1e-5), which on a noisy waveform is a max-hold that climbs to the largest noise excursion of the whole run-in: at the sync after 150 ms the half-swing sat 1.20 to 1.26 times the clean burst's at 22 to 25 dB and 1.34 to 1.38 at 18 to 21 dB, against 1.00 to 1.07 after 20 ms; the sync word's own outer symbols read 0.70 to 0.75 normalised against 0.89 to 0.94, a hair over the 2/3 slice; 32 of 36 front-of-frame errors at 22 dB were outer symbols demoted to inner; and at 18 dB the clock instant's stream never carried the sync word at all after 150 ms (0 of 40, against 40 of 40 after 20 ms). **The fix:** the follower keeps the job it is good at, reaching the signal from nothing inside the 20 ms run-in floor, for the first 32 symbols after the energy gate opens, and then hands over to a decision-directed envelope: one reading per symbol at the clock instant, outer decisions only, moving each peak 0.05 of the way toward its reading up or down alike, so noise averages out of the reference instead of being held at its maximum, and inner decisions hold it, which is the hold-through-inner-heavy-stretches behaviour the slow decay bought on the real corpus. After: half-swing at the sync 1.03 to 1.08 of clean at every TXDELAY, the sync outer symbols at 0.86 to 0.89, the sync found 29 to 40 of 40, c4fsk9600 at 18 dB decoding 32 of 40 after 150 ms where it decoded 0. **Measured** on the sim rig, N=200, seed 1, deterministic, before -> after of 200 (50 % knees in brackets). **c4fsk9600 AWGN** TXDELAY 0, 16/17/18 dB 10/106/182 -> 49/147/189 [16.9 -> 16.5]; 150 ms, 17/18/19/20 dB 0/0/3/16 -> 83/153/191/196 [22.3 -> 17.2]; 300 ms, 0/0/0/5 -> 55/148/181/197 [22.4 -> 17.5]. **c4fsk19200 AWGN** TXDELAY 0, 19/20/21 dB 23/118/180 -> 64/156/193 [19.8 -> 19.4]; 150 ms, 19/20/21/22 dB 0/0/0/3 -> 29/127/188/196 [24.6 -> 19.7]; 300 ms, 0/0/0/3 -> 18/120/184/198 [24.7 -> 19.8]. **c4fsk9600 fm-data** (CNR) TXDELAY 0, 12/13/14/15 dB 1/16/54/110 -> 23/88/161/186 [14.8 -> 13.2]; 150 ms, 0/0/0/8 -> 29/116/168/190 [16.7 -> 12.8]; 300 ms, 0/0/1/4 -> 29/101/164/188 [17.1 -> 13.0]. **c4fsk19200 fm-data** TXDELAY 0, 3/22/74/133 -> 54/126/176/195 [14.4 -> 12.6]; 150 ms, 0/0/6/26 -> 72/147/186/199 [16.2 -> 12.4]; 300 ms, 0/0/4/25 -> 73/150/181/196 [16.0 -> 12.4]. On the data port, which is where these modes live, the 150 and 300 ms knees now sit at or below the 0 ms knee on both modes, and every knee moved 1.6 to 4.7 dB. On AWGN the long run-ins finish 0.3 to 0.7 dB above the 0 ms knee, and that remainder is the rig rather than the receiver: the sim calibrates its noise against the mean power of the whole active burst, and the 0x77 alternation through our 1.0x shaping is a tone 2.15 dB hotter than the 4-PAM frame (measured, `C4fskPreamblePowerProbe`), so 150 ms of it hands the frame 1.0 dB less SNR than the 20 ms floor does and 300 ms 1.25 dB less; the FM axis is carrier-to-noise and has no such term. Recorded as a rig property, not changed. **Two measured negatives, kept out.** A running-mean envelope that restarts at the hand-over and again when the alternation ends, so the frame's own level replaces the preamble's within the sync word (the alternation reads 14 to 16 % above a symbol's level through the shaping, a clean-burst fact the probe also shows), lands the envelope prettily by the header and costs 138 frames over the six AWGN knee rows against the plain 0.05 tracker, neutral on fm-data. And reading the 4-PAM decision back to the instant the clock asked for (the wrap's point overshoot, as `QpskDemodulator` does) cures the +15 % phase's clean-burst sync misread that the later5 entry noted, and is a wash on the ladder: +11 to +22 frames of 600 at TXDELAY 0 and on fm-data, -15 to -34 at AWGN 150 ms, +4 net over the seven rows it was run on. Neither is in. **Fixtures**: `NinoTncParityTests` (TXDELAY-0 acquisition, both modes), `C4fskEqualizerTests` (the ISI-closed eye and the 20 s idle-then-burst), the MultiDecode round trips and the catalogue tests unchanged and green; the full suite 1785 passed, 0 failed. `C4fskTimingDiversityTests` still proves the phases differ, but its c4fsk19200 burst had to get noisier (sigma 0.20 -> 0.36): after the fix six of its seven phases read the old burst byte-perfect, which is the receiver improving, not the instrument weakening. The NinoTNC corpus probes (`C4fskContentProbeTests`) are env-gated on a corpus that is not on this box (`/tmp/nino-corpus` is empty here), so they were not run; the change is argued safe for them (a NinoTNC's 2-up-2-down preamble never alternates, the hand-over happens 32 symbols in, and a decision-directed outer reference sits below the peak, which moves ISI-squeezed outer symbols away from the slice, not toward it) but not proven, and that is the one thing a corpus run should check. **Masks**: the two C4FSK fm-data rows at 24 dB CNR are untouched (25/25 before and after); with both data-port knees down about 2 dB, a row a rung below is added for each at 18 dB CNR, TXDELAY 150, floor 36 of 40 against a measured 40/40 (seed 1) and 200/200 on the ladder. Regression cover: `C4fskTxDelayTests` pins the envelope at the sync after 300 ms to within 10 % of the 20 ms one and the sync outer symbols above 0.8, and a 300 ms run-in decoding at least what a 0 ms one does (c4fsk9600 22 dB, c4fsk19200 25 dB, 20 seeds each; 15 and 22 of 40 before).
- **2026-08-22 (later2) - a run of identical scrambled bits no longer reads as silence: the four ordinary frames a noiseless bench could never decode now copy, and the same latent fault is closed on every mode that shared it (issue #339).** pdn-qso's hermetic rig (its PR #21) found `bpsk300` deterministically failing 4 of 256 otherwise identical 56-byte UI frames - differing in one payload byte - over a perfectly silent channel, `RsFailures` and `CrcFailures` both zero, at every TXDELAY tried, while noise at even 120 dB SNR made every one decode first time. **The mechanism, traced to the line.** The demodulated bit stream was measured bit-perfect even on the failing frames; what killed them was carrier detect. Each failing frame's IL2P-scrambled stream holds a 48-bit run of one bit value starting at bit 270 (the other 252 values of that byte top out at 25), and a run of reversals is a full-strength alternating carrier whose differential product never changes sign: no slicer transitions. `PacketDcd.OnSymbol`'s quiet-symbol path - built so a squelched-silent channel cannot latch DCD on for ever - counted 24 transition-free symbols as carrier-gone and dropped DCD in mid-frame (measured at sample 12401 of a 29200-sample burst on all four frames), and `BpskModem`'s falling-edge deframer reset, the continuous-decode robustness fix, abandoned the collection with the sync word already consumed. Nothing was retried because nothing was left to retry: zero RS failures, zero CRC failures, exactly as reported. **Why noise "fixed" it.** During a reversal run the product grazes zero once per symbol at the envelope nulls, and which side of zero a graze lands is a float residue (measured decaying 3e-8 -> 3e-11 -> 2e-14 over successive grazes) that dies within a few symbols on a bit-exact input but chatters - as well-timed transitions - under any noise at all. Acquisition conditioned on noise being present, at its most fragile exactly where the signal is strongest, as the issue predicted. **The fix, in the shared class.** `PacketDcd.OnSymbol` now takes the demodulator's own per-symbol decision magnitude, keeps a ~32-symbol mean of it, and only counts a symbol toward the quiet drop when its magnitude has collapsed below a quarter of that mean: silence means no transitions AND no signal. All five users hand over their own evidence - BPSK the differential product (or coherent amplitude), AFSK and FSK their slicer excess, C4FSK its equalized reading, QPSK-coherent its baseband power. The assert path, transition scoring, and every falling-edge deframer reset are untouched, and release into real silence is measured landing on exactly the same sample as before the fix (bpsk300: release at sample 30601 for a burst ending at 29200, both sides). **Scope, measured not assumed - every one of the catalogue's 38 modes swept noiselessly, pre-fix and post-fix builds, over two 2048-frame families: the pdn-qso perf shape (256 session values x 8 sequence numbers, TXDELAY 300) and 2048 random-content 40-byte-payload UI frames (payload seeded by frame index).** Seven modes lose frames pre-fix on the perf family, all with the same frames and the same mechanism: `bpsk300`, `bpsk300-multi` and `bpsk300-nocrc` lose the 4 48-bit-run frames; `afsk1200-il2p` and both direct-FSK IL2P legs (`fsk4800-il2p`, `fsk9600-il2p`) lose 6, the four plus the two whose runs are 25 bits, because a held tone or a held NRZ level has no grazes at all, so those modes need no float coincidence and even noise does not save them (the issue's 300-arbitrary-frame afsk sampling had simply missed a 1-in-341 event; the pre-fix DCD traces drop inside the run, `fsk4800-il2p` at sample 16483 of a 20848-sample burst, `fsk9600-il2p` at 15465 of 17648); `afsk1200-il2p-nocrc` loses the 4. The random family corroborates independently: frame r1699 is lost pre-fix by `afsk1200-il2p`, `fsk4800-il2p` and `fsk9600-il2p` alike. Single `Afsk300Modem` and single `Afsk1200Il2pModem` lose the session-194 frame outright in direct traces, and the deployed `afsk300-il2pc` bank's escape at TXDELAY 300 was luck, not immunity: swept again at TXDELAY 150 it loses all six. The affected modes were also swept at TXDELAY 0 and 150 (pre-fix: the same frames fail; post-fix: none), consistent with the issue's every-TXDELAY finding. Clean pre-fix, with the structural reason stated: `bpsk1200` (the graze chatter happens to survive its 10-sample symbols - the pre-fix DCD trace holds through the whole burst), `qpsk600`/`qpsk2400`/`qpsk3600` (V.26A rotates at least 45 degrees every symbol by construction, and the differential family's decision DCD took #329's fix), classic `fsk9600` and the AFSK HDLC/FX.25 arms (bit stuffing bounds a run at six), `c4fsk9600`/`c4fsk19200` (bits are gated on in-band energy, so a run cannot read as silence), and the `freedv-datac*` and `ms110d-wn*` families (their own sync machinery, no `PacketDcd`). Post-fix: 0/2048 on every mode in both families. After: 0/2048 on every mode above. **Gates:** the new `NoiselessRunTests` pin the four frames through the single modem and the deployed bank plus the `PacketDcd` semantics both ways (a full-strength transition-free run holds DCD; 24 signal-free symbols still drop it), and all five fail without the fix; the full suite is green (1812 passed / 0 failed); the full `SM_MASK_GATE=1` Watterson ladder was run pre- and post-fix on the same seeds and is identical point for point (95 rows green both sides; the 81 per-point successes/trials readings match exactly) - the change is inert wherever noise exists, which is everywhere but a bench. Fix and evidence in PR #341.
- **2026-08-22 (later3) - bpsk300's factory and the deployed bank now transmit and match at the same 0.35 roll-off; the factory's 0.20 rested on a mis-measured NinoTNC reference, and the daemon never ran it.** No mode changes status, and no deployed behaviour changes at all: the fix moves the bench path to the air's value, not the air. Issue #340, found during #339 when a reproducer built against `BpskModem.Bpsk300` (roll-off 0.20) would not reproduce what pdn-qso saw through the catalogue's `BpskMultiModem` bank, whose branches take the constructor's 0.35 default. The history: 0.20 went into the factory on 2026-07-15 to match "the 328 Hz a NinoTNC's own mode-8 transmission measures"; the bank arrived three days later building its branches through the constructor and bypassing it; the QPSK bank (2026-08-21) routes branches through the per-mode factories precisely so per-mode configuration travels, but the BPSK bank never got that treatment, and `OccupiedBandwidthTests` certified the factory, so the divergence was invisible. Measured before deciding anything (`RollOffDiscrepancyProbe`, `ROLLOFF_PROBE=1`, deterministic seeds): **(1) the 328 Hz was the pre-issue-#2 highest-energy-window mis-measurement** - whole-burst and like-for-like, exactly as the shipping never-wider test measures, the same `samples/ninotnc/bpsk300.wav` reads **398 Hz**, its spectrum flat to about plus or minus 150 Hz with steep skirts (-27 dB at 250 Hz out, -44 dB at 300), wider than either of our candidate shapes (0.20 = 328 Hz, 0.35 = 352 Hz by the same meter), so the never-wider rule never forced 0.20. **(2) Loopback cost of the roll-off: none.** At the -5 dB AWGN knee (N=400) matched 0.35 copies 264 and matched 0.20 copies 268, statistically identical; the cross terms (246 and 258) put the worst filter-mismatch penalty near 0.2 dB. **(3) The interop direction favours 0.35, measurably.** The real NinoTNC burst under calibrated AWGN: rx 0.35 copies 248/400 against rx 0.20's 210/400 at -5 dB (about 4 sigma, roughly 0.4 dB), 36 vs 14 at -6 dB, 185 vs 174 and 199 vs 195 per 200 at -4 and -3. And the real-TNC bench interop was only ever validated at 0.35: nino-bench has always built bpsk300 through the constructor default, including the "all 13 modes re-benched, 6/6 both ways" run inside the very commit that chose 0.20, so the factory's 0.20 was never once measured against the real TNC. So the factory aligns to the bank, not the other way: `BpskModem.Bpsk300` now passes the 0.35 default, `OccupiedBandwidthTests` builds every mode through `ModemCatalog.Create` so the certified transmitter is the one the daemon keys (bpsk300's certified OBW becomes the bank's 352 Hz, 12 % inside the reference recording), and `BpskTxShapingParityTests` pins factory, bank and catalogue arm to sample-identical transmit audio for both bpsk modes so this class of divergence fails instead of lurking. The deployed receive path is untouched by construction and the mask smoke tier confirms it: all 48 rows green, the bpsk rows point for point at or above their recorded values (bpsk300 awgn -5 dB 37/40, -4 dB 40/40, good 24/40, moderate 25/40, poor 7/40; bpsk1200 34/40 and 40/40). Deferred, named: `qpsk600` keeps its 0.20 - its factory comment cites the same superseded 328 Hz figure, but its bank routes through the factory so both paths agree, its masks were measured at 0.20, and moving it is a separate campaign with its own A/B (issue #344). docs/ninotnc-loop.md carries the dated correction to the July table. Fix and evidence in PR #345.
- **2026-08-22 (later4) - the diversity banks and FX.25 variants stop swallowing byte-identical ARQ retransmissions: the content-dedupe window shrinks from a flat 3 s to a measured 200 ms and clears on carrier re-acquisition (issue #342).** No mode changes status; this is delivery-layer, no DSP moved. The defect: every bank (`QpskMultiModem`, `BpskMultiModem`, `Afsk300MultiModem`, `Afsk1200MultiModem`) and `Afsk1200Modem`'s FX.25 route merge deduped on frame content across `3L * sampleRate`, wider than any ARQ retry interval, so a stop-and-wait retransmission - byte-identical by definition, and AX.25's entire repair grammar is byte-identical repeats - was silently discarded. Measured with the issue's repro shape (same frame twice through `ModemCatalog.Create`, 600 ms gap): **14 modes delivered 1 of 2 before, 2 of 2 after** - `afsk1200-multi`, `afsk1200-fx25`, `afsk1200-fx25rx` (the fx25 pair measured for the first time; the issue had them by source census), the `afsk300` trio, the five `bpsk` names, `qpsk600`/`qpsk2400`/`qpsk3600`, plus the four PSK ensemble configurations. The window is now sized from what it actually compensates for, measured by the new `BankDedupeSkewProbe` (`DEDUPE_SKEW_PROBE=1`): copies of one transmission spread at most 130 samples at 12 kHz across the widest bank the catalogue builds (all of it the coherent-vs-differential detector gap; 50 same-detector, 0-10 on the AFSK banks, invariant from 25 to 200-byte frames), plus at most one feed slice of emission-clock quantisation - two dedupe chunks (200 ms) covers the worst case about twice over and sits below every bank mode's burst-time at any realistic TXDELAY, so two distinct transmissions deliver outside it (the one sub-window regime, a minimal frame behind an aggressively short preamble on the fastest mode, is exactly what the acquisition layer's test pins). Second, semantic layer: `FrameDeduper.CarrierAcquired()` clears the window when the modem's carrier detect rises after a drop - a re-acquired carrier is positive evidence of a new transmission however fast the timers run, proven by a test that fits two whole qpsk3600 bursts inside the residual window (50 ms gap) and still receives both. The FX.25 route window stays codeblock-wide (2040 bits, because a small frame's embedded-HDLC and Reed-Solomon readings really are up to a whole block of padding apart) but becomes direction-aware via `FrameDeduper.RecordDelivery()`: only the FX.25 reading is ever suppressed, the embedded-HDLC reading - which nothing remembered can be a copy of, since it always precedes its own block's FX.25 reading - is recorded and always delivered. **Gates:** `RetransmissionDeliveryTests` pins both properties permanently (all 14 retransmission cases fail against the pre-fix library; one-transmission-one-delivery holds at catalogue defaults and the 18-branch ensemble); full suite 1856/0 on the tree rebased over #345, with the bpsk skew re-measured there and identical to the sample (the probe's branches and the bank's transmit both always ran the constructor's 0.35 roll-off, which is the value #345 aligned the factory to); Watterson smoke tier 55/0, with the full ladder provably inert to the change (`SimBench` gives every burst its own payload, and content dedupe compares byte-identical frames only); consumer merge gates run against a local `0.40.1-dev342` pack: packet.net `Quality_Counters_Accumulate_Across_Frames` passes, pdn-qso's whole suite passes 444/0 (the held bump is unblocked), and packet.net's one remaining failure is the `qpsk2400-il2pc-multi9` mode string, issue #343, a separate defect. Fix and evidence in PR #346.
