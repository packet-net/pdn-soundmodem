# Figure D-9 — Constraint length 7, rate 1/2 convolutional encoder (doc page 219, PDF page 224)

Transcribed by eye from the page image (independent transcriber A), section
D.5.3.2.1.

## Prose (D.5.3.2.1)

A constraint length 7, rate 1/2 convolutional code shall be used prior to
puncturing. This shall be the same code as is used in the single-tone waveform
described in section 5.3.2 of this standard. Figure D-9 is a pictorial
representation of the encoder.

## Polynomials (as printed)

    (b0) T1 = x^6 + x^4 + x^3 + x^1 + 1
    (b1) T2 = x^6 + x^5 + x^4 + x^3 + 1

## Tap-by-tap wiring (read from the figure)

Shift register cells left to right: X^6, X^5, X^4, X^3, X^2, X, 1.
Input enters at the X^6 (leftmost) cell.

- Upper summing node → output b0 (polynomial T1): taps from cells
  **X^6, X^4, X^3, X, 1** (five taps; no taps from X^5 or X^2).
- Lower summing node → output b1 (polynomial T2): taps from cells
  **X^6, X^5, X^4, X^3, 1** (five taps; no taps from X^2 or X).

Both summing nodes are modulo-2 additions.

## Output order

"For each bit input to the encoder, two bits are taken from the encoder, with
the upper output bit b0, generated by polynomial T1(x), taken first."

## Octal form (derived, MSB = x^6 coefficient first)

- T1 (b0): 1011011 = 0o133
- T2 (b1): 1111001 = 0o171
