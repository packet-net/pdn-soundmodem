# MS110D Phase B — program plan (2026-07-24)

**Charter.** Design §6 (the single gate owner) defines Phase B's hard gate: **D-LXIV at mask, no allowance, AWGN + Poor, WN0–8+13, with Phase A regressions green**; the measured deliverable is an RLS-vs-NLMS A/B report. The AWGN column already holds (closeout: 10/10 at ≥3M bits, zero errors). The modulations (8PSK/16QAM) and RLS tracking landed in Phase A. What remains is exactly one thing: **make the Poor column true, then flip `MS110D_POOR_GATED=1`**. Umbrella issue: #69; feeding issues #64/#65/#67. Non-goals: WN9–12 (Phase C), off-air/OTA (Rung 3), 48 kHz WBHF.

Accept rule per point (§5.3 as restated at the A closeout): ≥3×10⁶ payload bits, ≥600 s simulated, zero acquisition failures; ≥30 errors → direct BER ≤ 1E-5, else the 97.5 % Poisson upper bound clears 1E-5. Evidence only through the `MS110D_MASK_LOG` chain, disjoint-seed cross-checked.

## 1. Starting position — the baseline sorts into three regimes

First complete Poor baseline (2026-07-23 closeout, measured-not-gated, evidence in `evidence/2026-07-23-phase-a-closeout/`):

| WN | Mode | Poor SNR | Baseline BER | × mask | Regime |
|----|------|----------|--------------|--------|--------|
| 4 | BPSK r2/3, U=96/K=32 | +10 dB | 2.36E-5 | 2.4× | **near** — the machinery works; needs margin |
| 13 | QPSK r9/16, U=256/K=32 | +11 dB | 6.2E-4 | 62× | **near-ish** — closes on core improvements |
| 3 | BPSK r1/3, U=96/K=32 | +7 dB | 8.7E-3 | 870× | structural |
| 5 | BPSK r3/4, U=256/K=32 | +11 dB | 2.2E-2 | 2 200× | structural |
| 1 | BPSK r1/8, U=48/K=48 | +3 dB | 2.85E-2 | 2 850× | structural |
| 2 | BPSK r1/4, U=48/K=48 | +5 dB | 3.67E-2 | 3 670× | structural |
| 0 | Walsh 75 bps | −1 dB | 8.1E-2 | 8 100× | structural (own detector) |
| 6 | QPSK r3/4, U=256/K=32 | +14 dB | 1.29E-1 | 12 900× | **broken** |
| 7 | 8PSK r3/4, U=256/K=32 | +19 dB | 4.66E-1 | 46 600× | **broken** |
| 8 | 16QAM r3/4, U=256/K=32 | +23 dB | 4.96E-1 | 49 600× | **broken** |

The regime split drives the whole plan. BER 0.47–0.50 is not a weak equalizer — it is random output, i.e. a **defect** (rotation, mis-mapping, self-corrupting feedback), and no amount of RLS science fixes a sign error. The structural tier has known physics deficits (below). Only the near tier is a tuning problem.

**What the baseline already tells us (facts, current code):**

- **The physics.** Poor = 2 equal-power Rayleigh paths, 2 ms apart, 1 Hz two-sigma Doppler → coherence ≈ 300 ms. U=256 modes solve the channel every 120 ms (≈2–3 solves per coherence time) and then hold that snapshot **static across a 107 ms data span** — the channel moves materially mid-frame and every downstream consumer (DFE decisions, BCJR model, LLR scaling) is fed a stale estimate. Deep fades arrive ~1/s and last tens of ms; the Long interleaver spans 5–10 s ≈ 5–10 independent fades — enough diversity **if** LLRs are honest about fade-time confidence.
- **The BCJR turbo path is BPSK-and-U>48 only.** WN1/2 (U=48) and every QPSK+ mode are excluded from MAP re-equalization; QPSK/8PSK instead get the DFE-re-solve turbo the WN6 handover suspects of self-corruption. That WN1/2 — the strongest codes on the ladder, with the tightest probe cadence (40 ms) — sit at 3 000× mask while WN4 (r2/3) sits at 2.4× points at the exclusions, not the codes.
- **The BCJR echo model is capped at lag 8** (3.3 ms) because the trellis is 2^delay states — the #64/#69 "echo ceiling". §3 removes this ceiling outright.
- **WN0 has no multipath or fade processing at all.** `TrackWalsh` is coherent Walsh correlation plus a decision-directed carrier PLL averaged over 8 symbols (107 ms — at the coherence time, so the loop is either too slow or too noisy), and the 2 ms echo = 4.8 chips of intra-symbol self-interference the correlator never models. 8.1E-2 at −1 dB is the expected result of that description.
- **WN6 vs WN13 is a 200× discrepancy on identical geometry** (both QPSK, U=256/K=32, 3 dB and one code rate apart). Either the coded operating point sits across a cliff, or something in the WN6 path is broken that WN13's margin hides. The autopsy (§2, B1) decides — compare **uncoded** symbol-error rates: matching uncoded SER ⇒ code operating point; diverging ⇒ equalizer/LLR defect.

## 2. Program structure

Five stages. Each has an explicit exit gate; families close under the full §5.3 accept rule before the next one starts. Diagnosis is a stage, not a vibe: **no fix lands without a written mechanism** — the Phase A audit's central lesson is that plausible fixes tuned against the rig produce numbers that die under de-rigging.

### B0 — instruments first (S/M)

The Phase A closeout proved the instruments are worth auditing as hard as the modem. Phase B needs three new ones before any science:

1. **Channel-truth genie.** The Watterson rig is ours, so the true tap trajectory is knowable: export it (env-gated, test-side) and give the demodulator a test-only injection seam for channel truth. A genie-aided run per point = the **achievability bound of the current detector under perfect channel knowledge**. This cleanly splits every deficit into *tracking* (genie ≪ measured) vs *detection/LLR* (genie ≈ measured, both bad) vs *infeasible-as-architected* (genie can't reach 1E-5 — escalate, don't tune). Validate the genie itself on the static rig, where truth is trivial.
2. **Failure-localizing telemetry.** Per-point: uncoded SER alongside coded BER (§5.3 already calls for it), errors time-stamped against the genie's fade envelope (are we only dying in fades?), turbo outcome counters (converged / reverted / aborted). All behind the existing evidence-file discipline.
3. **Off-rig discipline harness.** Watterson variants {1 ms, 0.5 Hz} and {3 ms, 2 Hz} plus the WID2 static, run as direction-checks (not gates) for every tuned constant. Nothing may encode the D.6.1 rig's exact numbers again — the delay-5 lesson.

Also in B0, the small honesty remainders from #64/#65/#67 that affect evidence quality: weighted-RLS consistency (weight scales the tap step but P always fully updates, so low-weight DD rows progressively **freeze** adaptation — scale the row into both, or drop the half-measure), λ policy stated against physics (current per-mode λ = 1 − ln10/U ∈ 0.952–0.991 vs design §2.5's 0.995 — RLS memory should be set by coherence time, not frame length; A/B it, feeds the §6 report), surplus decoded bits counted as errors, `Poor_At_Mask_Snr_Still_Uses_Turbo` made to assert something real, BCJR component tests on Gaussian noise with an **LLR-calibration** assertion (turbo trusts LLR scale; miscalibration silently costs dB), max-log documented as max-log, debug I/O out of library code, `_utpScratch` removed. Stretch, non-blocking: data-phase late entry, EOT-assisted termination, every-offset cold start (§5.1 leftovers).

**Gate B0:** genie validated against static truth; telemetry in the evidence chain; hermetic suite green; Phase A evidence set re-run clean.

### B1 — autopsies of the broken tier (time-boxed, M)

WN6, WN7, WN8, WN0 (plus a cheaper look at WN1/2) each get a root-cause note **before** any fix. Method per point: ablate turbo; first-pass vs turbo uncoded SER; genie-aided run; then the specific hypothesis list — WN6: the handover doc's ladder (turbo DFE-re-solve training on wrong QPSK decisions; LLR axis rotation after re-solve; scrambler/geometry mismatch) plus the WN6-vs-WN13 uncoded comparison. WN7: phase tracking loss vs detection (0.466 ≈ random says lock, not margin). WN8: the ~43°/frame rotation vs 16QAM decision regions, and the gain reference through Rayleigh amplitude — both currently uncompensated mid-frame. WN0: genie split of intra-symbol echo vs carrier-through-fades.

**Deliverable:** `docs/ms110d/phase-b-autopsies.md`, one section per point ending in *mechanism → fix family → genie ceiling*. **Gate B1:** every broken-tier point has a confirmed mechanism. Fixes for outright defects (sign/rotation/mapping bugs) may land here with regression evidence; anything that smells like tuning waits for B2.

### B2 — the science core (L)

The one architectural insight this plan commits to, plus the time-variation work, shared by every family:

1. **Time-varying channel representation.** The block-buffered architecture (everything is re-read for turbo anyway) makes retrospective interpolation free: both neighbouring probe solves are known before re-equalization runs. Interpolate the channel snapshot across each frame — per-position h1[]/h2[] for the BCJR (the API already takes spans; it is fed block constants today, #65), and probe-anchored **phase and gain ramps** for symbol decisions and LLR scaling (the 16QAM requirement, but it helps every mode: it converts the 107 ms staleness of U=256 frames into ≤ half-frame interpolation error).
2. **Chain-decomposed exact BCJR — removes the 2^delay ceiling and the BPSK restriction in one move.** For the sparse two-tap model h1 + h2·z⁻ᵈ, the symbol dependency graph splits into **d independent memory-1 chains** ({r, r+d, r+2d, …}: y[t] couples x[t] only to x[t−d], and per-symbol priors don't cross chains) — so per-chain BCJR has **M states** (constellation size), not M^d. Exact MAP under the model, no reduced-state approximation. Consequences: any echo lag becomes affordable (the 9 ms static's 21.6 symbols included — #64's ceiling gone), and QPSK (M=4) / 8PSK (M=8) BCJR are trivially cheap — the #69 worry about 4^L trellises dissolves. If the fractional 4.8-symbol echo demands a two-adjacent-lag model (taps at d and d+1), each chain becomes M² states — still cheap; measurement decides whether it's needed.
3. **Turbo/BCJR coverage extension.** U=48 (WN1/2) and QPSK/8PSK join the BCJR turbo path once (2) lands; the DFE-re-solve fallback is retired or repaired per the B1 verdict. 16QAM stays excluded until B3 decides on evidence (the 5×-LLR-scale trap is already a throw).
4. **RLS science** (the §6 measured deliverable): λ-vs-Doppler A/B, weighted-RLS fix from B0, probe-solve vs continuous-RLS tracking comparison → the RLS-vs-NLMS report, written against genie truth so it measures tracking error, not just BER deltas.

**Gate B2:** Phase A evidence set unharmed (AWGN 10/10, static, Doppler, hermetic); **WN4 Poor at mask under the full accept rule** — the nearest point is the proof the machinery works before the grind starts.

### B3 — family closure (XL, the bulk of Phase B)

Order by information gain and shared machinery, not by mask order:

1. **BPSK ladder: WN4 → WN3 → WN5 → WN1/2.** WN4 closes in B2. WN3/5 are the same machinery at worse operating points (WN5 adds the 107 ms cadence problem the time-varying model targets). WN1/2 add the U=48 turbo extension and the K=48 ridge/54-tap geometry at the ladder's lowest SNRs.
2. **QPSK: WN13 → WN6.** WN13 (62×) should close on B2 machinery alone; WN6 lands whatever its autopsy demanded.
3. **8PSK: WN7.** Chain BCJR + phase interpolation. Pre-registered fallback if it stalls at mask+2 dB: frequency-domain turbo equalization — an architecture decision taken explicitly, not a default.
4. **16QAM: WN8.** Probe-anchored phase+gain interpolation; amplitude reference through fades; turbo inclusion by measurement.
5. **Walsh: WN0 — its own mini-program**, run in parallel from B2 (it shares no DFE/BCJR machinery). Candidate ladder, genie-arbitrated before building: (a) two-finger probe-less RAKE with decision-directed per-path tracking; (b) noncoherent / differentially-anchored Walsh combining (the classic robust-mode answer to fades — likely the spec's intent, given no probes exist to track with); (c) chip-rate equalization ahead of the correlator. Longest-pole candidate of the whole phase; starting it early is the schedule hedge.

Every family closes per §5.3 full budget + a disjoint-seed (+10000) cross-check before the next family's tuning starts (background sweeps of later families are fine; conclusions aren't drawn from smoke).

### B4 — gate flip and closeout (S)

Full 10-point Poor sweep at full budget with `MS110D_POOR_GATED=1` armed, AWGN 10/10 + static + Doppler + OBW + hermetic regressions in the same evidence batch, disjoint-seed verification of every Poor point, evidence committed to `evidence/<date>-phase-b-closeout/`. `MS110D_POOR_GATED=1` becomes the standard sweep default; the nightly rotation (§5.3) gains the Poor points. `phase-b-closeout.md` mirrors the A record; design §6 row B marked closed same-day; plan.md amendment; #64/#65/#67/#69 closed with evidence trails; RLS-vs-NLMS report published.

## 3. Discipline (hard rules, carried from the Phase A audit)

- **No fix without a mechanism.** Structural/broken-tier changes cite their autopsy section.
- **No constant may encode the rig.** Every tuned value gets a physics justification and holds direction on the off-rig harness + disjoint seeds. (The BCJR delay-5 constant was silently load-bearing for an OOM constraint *and* rig-fitted — both failure modes of unexplained constants.)
- **Genie numbers are always labelled genie** and never cited as performance; SMOKE never cited as evidence; the `MS110D_MASK_LOG` chain is the only evidence path.
- **Phase A regression evidence re-runs before any merge touching the demod path** (cheap at workers=4).
- plan.md and the issues stay current as work lands; §6 remains the only gate table.

## 4. Risks and escalation

- **WN0 may be a redesign, not a fix** — no probes means nothing to anchor tracking on; if the genie says even perfect channel knowledge can't carry coherent correlation at −1 dB, the detector changes family (noncoherent), which is new science. Mitigated by starting its genie work in B1 and running it in parallel.
- **8PSK@19 dB / 16QAM@23 dB on 1 Hz Rayleigh are thin-margin detection problems.** If genie-aided runs can't reach 1E-5, escalate to the FD-turbo architecture decision immediately rather than tuning toward an unreachable bound.
- **Escalation rule** (mirrors Phase A's mask+4 dB rule): any family still > mask+2 dB when its stage budget closes gets a written stop-and-reassess — options on the table, cost of each — not more tuning.
- **Runtime is manageable:** the full Poor sweep fits an evening at `MS110D_MASK_WORKERS=4` (closeout measurement); WN0's 11 h serial point divides across workers. OOM discipline stands: fleets marked expendable (`choom +500`, baked into run-masks.sh), sweeps launched detached, 16 GB box.
- **Steep coded cliffs cut both ways:** several structural points may collapse to at-mask once LLRs become fade-honest (the WN13/WN6 cliff suggests operating points sit near a knee) — reason for the WN4-first proof and for re-measuring the whole table after B2 before assuming the B3 grind is long.

## 5. Issue mapping

| Item | Where |
|------|-------|
| #64 BCJR delay hard-coded → searched | done (Phase A closeout); ceiling itself removed in B2.2 |
| #64 bidirectional pass history, IsFlatChannel, CFO-immune fading detector | done (Phase A closeout) |
| #64 RLS λ deviation; RlsUpdate weight/P asymmetry | B0 → B2.4 (report) |
| #64 "~4.8 dB diversity" mechanism claim | B0 (re-justify from measurement or delete the claim) |
| #64 2-tap trellis ≤3.3 ms echo ceiling | B2.2 (chain decomposition) |
| #65 divergence protection (fixed-point revert), DD-row survival, per-dim noiseVar, dead-QAM16 throws, abort-path BeginTraining | done (Phase A closeout); acceptance-metric upgrade only if B1 implicates turbo |
| #65 per-position h1[]/h2[] block constants | B2.1 |
| #65 max-log doc, Console.Error/env-var in library, `_utpScratch` | B0 |
| #67 clock skew, ±75 Hz hermetic, WN×IL×K matrix, WN7/8 permutation, seed-offset knob | done (Phase A closeout) |
| #67 surplus-bit error accounting, weak turbo assert, BCJR test noise/calibration | B0 |
| #67 late entry / EOT termination / cold-start offsets | B0 stretch (non-blocking) |
| #69 QPSK BCJR, 8PSK path, 16QAM carrier recovery, BPSK deep-fade tracking, gate flip | B2/B3/B4 as structured above |

## Status log

### 2026-07-24 — B0 closed

All B0 exit-gate conditions met on `ms110d-phase-b0` (evidence: [evidence/2026-07-24-phase-b0/](evidence/2026-07-24-phase-b0/)). Instruments: the channel-truth genie landed with a bit-identical seam proof, ring-wrap/stream-lag guards, and one instrument-calibration lesson the plan §B0 anticipated — the first fading genie run was 26× worse than its baseline because noise-free rows produce the zero-forcing solve; the demodulator now measures σ² as mean |noisy−clean|² between its rings and restores the σ²·Σweight Gram term, making the genie the true-channel MMSE bound (validated: static rig 0 errors, WN4 Poor 3 vs 7 on matched seeds). Telemetry (uncoded SER, deep-fade error concentration from the rig's recorded tap trajectory, turbo outcome counters) is in every evidence line. The off-rig harness runs {1 ms, 0.5 Hz} and {3 ms, 2 Hz} — first light: WN4 clean on the former, BER 3.0E-1 on the latter (flagged for B1). Honesty remainders closed: weighted-RLS weight/P consistency (regression-pinned by DfeTests that fail on the old rule), surplus-bit accounting, the turbo assert asserts turbo, BCJR tests carry an analytic LLR-calibration identity, max-log documented, λ deviation documented, debug I/O host-side.

Results that moved during B0: the weighted-RLS fix alone took the Poor BPSK family up — **WN3 Poor to ZERO errors in 3.19M bits, confirmed at seed+10000 (first Poor point at mask)**; WN4 to 1.91E-5 canonical / 3.23E-6 disjoint (straddling); WN1/2/13 ~1.5–2× better; the broken tier (WN6/7/8, weight-1 fading paths — untouched by the fix) unmoved, as expected. Phase A hard gates all re-pass at full budget on the B0 code. The telemetry separates the regimes for B1: near points concentrate 38–48 % of uncoded errors in deep fades; WN7/8's errors are fade-uncorrelated (structural detection failure), WN7's turbo never converges (0c/88r) with coded BER WORSE than uncoded — an LLR-chain defect signature; WN0 barely codes (1.42E-1 → 1.12E-1). B1 (autopsies) is next: WN7 first (strongest defect signature), then WN8, WN6, WN0.

### 2026-07-24 (later) — B1 closed

All four broken-tier mechanisms confirmed in [phase-b-autopsies.md](phase-b-autopsies.md), each by ≥3 independent instruments, in a single day of instrument-driven diagnosis (the B0 investment repaying immediately). WN7: intra-frame rotation beyond ±22.5° collapses DD tracking into whole-frame phase smear (89/130 frames are donuts by the 8th-power concentration statistic), and the collapse persists because the anchored probe solve cannot re-lock — while the same chain decodes static 2-path with zero errors. WN8: same mechanism, phase AND amplitude, static 2-path clean at full budget (2.77E-6). WN6: not a defect — a rate-3/4 code cliff (its uncoded SER is BETTER than passing WN13's; the decoder falls off the threshold r=9/16 tolerates). WN0: echo self-interference refuted (static 2-path ZERO errors at −1 dB); flat Rayleigh alone costs 1.39E-2; adding the second path makes the coherent receiver 8× WORSE where a combining detector would gain diversity — the fix is the §B3.5 detector family, with flat-Rayleigh 1.39E-2 as the intermediate bar. New instrument: Ms110dScatterDiagnostics (per-symbol scatter + frame diagnostics + rig trajectory dumps); Static_2Path/Flat_Rayleigh isolation diagnostics now cover the autopsy WNs. B2 (time-varying channel representation + chain-decomposed BCJR) opens with one addition from the autopsies: collapse detection with a fresh non-anchored re-solve.

### 2026-07-24 (evening) — B2 closed

**The exit gate holds: WN4 Poor at mask under the full §5.3 rule on the merged code — 7.83E-6 canonical / 8.14E-6 disjoint (6.39M bits, ≥30 errors → direct rule, 0 acquisition failures, 5615 s simulated each)** — and WN13 Poor passed the full rule on its canonical seed too (5.70E-6), banked early for B3.2. Evidence: [evidence/2026-07-24-phase-b2/](evidence/2026-07-24-phase-b2/). What landed, in commit order: §B2.1 time-varying representation (per-symbol tap trajectory between the bracketing probe solves with rotation as a phase ramp, RLS tracking only the gated residual, per-probe gain-1 phase re-anchor, collapse detection with one fresh non-anchored re-solve per unhealthy episode, per-burst fading latch); §B2.2 chain-decomposed exact BCJR (M states per chain, exactness pinned by brute-force marginalization, the 2^delay ceiling and the BPSK restriction both gone); §B2.3 turbo on the chain BCJR for every PSK mode with a scrambler-exact echo model (the legacy lag correlation was phase-scrambled toward zero — a latent model defect) and per-position h1 from re-encoded mid-frame references; the flat-channel turbo skip retired with the DFE-re-solve fallback that motivated it. §B2.4 delivered: [rls-vs-nlms-report.md](rls-vs-nlms-report.md) — the frame-tied λ stands (0.995 never better across U=48/96/256), NLMS confirmed out of the signal path.

Poor table movement (smoke, vs the B0 baseline): WN2 3.67E-2 → 8.56E-5 (430×; the U=48 turbo exclusion was the bottleneck, as §1 predicted), WN13 6.2E-4 → 5.5E-6, WN4 1.91E-5 → 7.0E-6, WN5 2.2E-2 → 5.6E-6 (!), WN6 1.07E-1 → 3.5E-2 (cliff descending), WN7/WN8 broadly unmoved pending B3 (WN7's first-ever turbo-converged block; scatter BER 0.477 → 0.248 on the fixed realization). The collapse detector took three measured iterations (absolute MSE → MSE-relative → the Phase-A bad-probe criterion armed once per unhealthy episode) — each intermediate broke a different extreme (WN1 −3 dB AWGN false-fire; WN13 early-collapse blindness; WN2 K=48 fresh-solve death spiral), all caught by the instrument suite before merge. Known remainders carried to B3: WN13/WN6 disjoint-seed catastrophic-burst tail (rotation-invisible to correlation magnitude — the B3.2 entry autopsy), WN2 genie instrument defect on K=48 heavy-ridge modes (B3.1), WN5/WN2 full-budget closure runs, the whole-table re-measure before the B3 grind.

### 2026-07-24 (night) — B3 opened: the tail autopsy — two of four classes killed same-day

The B3-entry re-baseline's central finding (below 8PSK the mean is solved; the catastrophic-burst tail is everything) got its autopsy, and the tail turned out to be four distinct mechanisms, not one. Instruments first (evidence: [evidence/2026-07-24-phase-b3-tail-autopsy/](evidence/2026-07-24-phase-b3-tail-autopsy/)): a per-burst census in the mask harness (`MS110D_MASK_BURST_LOG`) that made the tail countable — WN5's 67,991 errors are ONE burst (99.97%), WN1's are 12 of 248 bursts with 236 at zero — and a corpse-reproduction rig (`Ms110dTailAutopsy`) that rebuilds any mask burst bit-exactly and dumps first-pass symbols against transmitted truth.

**Class A — carrier false lock (WN5 fatal, WN13-disjoint transient), FIXED.** The tail-superframe RefineCarrier refit's sequential phase unwrap random-walked ~3.5 turns through a deep fade (noise phases downweighted in the regression but still chaining the unwrap) and fitted −4.86 Hz on a burst whose initial acquisition read −0.53 Hz; beyond the ±4.17 Hz alias basin of the 120 ms frame, the tap-rotation trim locks the stable false fixed point at −1 cycle/frame — every probe statistic healthy, half of every data frame inverted (SER 0.55, uncoded worse than chance, 11/11 turbo reverts). Fix: `EstimateCarrierFit` — lag-product slope (faded groups self-suppress), no unwrap chain exists; unit tests pin the corpse scenario. The corpse decodes 0 errors.

**Class C — mid-fade burst abandonment (WN2 98.8% of errors, WN1 83%), FIXED.** The 25-consecutive-bad-probes SignalLost exit had frame-rate-scaled patience: 3 s at U=256 (never false-fired), 1 s at K=48 — and Poor fades outlive 1 s, discarding every abandoned burst's remaining bits. Patience is now wall-clock ~4 s uniformly.

**Post-fix, full §5.3 budgets: WN5 AT MASK (5.55E-6, bound 8.77E-6; was 2.10E-2; disjoint 1.02E-5 with no catastrophic bursts), WN2 1.59E-4 (was 1.32E-2; zero SignalLost), WN1 4.03E-3 with the only surviving errors in exactly the 2 Class-D bursts (was 2.31E-2), WN4 improved to 2.94E-6 bound 5.40E-6, WN3 0 errors unchanged.** WN13 hovers at the line post-fix (1.02E-5 canonical 6M, 1.26E-5 disjoint 3M) — Class A gone, benign tail owns it now — except one disjoint-6M specimen (w3/b5, 8,392 errors in ONE block): a fade cluster with the equalizer degraded between the dips (SER 0.5 at healthy and even up-faded gain), sitting exactly on the B3.2/B3.3 seam.

**Class B — genuinely marginal mean (WN6 spread over every burst)** — unchanged, stays the B3.3 program with WN7. **Class D — LLR overconfidence (2 WN1 bursts), mechanism found, fix next**: first-pass LLR scales are fixed per mode (BPSK 4·Re(y)) with no per-frame noise normalization, so fade-crushed frames emit confident garbage that dominates the Viterbi metric through the deinterleaver — a 15% sign-error stream that d_free=40 should decode clean comes out 49.6% wrong. Exonerations were experimental, not argued: turbo (new `DisableTurbo` option — identical errors), timing (accept@ diagnostics — identical lock noisy/noiseless), stream offset (decoded/payload cross-correlation — none). The per-frame σ² calibration fix is the B3.2 opener, and the benign near-miss tails on every sub-8PSK point (WN2's remaining 484, WN5's 18, WN13's ~24-66) are plausibly the same poison in small doses.

### 2026-07-24 (late) — Class D closed: the WID misread; WN1 Poor at ZERO errors

Class D was not LLR calibration — it was the WID. The autopsy rig's lock-info dump showed both corpses running wrong decode parameters for their whole bursts (K9 against a K7 transmission; Medium/K9 against Long/K7): the WID, read from ONE preamble super-frame, was corrupted by a −15 dB preamble fade and passed its weak checksum. Every alternative died by experiment first (turbo via the new `DisableTurbo` option, timing via the `accept@` diagnostic, stream misalignment by cross-correlation, decoder-order clustering against a random baseline, and per-frame LLR σ² derating — implemented, measured ineffective at ≤2×, reverted rather than landed unvalidated; the post-solve-residual-is-in-sample lesson is written into the evidence README for the benign-tail program).

Fix in two measured iterations (evidence: [evidence/2026-07-24-phase-b3-tail-autopsy/](evidence/2026-07-24-phase-b3-tail-autopsy/) `census-wid/`): soft-vote the WID across up to 5 pre-data super-frames (zero latency), then — after the census caught the unguarded vote breaking a wrong-CFO-bin burst that the old fail-and-relock path had saved — a vote-confidence gate (mean dibit margin ≥ 0.20, measured 4.6× separation) that restores the safety valve. **Final battery: WN1 Poor 0 errors / 3.04M bits / 248 bursts (bound 1.21E-6) — the second zero-error Poor point. WN2/WN5/WN13 bit-identical (the vote is neutral on healthy acquisitions), WN4 3.82E-6 at mask, WN3 0, AWGN 10/10 zero, static/Doppler zero.**

**B3 state after day one:** at mask — WN1 (0 errors), WN3 (0 errors), WN4, WN5; hovering at the line — WN13 (canonical 1.02E-5 at 6M, disjoint benign tail + one fade-cluster specimen); above — WN2 1.59E-4 (484 benign near-miss errors), WN6/WN7/WN8 (Class B / B3.3-B3.4), WN0 (B3.5). Next work, in order: **B3.1** repair the K=48 genie (σ²·Σweight over-regularization) so the WN2 margin work has a trustworthy bound; **B3.2** the benign near-miss tail (WN2's 484, WN5's 18, WN13's 24–66 + the fade-cluster block w3/b5 banked in `census-wid/`) — candidate mechanisms: fade-recovery tracking quality (the specimen shows SER 0.3–0.5 at healthy gain between dips) and the shelved LLR σ² calibration, which needs an out-of-sample noise statistic; **B3.3** 8PSK (WN7 relock latency; WN6 rides the same work); **B3.4** 16QAM; **B3.5** WN0 detector family; then **B4** — full-table gate runs and the MS110D_POOR_GATED=1 flip.

### 2026-07-24 (late night) — B3.1 closed: the genie was never broken by the ridge — and the baselines redraw the B3 map

The filed hypothesis (heavy-ridge σ²·Σweight over-regularization on K=48) is refuted by measurement (evidence: [evidence/2026-07-24-phase-b31-genie/](evidence/2026-07-24-phase-b31-genie/)). The banked WN2 genie 3.12E-2 reproduced bit-exactly on the pre-B3.0 commit, and the backported census localized ALL 15,328 errors to ONE SignalLost-truncated burst (abandoned at bit 9,216/24,544; 19/20 bursts zero) — the genie's clean ring still fades, so the 1 s K=48 patience fired there exactly as in the normal path. The genie failure WAS Class C; the wall-clock patience fix repaired both paths at once. On current main the same seeds read genie 0 errors (WN2 and WN1), WN4 bit-identical to the stale run (the K=32 control was never broken). The σ²·Σweight term itself is exactly the noise diagonal noisy rows carry implicitly — same expected Gram, trace, and ridge — so there was never an over-regularization to fix; the one structural genie caveat (the per-symbol RLS recursion lacks the compensation and has a ZF fixed point, bounded by the per-frame tap reset) is recorded in the evidence README with a measured-impact-none verdict. No library code changed.

**The full-budget genie baselines are the real payoff — a measured attribution map for everything left in B3.** WN2 genie: **0 errors / 3.04M / 124 bursts (bound 1.21E-6)** against normal 484 — the whole benign tail is tracking deficit, B3.2's biggest lever. The fade-cluster specimen (WN13-disjoint w3/b5) under genie: **8,392 → 12 errors**, dead block repaired, turbo 11c/0r — tracking deficit, real headroom for B3.2/B3.3 fade-recovery work. But WN5 genie 19 vs normal 18 and WN13-canonical genie 59 vs normal 24–66: **detector ceiling** — their residue is NOT reachable by tracking; the lever there is detector-side (the shelved out-of-sample LLR σ² calibration, or erasure marking). And WN6 genie 2.30E-2 / WN7 genie 4.52E-1 ≈ normal: **detector-limited outright** — WN7 fails identically with a PERFECT channel estimate, so B3.3's pre-registered FD-turbo fallback is promoted to the expectation, not the fallback. B3.2 opens next with a sharpened target list: tracking work aimed at WN2 + the fade-cluster class, detector-side calibration aimed at the WN5/WN13 residue.

### 2026-07-25 (small hours) — B3.2 first result: WN2 Poor AT MASK — the anchor-ridge fix

B3.1's attribution (WN2's tail = pure tracking deficit) paid out in one autopsy cycle (evidence: [evidence/2026-07-25-phase-b32-wn2-anchor/](evidence/2026-07-25-phase-b32-wn2-anchor/)). The worst tail burst's corpse pair localized the deficit: NOT fade-edge lag (fade-entry/recovery classes hold ~8% of the normal-vs-genie SER excess), NOT LLR overconfidence (deep-fade symbols already shrink honestly to |Re|≈0.19), NOT DD decision poison (genie shares the noisy decisions — it cancels in the pair), and NOT the RLS λ (0.995 → no change; the recursion's deviations are discarded at frame end) — but a FLAT +0.02 SER estimation-noise tax across healthy frames, i.e. per-probe solve noise, and a coded cliff sitting razor-thin between the hardest block's normal (0.186) and genie (0.171) uncoded SER.

The fix is one number: the K=48 per-probe anchor ridge, 1.0 → 8.0 (the anchor ridge IS the solve's cross-frame memory). Measured sweep 0.5/1/2/4/8/16 → 43/42/20/5/**1**/23 coded errors on the WN2 smoke — an interior optimum, and the mechanism is error-CONFIDENCE, not error-count: at ridge 8 the corpse decodes 0 (was 54) with MORE symbol errors, because wrong-sign LLR mass drops 2.35× — the anchored equalizer coasts instead of chasing fades with noisy solves, and where the channel deviates the output amplitude collapses, so errors self-report low confidence (soft erasures). The 40 ms K=48 frame puts 8-frame memory ≈ 300 ms inside the 1 Hz coherence time; the same move at U=256 is measured-catastrophic (WN13 at 4× its ridge: 4.9E-2) — which simultaneously validates the B3.1 detector-ceiling attribution for WN13/WN5 (more solve memory helps them negatively). New A/B knob banked: `MS110D_MASK_TRACK_RIDGE` / `Ms110dDemodOptions.TrackRidge`, same report-only rule as the λ knob.

**Validation: WN2 Poor 18 errors / 3.04M / bound 9.35E-6 canonical AND 12 / 6.89E-6 disjoint — AT MASK on both seed sets, the fifth sub-8PSK Poor point. WN1 2 errors canonical / 0 disjoint. WN4/WN5 bit-identical. Phase A: AWGN 10/10 zero at full budget (incl. the −3/0 dB K=48 gates the old ridge defended), static WID2 zero, Doppler ±75 zero.** Remaining in B3.2: the WN13 fade-cluster specimen (tracking deficit at QPSK, where the ridge lever is unavailable — needs the non-causal trajectory-anchor smoothing idea or B3.3's re-lock work) and the WN5/WN13 detector-ceiling residue (LLR-calibration lever, out-of-sample noise statistic). Then B3.3.

### 2026-07-25 (morning) — B3.2 closed: the fade-cluster specimen is probe-information-limited; the smoothing candidate measured and reverted

The WN13-disjoint fade-cluster specimen (w3/b5, 8,392 errors in one block, genie 12) got its mechanism autopsy and its candidate fix got a full A/B — negative, and the negative is the payoff (evidence: [evidence/2026-07-25-phase-b32-wn13-smoothing/](evidence/2026-07-25-phase-b32-wn13-smoothing/)). The corpse pair measured: the normal-vs-genie excess is the same FLAT per-probe solve-noise tax as WN2 (deep/edge/healthy +0.035/+0.038/+0.039 SER — not fade-edge lag), the anchors carry solve noise comparable to the true channel movement (tap change/frame 0.0495 vs genie's 0.0363), confidence is honest (|y| on errors identical), and the coded cliff sits between normal's 0.360 and genie's 0.313 on the dead block — the genie itself decodes with just 12 errors, AT the cliff edge.

The pre-registered candidate — non-causal ±1 probe-anchor smoothing, implemented as a FinishBlock pure-detection re-equalization pass with per-frame carrier snapshots (bit-identical window re-reads) — was built, instrumented (per-block LLR-stats: uncoded SER + signed LLR mass per pass), and measured: it closes ~50% of the wrong-sign-mass gap to genie but ~none of the detection gap (errBits 6,628 vs genie's 5,812; curvature loss cancels the √3 noise gain at 120 ms anchor spacing), and it costs the healthy tail 3–9% raw SER (missing intra-frame RLS), turning a 14-error disjoint smoke into 22. Implementation preserved in branch history, reverted from the library; the autopsy LLR-stats instrument landed. The remaining genie gap is clean MID-FRAME data-row estimation, unreachable from probes at U=256's density (3 per coherence time, each ~0 dB) — reliable mid-frame channel info at local SER 0.36 requires code-domain soft feedback (the turbo trains on the re-encoded first decode, 45% garbage on this block — its measured 1 revert). The specimen therefore moves to B3.3's detector program alongside WN7 (genie-identical failure) and the WN5/WN13-canonical detector-ceiling residue.

**B3.2 closes.** Delivered: WN2 Poor at mask on both seed sets (anchor ridge 8.0, PR #77), the genie attribution map (PR #76), the fade-cluster mechanism + measured-negative smoothing A/B + LLR-stats instrument (this PR). B3 scoreboard: at mask — WN1, WN2, WN3, WN4, WN5; blocked on B3.3 detector work — WN13 (benign tail at the line is detector-ceiling per genie; the specimen class ≈ once per 6M bits kills any budget it lands in), WN6/WN7 (detector-limited outright), WN8 (B3.4), WN0 (B3.5). Next: **B3.3** — the detector program: WN7 relock + soft-feedback turbo (SISO outer decode driving the chain-BCJR re-estimation), with the specimen corpse pair and the LLR-stats instrument as its acceptance rig.

### 2026-07-25 (mid-morning) — B3.3 opens: the WN7 mechanism, and the oracle-labels ceiling that splits the detector program in two

The WN7 mechanism autopsy ran first, per discipline, and retired every pre-registered hypothesis by measurement (evidence: [evidence/2026-07-25-phase-b33-wn7-oracle/](evidence/2026-07-25-phase-b33-wn7-oracle/)). The corpse pair (w0/b0, all 8 census bursts uniformly dead at SER 0.39): the B1 whole-frame smear is still the carrier — 564/704 donut frames holding 99% of errors — but it is NOT relock latency (probe-reset correlation r ≈ 0.2–0.3: frames start fresh), NOT anchor corruption, NOT chord error (SER flat across the frame, head ≈ mid ≈ tail), NOT feedback runaway (run lengths modest), and NOT estimation noise (genie identical on every profile). The load-bearing number: median |Δθ| over ALL symbols is 15–16°, flat in u — a residual complex perturbation whose phase projection sits at 8PSK's ±22.5° sector budget, converting directly to SER ≈ P(|Δθ|>22.5°) ≈ 0.39. QPSK's ±45° budget rides the same spread out — same machinery, WN13 at mask, WN7 dead.

The new instrument is the session's centrepiece: the **oracle-labels turbo** (`Ms110dDemodulator.OracleInfo`/`OracleBlockLlrs`, rig `MS110D_AUTOPSY_ORACLE=1`, llrstats pass `oracle`) — one extra TurboReequalize per block trained on the TRUE info bits, after the normal pipeline, bit-identical when unset (re-verified exactly). It measures the ceiling of any soft-feedback iteration with the current chain-BCJR channel model, and it splits B3.3 in two:

- **Labels front (soft-feedback turbo — SISO outer decode replacing the re-encoded hard decode): sufficient for the QPSK casualties.** The WN13 fade-cluster specimen under oracle: **0 coded errors on every block** (normal 8,392, genie 12) — the B3.2 "probe-information-limited" verdict now has its existence proof. WN6 under oracle: 7,572 → **75** (errBits 6.4–9.6% → 1.2–3.1%). Convergence from a garbage first decode is the engineering risk; the ceiling is not.
- **Model front (WN7): even perfect labels leave 3.7E-2** (per-block errBits 3.6–7.5%, wrong-mass ratio 0.001–0.008 — honest, and genie+oracle ≈ oracle, so noise on the estimation reads contributes nothing). The temp-edit split walks the residual: 16 h1 segments −10%, + per-segment h2 −25% cumulative — real levers, not the answer. The floor is the model class: 65% of frames run the chain BCJR ECHO-FREE (h2 killed by the 0.04 significance floor; pre-floor |h2|/|h1| ≈ 0.1–0.3), and the surviving picks cluster at delay 5 AND delay 10 — the FF's full inversion of the 4.8-symbol path leaves a truncated geometric echo train plus fractional-delay sidelobes that a single-lag model cannot hold. Mean BCJR noiseVar 0.045 at |h1| 0.847: **effective detection SNR ≈ 9.5 dB at a +19 dB operating point** — the model residual IS the 15° spread.

Pre-registered next steps, in order: **(1)** the soft-feedback turbo (SISO BCJR over the K7 outer code producing soft symbol expectations for the re-estimation rows and priors for the chain BCJR), acceptance = walking WN13-specimen/WN6 blocks from their `first` llrstats line to their `oracle` line; **(2)** the WN7 model work — channel-shortening TIR solve (FF targets 1 + c·z^{-d}, leaving one strong echo the chain BCJR converts to diversity instead of an inverted train), plus the measured time-varying-h2 and finer-h1 levers; FD-turbo remains the fallback if TIR stalls. Landed this PR: the oracle instrument (library hook + rig wiring) only — all measurement edits reverted (final tree: +37 library lines, dead when unset; bit-identity re-verified corpse-exact).

### 2026-07-25 (afternoon) — B3.3 labels front lands: SISO soft-feedback turbo, hybrid bootstrap — the WN13 specimen decodes

The pre-registered step (1) is implemented and measured. `TailBitingSisoDecoder` — a tail-biting log-MAP BCJR over the K7/K9 rate-1/2 mother lattice, same trellis and wrap conventions as the package Viterbi — turns the current block LLRs into per-coded-bit posteriors; float mirrors of puncture/interleave carry them to wire order; per-symbol products give soft expectations E[x] (scrambler-rotated) plus variances 1−|E[x]|². `TurboReequalize` split into a hard front-end (unchanged — the oracle instrument's path, re-verified bit-identical: banked first/oracle llrstats byte-for-byte, oracle per-block lines digit-for-digit) and `TurboCore(expected[], variance[]?)`: every estimation consumer keeps its exact form (E[|x|²]=1 on the PSK ring makes the /count normalizations the EM answer for soft labels too); the one new term is the EM noise correction |h|²·(1−|E[x]|²), null-skipped on the hard path.

**The pure-soft loop was a measured regression first** (banked in the evidence dir): WN6 corpse 0c/11r, 65,680 — and the per-iteration diagnostic showed why: not oscillation but a cold start. The first-pass fixed max-log scale is ~5–8× under-confident at high SNR (mean |LLR| 1.6 at +14 dB, 97 % of symbols at var > 0.25), so the loop contracts steadily (4440 → 1111 → 1460 → 180 → 12 decode-changes) yet can't reach an exact fixed point inside cap 5, and the revert discards a nearly-converged decode for the first pass. **The shipped fix is a hybrid**: iteration 0 trains on hard re-encoded labels (the proven bootstrap, which also hands iteration 1 calibrated chain-BCJR LLRs), soft iterations follow, cap 8, convergence rule and #65 revert discipline untouched. Sharp LLRs saturate E[x], labels quantize, exact fixed points return: every WN6/WN13 block ends at decode-changes=0, the WN13 dead block in 7 iterations (7656 → 3931 → 3350 → 1132 → 61 → 6 → 0).

**Corpse results** (evidence `2026-07-25-phase-b33-siso/`): WN13 fade-cluster specimen **8,392 → 16** coded errors (genie 12, oracle 0; 11c/0r) — the B3.2 probe-information-limited class is broken; WN6 **7,572 → 240** (oracle 75; 11c/0r); WN7 **unchanged** at 172,688 (1c/10r) — model-limited exactly as the oracle split predicted, riding the TIR front. Residue: WN13 16-vs-12-vs-0 and WN6 240-vs-75 leave iteration-0 bootstrap quality and first-pass LLR calibration as knobs — to be pulled only if the mask runs ask. Mask acceptance (WN13 @ +11, WN6 @ +14, canonical + disjoint, full §5.3 budgets) and the standard battery + Phase A regressions ride this PR.

Amendment (same day): the first WN6 mask run put 33k of 34.7k errors in two bursts whose dead blocks REVERTED at cap 8 — and the w2/b2 corpse showed both blocks still converging when the cap hit (one halving per iteration at …88 → 52 → 39; the other on a plateau that turned out to be a mid-loop excursion, 1543 → 2428 → 988 → 179 → 9 → 0 at cap 16, converging at iteration 15 — burst 21,566 → 633, 11c/0r). The shipped cap is 24: convergence-or-revert is unchanged, blocks converging earlier take bit-identical paths, and the asymmetry (a cap-limited revert discards a ~10k-error repair; a spare iteration costs seconds on rare dead blocks) buys the headroom. Evidence `cap/` in the same dir. WN13's disjoint-seed 26-error run (bound 1.17E-5, a <30-error small-sample artifact over a thin scatter of 2–7-bit clusters — no burst above 7 errors, specimen class gone) re-runs at 6M bits in the final battery.

Final battery (cap-24 binary, `2026-07-25-phase-b33-siso/battery.log`): **WN13 Poor canonical AT MASK** — 6 errors / 3.24M, bound 4.03E-6; the disjoint family run to 6.5M bits measures 1.14E-5 (74 errors as 2–16-bit clusters across 14 bursts, all 11c/0r — the converged-residual class sits at the mask boundary; combined families ≈ 8.2E-6; open item for the chain-BCJR-priors lever and the model front). WN6 lands at 7.22E-4 / 5.43E-4 with **zero reverts** — the dead-mass class is gone and the rest is the model tail the oracle already priced (2.8E-4-scale); WN6 formally joins WN7 on the model front. WN7 full-budget improves 4.59E-1 → 3.89E-1 (16c/72r — the longer loop converges 16 blocks even there). Regressions: WN2 18/10 (canonical identical to #79's battery; disjoint −2), WN1 2/0, smokes 6/6, AWGN ×10 zero, Static zero, Doppler zero. Unit suite 689/0. Scoreboard: **WN13 joins the at-mask set on the canonical seed with the disjoint family at the boundary**; WN6 moves from "blocked on detector" to the model front with a 30× BER cut; WN5/WN13-canonical residue and WN8/WN0 unchanged.

### 2026-07-25 (midday) — B3.3 model front lands: TIR channel shortening + chain-BCJR priors + per-segment h2 — WN13 clears the boundary, WN6 reaches it, WN7's oracle ceiling collapses 64×

The pre-registered step (2), taken as three separately-measured levers with the design registered before any code ([evidence/2026-07-25-phase-b33-tir/](evidence/2026-07-25-phase-b33-tir/)). **TIR channel shortening**: the turbo FF batch-solve stops inverting the second path (an IIR ask that a 12-symbol FIR truncates into the measured d5+d10 echo train, paying notch noise-enhancement — effective detection SNR was 9.5 dB at a +19 dB operating point) and instead solves the unit-tap-constrained shortening target 1 + c·z^{-d}, jointly linear in (ff, c) via the DFE's feedback-column Gram, per-lag subset Choleskys against one accumulation, and an acceptance margin of 4·ln(L)·SSE₀/rows (4× the noise-only best-of-L free-parameter reduction — the same construction as the 0.04·|h1|² floor) so echo-free frames keep the full-inversion solve exactly; on acceptance the chain-BCJR echo is pinned to the designed lag with the floor bypassed. TIR engages on 30–56 of 64 WN6 frames at mean lag 5.0 (the physical 2 ms path), |c| 0.3–0.6 — the 65%-of-frames-echo-free starvation is gone by construction. **Chain-BCJR priors**: the loop was code-blind on the detector side; SISO EXTRINSICS (posterior − channel input; posterior would double-count and lock the loop) now enter each symbol's branch metric once as per-symbol log-priors, and the detector hands back extrinsics only. **Per-segment h2** on the pinned lag (under TIR the echo is a real fading path — §B2.1 applied to the second tap). A cap-96 sweep confirmed cap 24 exactly (identical outcome; the unconverged blocks wander at ~300–6k decode-changes without fixed points — model-shaped residue, not iteration starvation).

Corpse chain (each step committed only after its measurement): WN6 w0/b0 7,572 → 15 (TIR) → **0** (priors), oracle 75 → **0**; WN13 specimen 16 → **0** at every step; WN7 w0/b0 oracle **15,136 → 326 (TIR) → 237 (seg-h2)** — the model ceiling collapsed 64×, and WN7 is now labels-limited (shipped 172,691 → 107,938, 5c/6r, the six reverted blocks having the worst first-pass starts at errBits 17–22%).

Full battery (all legs green, `battery.log`): **WN13 Poor: 0 errors / 3.24M canonical (bound 1.13E-6) and 2 errors / 6.49M disjoint (3.08E-7)** — the #80 converged-residual class (74 errors, 1.14E-5) is dead, killed by the priors; WN13 is at mask on both families with 9× margin. **WN6 Poor reaches the mask boundary**: disjoint family formally passes at full budget twice (3M: 21 errs, bound 9.90E-6; 6M: 51 errs, direct 7.86E-6) while the canonical family misses at 6M by 22% (79 errs, 1.22E-5; combined families 1.00E-5) — a ~70× cut from #80's 5.4–7.2E-4, zero reverts anywhere, residue = WN6's own thin converged-residual class (2–31-error clusters in converged blocks; the WN13 kill proves the class is closable). **WN7 improves 3.89E-1 → 2.49E-1 / 2.50E-1** (43c/45r from 16c/72r). Regressions: WN2 15/15 (bound 8.13E-6 both — canonical −3 vs #80), WN1 0/0 (−2), smokes WN5/WN4/WN3 clean, AWGN 10/10 zero at full budget, static WID2 zero, Doppler 3/3 zero. Unit suite 712/0 (incl. 4 new TIR solve tests; 9 new Dfe tests total).

Scoreboard: AT MASK — WN1, WN2, WN3, WN4, WN5, **WN13 (both families, 9× margin)**; AT THE BOUNDARY — **WN6** (disjoint passes full-budget, canonical 1.22E-5); OPEN — WN7 (2.5E-1 shipped vs 5.8E-4 oracle floor: the labels basin binds, then the fractional-echo model floor — pre-registered next levers: two-adjacent-lag chains (§B2.2, M² states) for the d/d+1 sidelobes, bootstrap quality for the basin; FD-turbo remains the escalation), WN8 → B3.4, WN0 → B3.5.

### 2026-07-25 (evening) — B3.3 basin campaign closes as a decisive negative result; two-adjacent-lag echo lands with a battery-caught label-trust gate

Two legs in one session, both fully pre-registered ([evidence/2026-07-25-phase-b33-basin/](evidence/2026-07-25-phase-b33-basin/), [evidence/2026-07-25-phase-b33-twolag/](evidence/2026-07-25-phase-b33-twolag/)).

**The basin campaign** measured all three pre-registered levers against the WN7 labels basin and none ships — and that is the finding. Lever 1 (per-frame first-pass LLR calibration from the pre-solve probe residual — itself corrected mid-lever when the post-solve estimator's in-sample optimism was measured against per-symbol truth: span residual 0.438 vs pre-solve 0.460, r = 0.58) improved first decodes on 9/11 blocks (−1.7% total, the healthiest block −33%) and made right-bit confidence block-resolving (2.6–5.7 vs the fixed scale's flat 1.95) — but flipped the marginal b9 out of convergence by pure trajectory chaos (its own start IMPROVED). Lever 2A (soft iteration 0) lost even the recoverable deep starts — beyond the code's threshold the SISO posterior is LESS calibrated than the channel LLRs it consumed. Lever 2B (agreement-weighted hard rows, w = Π σ(±llr)) measurably shifted the iteration-0 solves and changed ZERO block outcomes. Lever 3 (damped priors, γ = 0.75, the single literature value — no sweeps) shifted every trajectory and converted nothing. Verdict, pinned by the new first→final→oracle llrstats walk: **the converge-or-wander split is decided by the soft loop's attractor structure** — the wander states hold first-pass-grade error counts at 3–12× the first pass's wrong-bit confidence, and neither start quality nor prior damping reaches them. The deep blocks start at 35–49%-wrong first decodes (the basin boundary is razor-thin: b3's 46% recovered, b9's 48% wandered); their oracles (6–29 per block) prove the model supports near-perfect decodes there. Instruments landed: llrstats `final` pass (TurboBlockLlrs), rig-side per-block FIRST-DECODE errors, oracle wrong-bit positions. The lever-1+3 implementation is banked as a patch for resurrection.

**The model front** (two-adjacent-lag echo, correcting §B2.2's sketch: exact two-lag chains don't exist at M² — the pair ladder is cyclic with treewidth ~d+1; the EM-consistent form is exact single-lag chains + adjacent-tap soft cancellation): `SolveTrainingTir` gains straddle-pair candidates {d−1,d}/{d,d+1} tied to the established lag (margin 4·SSE₀/rows, no ln L — no selection effect), and the demod cancels the second tap at the segment-interpolated coefficient with the EM variance bump. WN7 corpse: **oracle 238 → 209** (b1 15→6, b5 83→60, b7 29→19; b4 stubborn at 99; b10 6→19 the honest cancellation cost), shipped unchanged, pair accepted on ~half the TIR frames at mean |c2| 0.12–0.18. The banked #81 16-segment lever measured as a clear regression under TIR (oracle 209→495) — retired as inversion-regime-specific. The remaining WN7 oracle floor is localized (new instrument): **partially-faded frames** (gain 0.12–0.45, preMse 0.6–1.3, ~17% raw under oracle labels, uniform in-frame) — the per-frame solve's stationarity assumption through fade crossings is the leading suspect; FD-turbo remains the pre-registered escalation.

**The battery caught what corpses could not**: the first run put WN6 canonical at 1.78E-3 — 11,447 of 11,526 errors in ONE marginal deep-start block (w3/b3) flipping out of convergence, while the SAME build halved the WN6 disjoint residue and kept WN13 perfect. Mechanism: iteration-0 cancellation with re-encoded labels up to ~49% wrong injects unpriced error into the observation itself (the variance bump exists only on soft iterations) — the basin campaign's "worse start loses recoverable blocks", reproduced at mask scale. Fix, no knobs: **pair candidates require trusted labels** (oracle truth; variance-priced soft iterations); hard iteration 0 runs the #81 single-lag path bit-exactly. w3/b3 recovered to 0 errors / 11c/0r; WN7 shipped and oracle exactly held.

Full battery, gated build, all green (`battery.log`): **WN13 0/3.24M canonical + 2/6.49M disjoint** (at mask, both families, 9× margin); **WN6 canonical 6M 79 errs = 1.22E-5** (the exact #81 boundary state, 264c/0r) **and disjoint 6M 44 errs = 6.78E-6 — a formal full-budget pass, better than #81's 51**; WN7 2.44E-1/2.55E-1 (44c/44r / 42c/46r — marginal-block reshuffle, net wash); WN2 15/15 (8.13E-6 both); WN1 0/0; smokes WN5 6 (the known #80-era residue)/WN4 0/WN3 0; AWGN 10/10 zero at full §5.3 budget; static WID2 zero; Doppler 3/3 zero (one environmental CLR crash on the first attempt, clean on retry). Unit suite 695/0 (incl. 2 new pair-TIR tests).

Scoreboard: AT MASK — WN1, WN2, WN3, WN4, WN5, WN13 (both families, 9× margin); AT THE BOUNDARY — WN6 (disjoint now formally passes at 6M full budget with margin, canonical 1.22E-5 exactly as #81 — the thin converged-residual class is the whole residue, all clusters in converged bursts); OPEN — WN7 (shipped ~2.5E-1, attractor-bound; oracle floor 209 ≈ 5.2E-4, fade-frame class; next levers: the fade-crossing solve or FD-turbo), WN8 → B3.4, WN0 → B3.5.

### 2026-07-25 (night) — B3.3 the weak-cursor floor falls: floating-gain (eigen) TIR refit — WN7 oracle 209 → 15, WN6 AT MASK both families

One pre-registered leg ([evidence/2026-07-25-phase-b33-fadecross/](evidence/2026-07-25-phase-b33-fadecross/)), and the corpse killed the leading suspect before any lever was coded. Joining the WN7 w0/b0 corpse against the recorded Watterson truth (aligned at +75 symbols by error-density anti-correlation — the frame-gain EWMA is lag-contaminated and useless for alignment): 74% of the raw oracle error mass sits in frames with HEALTHY total power (the energy is in the echo path — the discriminator is min|p0| < 0.45, the cursor path alone), within-frame channel motion is acquitted (corr(noise floor, motion) ≤ 0.19 at fixed level), and both representation (4-anchor piecewise-linear fits the true trajectory to ≤1E-4) and anchor estimation (deviation a fraction of the frame's own noise floor) are acquitted. The pre-registered "stationarity through fade crossings" mechanism — split solves, fade-aware weighting, probe-anchored trajectories — targets motion, and motion is not the driver. The real mechanism, pinned by a new `turbo-frame` instrument (per-frame anchors, BCJR noise floor, FF tap energy on the oracle pass): **the monic TIR target**. Pinning unit cursor gain forces the FF to invert a faded cursor path — by cursor quartile, FF energy 10.1 → 24.5, noise floor 0.0089 → 0.0471 (5.3×), oracle errors 0.5 → 48.7/frame — the classic monic-shortening noise boost (Melsa/Al-Dhahir).

The lever: **floating-gain (unit-norm target) refit on the accepted lag set**. Selection and acceptance stay monic and bit-identical to #82 (same candidates, SSEs, margins, label-trust pair gate — echo-free and rejected frames never change); accepted lags re-solve min ‖ff·window − g·x − c·x[−d]‖² s.t. ‖(g,c)‖ = 1 — the smallest eigenvector of the 2×2/3×3 Schur complement over Gram blocks the accumulator already carries (shared FF Cholesky, inverse iteration on a local small factor, deterministic phase gauge). Amendment 1, caught by the unit suite before any corpse run: the eigen form must NOT select — the target columns' post-projection residuals are correlated through the shared window, handing λmin a FIRST-ORDER gauge gain on echo-free noise that the monic margins aren't calibrated for (the echo-free rejection test accepted a fake lag-1 echo; the refit-only form restores it). At weak cursor the unit-norm optimum shifts target weight onto the strong echo column instead of inverting the fade; the chain BCJR (exact for h2 ≥ h1) collects both paths in the trellis.

Corpse: **WN7 oracle 209 → 15 (−93%)** — b4 99→0, b5 60→15, b7/b10/b1/b6 → 0; **shipped 107,938 → 72,666 with 5c/6r → 7c/4r** — two wander blocks converged, the predicted basin dividend (sharper per-iteration LLRs strengthening the contraction), first decodes bit-identical. Instrument signature exactly as pre-registered: weak-half FF energy 22.9 → 13.1, weak-quartile noise floor 0.0471 → 0.0254, n/ffE flat across quartiles (the boosted-unmodeled-residual gradient is gone). Guards WN6/WN13sp bit-identical 0/0. DfeTests 12/12 (incl. the new weak-cursor refit test: ratio 3.33 at matched-filter FF energy); suite 696/0.

Full battery, all green (`battery.log`): **WN6 6M canonical 57 errs = 8.79E-6 — a DIRECT formal pass** (stuck at 79/1.22E-5 since #81; the thin converged-residual class was weak-cursor physics — the census shows the old clusters gone) **and disjoint 57 = 8.79E-6** (coincidence of totals, disjoint bursts entirely different) — **WN6 moves from AT THE BOUNDARY to AT MASK, both families**. WN13 0/3.24M + 0/6.49M with 352c/0r (the #82 disjoint 2-error residue also gone). WN7 shipped **2.44E-1 → 1.73E-1 canonical / 2.55E-1 → 1.90E-1 disjoint** (57c/31r / 54c/34r from 44c/44r / 42c/46r) — the first shipped movement since the basin closed. WN2 15/12 (bounds 8.13E-6/6.89E-6), WN1 0/0 (1984c/0r each), smokes WN5 6/WN4 0/WN3 0, AWGN zero (WN0 −6 dB, WN1 −3 dB, K=48 legs; the first attempt was SIGTERMed mid-leg by a concurrent agent's process kill on the shared box — clean on the detached retry), static WID2 zero, Doppler 3/3 zero.

Scoreboard: AT MASK — WN1, WN2, WN3, WN4, WN5, **WN6 (both families)**, WN13 (both families, 9× margin); OPEN — WN7 (shipped 1.73E-1/1.90E-1 attractor-bound; oracle floor now 15 ≈ 3.7E-5 on the corpse — b5's last 15 the only survivors; the labels basin is the whole remaining story), WN8 → B3.4, WN0 → B3.5. Backlog: WN2 clean-channel first-pass triplet (positions 24/40/46, the three least-confident decisions — e2e-rig observation, margin item).

### 2026-07-25 (late night) — B3.3 two negative results, both banked with mechanism: start-side calibration is dead under this loop structure; per-segment noise pricing needs a better estimator

Two fully pre-registered legs on one branch ([evidence/2026-07-25-phase-b33-basin2/](evidence/2026-07-25-phase-b33-basin2/), [evidence/2026-07-25-phase-b33-segnoise/](evidence/2026-07-25-phase-b33-segnoise/)); no shipped-path code lands (code committed, measured, and reverted in-branch — the tree is bit-identical to main).

**The basin re-measurement**: the eigen-TIR refit changed the contraction calculus (b9 — the old razor-edge chaotic flip — now converges at i10; the four remaining wanderers b4/b5/b7/b10 sit at 1309–4755 decode-changes), which was the banked lever-1 patch's resurrection contingency. Measured: first decodes improve broadly (b0 −33%), oracle holds, b7's amplitude halves — and **b1 and b6, both late convergers (i14), flip OUT while no wanderer converts** (shipped 72,666 → 107,643). Second independent measurement, different victims, same mechanism: calibrated first-pass LLRs reliably improve starts and reliably perturb marginal soft trajectories net-negative. **Start-side calibration is dead under this loop structure** — the bank note tightens from "if the calculus changes" to "only if the loop changes shape".

**Per-segment BCJR noise pricing** (the fadecross note's first secondary structure — turbo-side per-iteration sharpening that cannot touch first-pass trajectories): a four-amendment arc, each step pre-registered before its measurement. Corpse: shipped identical, **oracle 15 → 3** — the conversion bar failed but the model floor fell 5×. Battery-scale decision (Amendment 2): WN7 exactly neutral (paired seeds), **WN6 6M canonical 57 → 35 / disjoint 57 → 39 (−32%)** — a real margin lever on a gated waveform, mechanism-consistent (WN6's residue lives in converged blocks = the near-perfect-label regime the pricing sharpens). Then the battery caught the cost (Amendment 3): **WN2 canonical 15 → 32 (1.05E-5, above the line)** — five new thin clusters in converged blocks on paired seeds: on flat-floor frames the 4× ~64-symbol segment estimates are pure χ² jitter. The derived 2× heteroscedasticity gate (noise-only spread ~1.3–1.4× at 99th pct) fixed the canonical family (17 errs, one 2-error cluster adjudicated by corpse as genuine pricing — 37% of that burst's frames gate-engaged) and retained WN6's gains (41/39) — but **WN2 disjoint still failed outright (12 → 46, 1.51E-5, four new clusters to 13 errors)**: WN2 is the MARGINAL waveform, and LLR sensitivity to floor-estimate error scales with marginality, defeating any ratio gate. Pre-registered consequence executed: **REVERTED** (cb5c032 + 3fc5153 reverted in-branch, baseline corpse reproduced exactly). Banked: both patches + the estimator lesson — the ~30% WN6 gain and 5× oracle cut are real and reproducible; a shippable form needs a floor estimator whose error stays out of marginal waveforms' decision boundaries (cross-frame smoothing at fade bandwidth ~8× variance cut, or floor-from-|h1|-trajectory), not a bigger gate threshold.

Operational: the shared-box process-kill hazard recurred in reverse (a mis-templated battery script briefly wrote into the #83 evidence tmp dir — caught after one leg, #83 outputs preserved, WN2 reference censuses banked into the evidence dir as `ref83-*`); rc=143 on this box = check the other agent first (memory updated).

Scoreboard unchanged from #83: AT MASK — WN1, WN2, WN3, WN4, WN5, WN6 (both families), WN13 (both families); OPEN — WN7 (1.73E-1/1.90E-1 shipped, attractor-bound, oracle floor 15 ≈ 3.7E-5 — every start-side and pricing-side lever is now measured; the loop structure itself (FD-turbo, damping schedules) is what remains), WN8 → B3.4, WN0 → B3.5. Backlog: WN2 clean-channel first-pass triplet (e2e-rig observation, task #38); the noise-floor estimator lever (pre-registration required).

### 2026-07-25 (small hours) — WN2 clean-channel triplet answered: the K=48 anchored-ridge warm-up, by design; instrument ships, demod untouched

The e2e-rig observation (3 wrong first-pass hard decisions in 768 on a NOISELESS channel, positions 24/40/46, the three least-confident in the block; WN0/6/13 exactly 0; preamble length irrelevant) is closed with a pinned mechanism and a no-fix verdict ([evidence/2026-07-25-wn2-cleantriplet/](evidence/2026-07-25-wn2-cleantriplet/)). New rig instrument: `MS110D_AUTOPSY_CLEAN=1` (no fading, no noise — lead-in/out padding only). Reproduction: uncoded 5/98304, coded 0, all five in block 0 frames 0–1, wrong-|LLR| mean 0.1 vs right-mass 3.56 — near-erasures. The constellation view is the real finding: on a clean channel WN2's equalized amplitude opens at 0.23 and ramps to 1.0 over ~15–20 frames with ISI scatter to match, while WN3/WN6 open at 0.998; WN1 (the other U/K = 48/48 waveform) shows the same ramp and 3 errors of its own. Mechanism: the K=48 anchored-coasting policy (initRidge 1.0 / trackRidge 8.0 vs 1e-3/0.15 elsewhere) deliberately over-regularizes the FF solve — the ridge that bought WN2 its mask (the documented 0.5…16 → 43/42/20/5/1/23 sweep) shrinks the taps at burst start until the anchored batch solve accumulates. Confirmed with the existing knob: `MS110D_AUTOPSY_TRACK_RIDGE=0.15` collapses the ramp to ~4 frames and uncoded to 0. No fix: the clean-channel cost is ~5 near-erasure bits per burst with coded cost zero, and softening the ridge is a first-pass trajectory perturbation on the marginal waveform — the lever class measured net-negative twice at battery scale. Rig + docs only; the demodulator is untouched, so no battery is owed.

### 2026-07-26 (small hours) — B3.4: the QAM16 turbo wiring lands as an oracle instrument; the bootstrap basin is measured uncrossable

One pre-registered design plus a four-amendment arc ([evidence/2026-07-25-phase-b34-wn8/](evidence/2026-07-25-phase-b34-wn8/)), each step's bar and consequence stated before its measurement. The map first (the rig gained wire-domain QAM16 truth — nibble XOR scramble, per-frame register reset): WN8 Poor @23 dB on current main is 4.96E-1 / 26.4% uncoded, byte-for-byte the B2-era state, SER 0.67 FLAT in u with |g| ≈ 0.75 flat, genie ≈ normal — detector-limited outright, the pre-B3.3 WN6/WN7 verdict reproduced at 16QAM (mechanism: the same residual channel-model error the B3.3 arc measured, priced against half the min distance — 6.4 dB harsher — which 4 dB of extra mask SNR does not pay for; the first pass is additionally rank-starved: K=32 probe rows against 36 taps with the QAM16 DD gate passing nothing at MSE 0.6).

The design (wire-domain turbo inclusion — the XOR scramble is a label permutation with no geometric descrambled form, so the chains run wire samples with identity labels, priors permuted per symbol, output LLR signs flipped per scramble bit; true E[|x|²] second moments replace the PSK-ring /count identities) is implemented in full and proved by the oracle: **WN8's converged-soft-feedback ceiling is 9.3E-4** (485–505 coded on the w0/b0 corpse, one block fully clean). The shipped loop is another story, closed by measurement: hard-label iteration 0 stalls (labels bias ĥ multiplicatively toward zero at coin-flip accuracy — 0c/11r, zero movement); probe rows joining the re-solve change nothing alone; the label-free probe-anchored bootstrap (Amendment 2 — solve, h1 anchors, and noise floor all from the bounding probes, chains as exact-MAP detector) transforms the dynamics from stall to monotone descent but the descent asymptotes; and at cap 96 the floor is exposed by a new rig instrument (the final stream's info decode): **50.1% — still coin-flip**. The falling churn was a self-consistent wrong attractor — the SISO and chains agreeing with each other, wrongly — and the §B3.3 revert principle survives its harshest test yet: the revert was protecting the output from a confident echo chamber.

Shipped: the rig instruments (QAM16 truth, final-decode), the complete turbo wiring behind the RESTORED FinishBlock gate (dead in the shipped path; exercised by MS110D_AUTOPSY_ORACLE=1), the measured-rationale comment at the gate, and the banked `qam16-turbo-full.patch`. Shipped behavior bit-identical everywhere — WN7 corpse 72,666 / 7c/4r / oracle 15 exact, WN6 / WN13sp guards 0 / 11c/0r, WN8 269,237 with turbo back at 0c/0r/0a/11s, suite 696/0; full battery gates the merge.

Scoreboard: AT MASK — WN1, WN2, WN3, WN4, WN5, WN6 (both families), WN13 (both families); OPEN — WN7 (1.73E-1/1.90E-1, loop-structure levers remain: FD-turbo, damping schedules), **WN8 (doubly blocked, both fronts now instrumented: the bootstrap basin — coin-flip first decode with no label-free crossing — AND the model ceiling, 9.3E-4 ≈ 93× mask with TRUE labels; a model-front leg — pilots-in-data, FD processing, or a fade-trajectory prior — must move the ceiling before turbo inclusion is worth reopening)**, WN0 → B3.5.

### 2026-07-26 — B3.5: the Walsh RAKE lands and the last single-read acquisition field falls; WN0 Poor 7.96E-2 → 5.99E-3

One registered design, one amendment, both classes closed by corpse-level mechanism proof ([evidence/2026-07-26-phase-b35-wn0/](evidence/2026-07-26-phase-b35-wn0/)). The fresh map first: the B1 inversion reproduces exactly on today's HEAD — static 2-path FREE (8.83E-4 uncoded, zero coded), flat Rayleigh 1.20E-2 coded, Poor 7.96E-2 from uncoded only 1.4× higher — the coherent single-reference correlator turns the echo's half of the received energy into self-noise and chases the two-phasor resultant with a PLL a coherence-time behind, spreading errors everywhere (20% fade concentration) so the code gets no erasure structure. Failure uniform: 476/476 bursts.

The registered lever (DD-MRC 7-finger "Walsh RAKE", confined to the WN0 path): per-finger correlations at chip delays 0..6 off one scramble draw, decision-directed per-finger complex gains (α=1/6, persisting across blocks), MRC statistic with quadratic-CSI max-log LLRs, noncoherent 8-symbol warm-up, PLL driven by the combined winner statistic, all-fingers-weak discriminator. **Every acceptance bar passed on the first form, no rungs**: Poor 1.03E-2 (< the 1.20E-2 flat bar — the autopsy's combining proof), flat itself 26× better (4.56E-4), static uncoded 10× better (the echo finger converts the second path to signal), AWGN 0/3M at uncoded 1.59× (the idle-finger price, inside the registered 2×). Census reshaped from uniform failure to 326/476 zero-error bursts.

The census then exposed what uniform failure had hidden (Amendment 1, registered before the fix): four bursts at coin-flip from bit 0 = 43% of all remaining errors, the same four with the same counts on the OLD detector. Corpse-confirmed mechanism: the preamble COUNT is the last single-read acquisition field (4 dibits, 3 check bits — 1-in-8 corrupted reads beat the check), and a burst-start fade or mis-walkback that corrupts it places data start whole super-frames off behind a clean-looking lock: the WID's Class-D failure, again (the WID got its vote in §B3; the count never did). The dead four read 31/25/3/16 against truth 19 — and EncodeCount(31) is `3333`, so all-same-symbol mush decodes to a VALID codeword. Fix: decrement-aligned joint count vote over the same super-frame span the WID vote already waits for (candidate c at frame v expects EncodeCount(c−v)), margin floor 0.10 with the BackToSearch safety valve. Corpse: all four joint-decode the true count — two to ZERO coded errors — healthy burst byte-identical, all four corpse guards bit-identical; census re-run **5.99E-3 with zero coin-flip bursts** (registered prediction ≈5.9E-3), AWGN/flat/static byte-identical. Suite 696/0.

The residual 5.99E-3 is quantified and parked with a direction, not chased: the fade-cluster block cliff (Viterbi collapses at block-uncoded ≈17–20%; collapsing blocks have right:wrong LLR mass 13:1 — the CSI weighting works, the blocks simply hold 3–8× less signal mass). Next leg opens with a WN0 genie-gain oracle (true per-finger gains from the recorded Watterson taps) to bound every reference-quality lever BEFORE any is tried — the B3.4 lesson applied forward. WN0 stays measured-not-gated for B4.

Scoreboard: AT MASK — WN1, WN2, WN3, WN4, WN5, WN6 (both families), WN13 (both families); OPEN — WN7 (1.73E-1/1.90E-1, loop-structure levers remain), WN8 (doubly blocked, both fronts instrumented), **WN0 (5.99E-3, was 7.96E-2 — detector combining proven, acquisition class dead, residual = block-cliff energy ceiling with the genie-gain oracle as the next leg's opening instrument)**.

### 2026-07-26 — B4: the at-mask set becomes a hard gate; WN5 re-founded at full budget; WN6 honestly parked AT THE LINE

The gate-flip leg ([evidence/2026-07-26-phase-b4-gate/](evidence/2026-07-26-phase-b4-gate/)), registration committed before any run. The registration's pre-run arithmetic reshaped the flip itself: WN6's banked 57/57-per-6M rate (pooled 9.5E-6, CI [7.8, 11.4]E-6) gives a ~32% per-family §5.3 false-red at any feasible budget — default-arming it would institutionalize a coin-flip nightly — so the flip criterion is rate-driven, not draws-driven: both families §5.3-green AND the pooled both-family 97.5% bound under mask AND ≤5% per-family false-red at the default budget. WN2/WN5/WN6 were pre-committed to 6M/family (3M is not §5.3-decidable at their rates: WN6 needs the ≥30-error direct path, WN2 trips the <30-error Poisson bound from k=20).

The battery is the b4-parallel form Tom asked for: three concurrent detached lanes (Poor canonical / Poor disjoint / AWGN-per-WN + static + Doppler), 33 legs in ~33 min wall, gated legs asserting §5.3 in-process via `MS110D_POOR_GATED=1`. **All 33 green, zero acquisition failures, zero retries.** Full table: WN1 0/0, WN2 30 (4.93E-6 direct — landing exactly on k=30)/29 (bound 6.84E-6), WN3 0/0, WN4 0/3, WN5 23 (bound 5.32E-6)/**0**, WN6 57/57 (8.79E-6 direct, both), WN13 0/0 (3.24M+6.49M); measured trio bit-identical to #87 (WN0 5.99E-3/6.40E-3, WN7 1.73E-1/1.90E-1, WN8 4.96E-1/4.97E-1); AWGN 10/10 zero at full §5.3, static zero, Doppler 3/3 zero — the replication rows are themselves evidence the branch changes nothing but gating.

**The WN5 verdict** — the one genuinely open question — resolved decisively green: its at-mask status had rested on the B3-entry re-baseline with only 480k smokes since (persistently 6 errors = 1.25E-5 on the smoke denominator), and the fresh 12.97M shows 23 canonical errors in two tail clusters (46/48 bursts clean) with the disjoint family flat zero — the smoke residue was a cluster, not a rate; pooled 1.77E-6.

**Flip set = {WN1, WN2, WN3, WN4, WN5, WN13}**, hard-gated by default in `Poor_Channel_Mask_Gate` with 6M in-test default budgets for WN2/WN5/WN6; `MS110D_POOR_GATED=1` remains as the force-all chasing-leg tool. **WN6 does not flip — its pre-registered consequence clause executed**: fresh pooled 114/12.97M (bound 1.14E-5; the fresh counts are deterministic replications of the banked seeds, noted honestly in the evidence) — the point needs *margin*, not re-measurement, and its path to the gate is the banked B3.3-segnoise lever (−32% on WN6 both families) behind a shippable noise-floor estimator. Post-flip verification: WN2/WN5/WN13 default-budget re-runs bit-identical to their gate legs with the assert executing by default, plus an armed-negative check (WN2 at a 100k budget red by default on the Poisson clause — positive proof of arming), plus the plain suite green.

Scoreboard: **GATED (hard, default) — WN1, WN2, WN3, WN4, WN5, WN13**; AT THE LINE (measured) — WN6 (§5.3 draws pass at 8.79E-6 both families; pooled bound 1.1E-5; margin lever banked); OPEN (measured) — WN7 (1.73E-1/1.90E-1, loop-structure levers), WN8 (doubly blocked, both fronts instrumented), WN0 (5.99E-3/6.40E-3, block-cliff residual, genie-gain oracle next). Phase B closeout artifacts (phase-b-closeout.md, design §6 row B, issue closures, RLS report) stay open until the WN6/WN7/WN8/WN0 stories resolve.

### 2026-07-26 — B4.1: the floor estimator lands on χ² statistics, not smoothing — WN6 35/39 both families, GATED; the B3.3 lever class ships at its third form with zero collateral

The WN6 margin leg ([evidence/2026-07-26-phase-b41-wn6floor/](evidence/2026-07-26-phase-b41-wn6floor/)), registration-first with two amendments, each before its measurement. Phase 1 instrumented the truth (oracle-pass per-position residual field + per-segment `nseg=` floors, behavior-neutral, guards exact) and characterized the floor against a split-half reference: **the residual-power process measures 0.8–2.2 Hz everywhere** (the 4 Hz reference bound validated); **WN2 has zero fade frames** — at 40 ms frames its true within-frame range never exceeds 2×, so B3.3's WN2 damage was estimator noise priced onto a flat truth, unfixable by any gate; **the |h1|²-trajectory candidate is dead** (the floor does not follow it); **the floor's anatomy is a ≤4 Hz slow background plus rare fast frame-local events** — cross-frame smoothing captures the background but loses the events that carry WN6's gain (and a frame-periodic "anchor-geometry" hypothesis was tested and killed the same afternoon).

That anatomy forced the third estimator family, registered as Amendment 2's ladder: **SPIKE-UP** — frame-constant base, a segment's windowed floor engages only beyond the segment's own 3σ χ² band (thr = exp(3/√count): 1.45× at WN6/WN7's 128 dof, 2.4× at WN2's 24 dof — derived, never tuned), upward only, so pricing can de-confidence a locally-bad span but can never inject the over-confident low floor that was the §B3.3 damage mechanism. WN2 is protected *by construction*: its flat truth cannot cross its own band.

It passed everything on the first rung. Corpse: the two pricing-reachable WN6 residue bursts 14 → 0 and 16 → 8; the two model-ceiling bursts (oracle 8/31) byte-identical; all four WN2 corpses byte-identical; WN7 and all guards exact. Battery (B4 three-lane form, 33 legs green): **WN6 6M 57 → 35 canonical / 57 → 39 disjoint** — the full banked raw-pricing gain to the digit, and **WN2 exactly 30/29 with every census file bit-identical to B4's**; everything else held (WN7 +0.002 %/+2.9 %, inside the neutral bar; AWGN/static/Doppler zero — flat floors cannot engage a 3σ band). Ship form: pricing default-on (`TurboNsegMode` null = "spikeup", "off" restores the frame constant); ship-form guards exact with no environment; suite 697/0.

**WN6 enters the default-gated set** under the B4 flip criterion, same PR: pooled 74/12.97M → bound 7.15E-6, per-family false-red ≈ 0.02 % (from ~32 % at 57/57 — the coin-flip nightly the B4 registration refused to institutionalize is solved, not waived).

Scoreboard: **GATED (hard, default) — WN1, WN2, WN3, WN4, WN5, WN6, WN13**; OPEN (measured) — WN7 (1.73E-1/1.95E-1, loop-structure levers; FD-turbo standing escalation), WN8 (doubly blocked, both fronts instrumented), WN0 (5.99E-3/6.40E-3, block-cliff residual, genie-gain oracle next). Phase B closeout artifacts stay open until the WN0/WN7/WN8 stories resolve.

### 2026-07-26 — B3.5b: the genie-gain oracle finds the echo-lock lottery; the symmetric RAKE window ends it — WN0 0/3 errors both families, GATED

The WN0 instrument leg ([evidence/2026-07-26-phase-b35b-wn0genie/](evidence/2026-07-26-phase-b35b-wn0genie/)), registered as "price the reference-quality ceiling before any lever" and resolved by the ceiling never being the story. The oracle (truth finger gains from the §B0 genie stream correlated against the true Walsh row, O-inst/O-pole modes, PLL-freewheel and self-noise mechanics registered up front) came back R = 1.000 — perfect gains decode every corpus block clean — but the registered M3 fidelity pairing came back RED, and its consequence clause (stop; audit the instrument before reading findings) did the real work: the true mapping showed finger 0 tracking the WRONG PATH on every error corpse. **Acquisition locks onto the 2 ms echo on ~31% of bursts, the direct path lands at −4.8 chips — outside the §B3.5 causal finger window — and the burst runs 84 s as single-path Rayleigh with no diversity. 18/18 classified bursts concord: every zero-error burst two-path, every error burst echo-locked, down to the 4-error specimens.** The census tail was never deep fading, and the "block-cliff energy ceiling" suspicion dissolves: the energy was 4.8 chips left of the window.

Amendment 1 registered the mechanism-directed lever over the anticipated reference family: **rung G — the RAKE finger window goes symmetric, chip delays 0..+6 → −6..+6** (13 fingers; DD rule, α, warm-up, CSI-weighted MRC, LLRs, PLL all untouched; WN0-only code; forward sample requirement unchanged). Corpse: all 9 echo-locked corpses (2,445 pooled errors, including the 697-error monster) → **0 coded errors with the shipped DD detector**, two-peak profiles restored at −5/−4, both paths DD-tracked at ρ 0.91–0.94; healthy corpses exactly 0 throughout. Battery (house three-lane form, 32 legs, zero retries): **WN0 Poor 5.99E-3 → 0 errors canonical (bound 1.22E-6), 6.40E-3 → 3 errors disjoint (bound 2.92E-6)** at full §5.3 budgets, 0 acquisition failures, end-reason profile improved (9 non-Eom → 0); **all 72 non-WN0 census files byte-identical to the #88/#89 baseline** — the gated seven at their exact digits, the open pair unchanged. Guards byte-identical through both commits; suite 697/0.

**WN0 enters the default-gated set** under the B4 flip criterion (bounds 1.22E-6/2.92E-6, pooled 1.46E-6, false-red ≈ 1E-15), armed-negative verified at a smoke budget. The reference-quality lever class closes unpulled — priced possible by the oracle, obsoleted by the geometry fix. From Phase B entry's worst headline number (7.96E-2, "detector wastes the channel twice") through §B3.5's RAKE (13.3×) to §B3.5b's window (at mask): the 75 bps mode the whole waveform ladder falls back to is now certified on the channel it exists for.

Scoreboard: **GATED (hard, default) — WN0, WN1, WN2, WN3, WN4, WN5, WN6, WN13 (8 of 10)**; OPEN (measured) — WN7 (1.73E-1/1.95E-1, attractor-bound; loop structure is what remains; FD-turbo standing escalation), WN8 (doubly blocked: coin-flip bootstrap basin + 9.3E-4 true-label model ceiling; needs a model-front leg). Phase B closeout artifacts stay open until the WN7/WN8 stories resolve.

### 2026-07-26 — B3.6: the scaffold mechanism ends the loop-structure question; the revert-path salvage rung ships — WN7 1.73E-1 → 5.63E-2

The WN7 design leg ([evidence/2026-07-26-phase-b36-wn7loop/](evidence/2026-07-26-phase-b36-wn7loop/)), registered as an architecture decision — what "loop structure" means (the channel-laundering path P-chan vs the extrinsic-agreement path P-prior), three candidates with mechanisms and kill bars, instruments before code. The instruments answered in one day. M1's perturbed-restart ensemble (8 registered seeds) destroyed every deep convergence uniformly — a 2% info-domain flip re-encodes into ~7–8% of wire labels spread UNIFORMLY by the interleaver — and the analysis of that destruction is the leg's discovery: **convergence rides a healthy-frame scaffold.** Per-frame first-pass label-error distributions separate the converge/wander split perfectly (every converged block ≥35 of 64 frames under 20% label error, every reverting block ≤33); even the b7-vs-b9 "trajectory luck" pair dissolves (33 vs 36 scaffold frames). Restarts cannot manufacture scaffold — C1 dead on bar and mechanism; C3 dead with it. The label-free selector doesn't exist either (M1a: probe-priced residuals are channel-state-dominated).

C2a's frozen probe pass — label-free re-detection: probe-only TIR solve, probe-anchored h1, the solve's own shortening target as the chain echo model, probe-priced noise floor, no-prior exact-MAP chains — improves every corpse block without touching a label (deep starts 48–50% → 22–45%). Its registered basin-entry bar failed as a proxy (1/4 under 30%; disclosed), but the composed diagnostic overturned the proxy: frozen decodes seeding the shipped loop convert 3 of the 4 dead blocks (corpse 72,666 → 18,239, b4/b7 → 0, b5 → 3) — b4 converged from a 41% frozen start, proving the basin coordinate is frame-structural, not label-percentage. Amendment 1 (B3.5b precedent: consequence clauses overridden only by mechanism evidence, disclosed, judged on unseen data) registered the shipped form — **8PSK-only, revert-path-only salvage: on revert-at-cap, the frozen pass re-detects and a fresh soft loop runs from that seed; fixed point accepted, else revert as before; converging blocks bit-identical by construction** — and judged it held-out: w1/b0 (baseline banked pre-implementation) 90,829 → 54,616 (0.60× vs the 0.75 bar), two exact-0 conversions, zero per-block regressions. Guards: WN7 corpse re-pinned at 18,239 / 10c/1r/3v (oracle b5:15 unchanged), WN6/WN13sp numerically identical, suite 697/0.

Battery (32/32 legs, WN0 legs gated for the first time — green at the exact B3.5b digits): **WN7 Poor 1.73E-1 → 5.63E-2 canonical, 1.95E-1 → 6.14E-2 disjoint**; per-burst, three canonical bursts and one disjoint burst collapse to exact ZERO, no burst regressed in either family; **every non-WN7 census byte-identical to the #90 baseline (72 files), AWGN ×10/static/Doppler unchanged, gated eight at their exact digits.** The loop-structure question closes ANSWERED: the wander attractor is escapable exactly when a label-free start gets inside the basin — the binding constraint has moved from loop structure to label-free detection quality on scaffold-poor blocks, which is FD-turbo's question (exact per-bin channel application vs the single-lag chain constraint) and opens as its own registered design leg only from this measured base. Instrument lesson banked: WN7 bars are set in scaffold terms from now on.

Scoreboard: **GATED (hard, default) — WN0, WN1, WN2, WN3, WN4, WN5, WN6, WN13 (8 of 10)**; OPEN (measured) — WN7 (**5.63E-2/6.14E-2**, salvage shipped; residual = scaffold-poor blocks + the oracle-floor model tail; FD-turbo design leg is the registered next step), WN8 (doubly blocked: coin-flip bootstrap basin + 9.3E-4 true-label model ceiling; needs a model-front leg). Phase B closeout artifacts stay open until the WN7/WN8 stories resolve.

### 2026-07-26 — B3.7: the FD-turbo escalation resolves into the pre-cursor discovery; exact pre-cursor chains ship — WN7 5.63E-2 → 3.38E-2, the canonical corpse at ZERO

The registered C2b resolution leg ([evidence/2026-07-26-phase-b37-wn7-fdturbo/](evidence/2026-07-26-phase-b37-wn7-fdturbo/)) opened with an architecture verdict: per-bin FD MMSE is a LINEAR application, dominated by the exact chain BCJR at equal channel knowledge — every deficiency it would fix on the Poor channel is a channel-KNOWLEDGE gap of the label-free frozen pass (registered revival condition: post-FF response beyond the straddle pair; measured unmet). M0 recalibrated the basin edge in frozen-scaffold terms (converging seeds ≥53 of 64 frames under 20% wire error, failing ≤51 — perfectly clean across the 9 banked salvage attempts). M1 then killed the pair candidate (unestablishable from 40 probe rows, 0.3% acceptance) and found the real thing: **the K=32 mini-probe is a base-16 sequence cyclically extended (Table D-XXI), so probe-row regressors repeat mod 16 — and on frames where the lock rode the LATER Watterson path, the earlier path's −4.8 T pre-cursor aliases into an accepted causal lag 11** (~27% of frozen acceptances; the oracle picks lag 11 zero times; the label-full solve is immune because scrambled data rows are aperiodic).

Two intervention-tested negatives sharpened the mechanism before the fix. E1′ (Amendment 1, burst-consensus constrained solve) went RED — constraining every frame to the burst's physical lag 5 made EVERY block worse, and the per-frame damage attribution proved why: 5→5 frames moved +0.0 exactly, alias→5 frames +150/+180 bits/frame (the aliased solve's FF was correctly fitting the true two-path response through the folded column), 0→5 frames +76/+110 (the ln 12 margin was calibrated protection, not starvation), and alias→0 frames IMPROVED −30/−38 (the bogus causal +11 chain model is worse than no echo model). The free solve's per-frame lag choices are frame-local channel truth. Amendment 2 therefore fixed the CHAIN side: **an accepted lag beyond half the probe period decodes as a pre-cursor at −(period−lag), and the chains run EXACTLY on that structure via an observation shift** — o[u] = y[u−d] couples x[u] (pre-cursor coefficient, rotor-free in the cursor slot) and x[u−d] (h1, usual rotor fold) — the same two-tap exact-MAP machinery with the roles swapped; solve, FF, anchors, floor all unchanged. Sub-form (a) (alias-priced null) and (b) (exact chains) both converted w0/b0's b10 — the block no lever had moved in two legs — taking **the canonical corpse burst 72,666 → 18,239 → 0 (11c/0r/4v)**; (b) is the strictly better detector (21 of 22 blocks) and shipped as the frozen-pass default under Amendment 3, judged on unseen data: **held-out w2/b1 (banked pre-design, untouched by B3.6 salvage) 54,609/0v → 18,233/2v, two conversions, zero regressions.** Guards re-pinned (WN7 corpse 0 / 11c/0r/4v + oracle b5:15; WN6 0/11c/0r; WN13sp 0/11c/0r; suite 697/0); flag-off bit-identity proven twice (once by accident).

Battery (32/32, zero retries): **WN7 canonical 5.63E-2 → 3.38E-2 (turbo 82c/6r), disjoint 6.14E-2 → 2.23E-2 (84c/4r)** — canonical landing within 0.1% of the registered prediction; ~10 blocks converted across the families, NO burst regressed, five of eight canonical bursts now exactly zero (plus disjoint 10508 at 16 and 3011508 at 0); **every non-WN7 census byte-identical to the #92 baseline (72 files), gated eight at their exact digits, AWGN ×10/static/Doppler green.** Cumulative across B3.6+B3.7: canonical 5.1×, disjoint 8.7×. The C2b escalation closes RESOLVED — what it was pointing at was the one channel shape the causal chain model couldn't express, hidden behind a periodic-probe alias; the label-free pass now handles both path orderings of the Watterson channel.

Scoreboard: **GATED (hard, default) — WN0, WN1, WN2, WN3, WN4, WN5, WN6, WN13 (8 of 10)**; OPEN (measured) — WN7 (**3.38E-2/2.23E-2**; residual = the w1-trio class: frozen scaffold 43–51 vs the ~52 edge, worst-half-mean ~31% — the mid-frame fade-null profile; **E2 anchor-track h1 is the registered next arm**: the anchor series oversamples the 1 Hz fade 4×, the 2-point interpolator throws it away, M1b measured the h1 error at 7–10% RMS median / 13–21% p90), WN8 (doubly blocked: coin-flip bootstrap basin + 9.3E-4 true-label model ceiling; needs a model-front leg). Phase B closeout artifacts stay open until the WN7/WN8 stories resolve.

### 2026-07-26 — B3.8: the E2 gate refutation buys the lock-geometry discovery; the late-lock salvage rung ships — WN7 3.38E-2 → 2.56E-5, zero reverts in both families

The registered E2 leg ([evidence/2026-07-26-phase-b38-wn7-anchortrack/](evidence/2026-07-26-phase-b38-wn7-anchortrack/)) opened by killing its own premise at the offline gate, at zero demodulator cost: the gauge-stitched anchor track (cubic and every smoothing variant) is a wash against oracle truth at operational spacing — the label-free hold-out improves 20% at DOUBLE spacing (the stitch works), but at 4× oversampling curvature is negligible, and the direct decomposition put the frozen h1 model error at **~2% of the priced floor with a flat position profile** (superseding B3.7's indirect "model error at the floor" arithmetic). G2 and positional G4 struck as measured-dead; no code built. The same instrument set then found what does bind: a first-ever w1/b0 oracle run proved the trio's basin exists (b2/b7/b8 → 0 with labels), and the per-frame correlation inverted every prior story — **the catastrophic frames (40–67% wire error) are causal-lag acceptances that AGREE with the oracle's lag, carrying |c| = 1.0–1.3 and priced floors 30–80× normal**: when the delayed Watterson path dominates the cursor, the early-lock feedback-free FF cannot equalize the frame, the probe residual explodes, and the chain drowns the frame in honestly-priced garbage. The echo-rejecting (lag0) frames run clean at 5–10% (the FF suppressed the echo), and the natural pre-cursor frames — the SAME physics with the lock ridden onto the late path — are the best class in the pass (2–4%, n ≈ 0.02). Triggered frames carry 65–74% of the trio's frozen error mass.

E3 shipped through three pre-registered amendments. The offer (Amendment 1): on every causal accept, re-train with the equalizer window shifted by the accepted lag — the shift performs the re-lock and the tap shape carries over, so the ridge anchor stays approximately right — solve only the aliased pre-cursor lag (single-candidate margin), and keep the geometry that prices the lower floor; winning frames run the existing §B3.7 pre-cursor chains with the shift threaded through. The margin (Amendment 2, after the full form converted the whole trio but wobbled w0/b0's ceiling block b5 by 3 bits): adopt only on decisive floor improvement (0.5; the target class clears 7–38×) — and the margin runs proved the b5 wobble is NOT marginal flipping but fixed-point sensitivity of a model-ceiling block to ANY seed change, which no arbitration can fix. The rung (Amendment 3): scope the offer to a SECOND salvage rung — only blocks whose standard salvage fails get the late-lock seed, so converging blocks are structurally untouched. All predictions green: w0/b0 bit-identical (0 / 11c/0r/4v), WN6/WN13sp bit-identical, **w1/b0 54,616 → 20 / 11c/0r/5v — all three trio blocks convert and the residue IS the burst's oracle floor** (b3:6 + b6:14); trio frozen scaffold 43/51/50 → 53/64/61, across the ~52 edge. Ship bar: suite 697/0; guards exact; new pins w1/b0 corpse 20 / 11c/0r/5v and WN0 w2/b97 (SEED=500) 0 coded (the banked E1 guard candidate, finally in the set); **both pre-design held-outs convert unseen** — dj-w1/b0 17,897 → 12, dj-w3/b0 17,871 → 14, both 10c/1r → 11c/0r, zero regressions.

Battery (32/32, zero retries): **WN7 canonical 3.38E-2 → 2.56E-5 (83 errors, turbo 88c/0r), disjoint 2.23E-2 → 1.48E-5 (48 errors, 88c/0r)** — ZERO reverts in either family, every block of all 16 bursts converges, all four disjoint one-block leftovers convert (two to exact zero), NO burst regressed; **every non-WN7 census byte-identical to the #93 baseline (72 files), gated eight at their exact digits, AWGN ×10/static/Doppler green, WN8 unchanged** (QAM16 — structurally outside the rung). Cumulative B3.6 → B3.8: canonical 6,760×, disjoint 13,180×. WN7 is not yet at mask (pooled ≈ 2.0E-5 vs 1E-5, 1.5–2.6× above the line) — but the residual changed KIND: with zero reverts the detection/salvage story CLOSES, and what remains is the converged-decode model-floor tail (the oracle's own residue class), a model-refinement question (per-segment echo/noise pricing at the tail), not a detection question. Banked negative: margins cannot stabilize a ceiling block's fixed point — only structural scoping retains exactly.

Scoreboard: **GATED (hard, default) — WN0, WN1, WN2, WN3, WN4, WN5, WN6, WN13 (8 of 10)**; OPEN (measured) — WN7 (**2.56E-5/1.48E-5**, zero reverts; within 2.6× of the mask; the remaining question is the converged model-floor tail — the B4.1 SPIKE-UP pricing family and the oracle's per-segment echo model are the registered directions, and a §B4-style flip needs ~2–2.6× more), WN8 (doubly blocked: coin-flip bootstrap basin + 9.3E-4 true-label model ceiling; needs a model-front leg). Phase B closeout artifacts stay open until the WN7/WN8 stories resolve.
